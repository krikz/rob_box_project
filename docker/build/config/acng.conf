# APT-Cacher NG configuration
# Минимальная конфигурация для новой версии apt-cacher-ng

# Директория для кэша
CacheDir: /var/cache/apt-cacher-ng

# Порт для HTTP прокси
Port: 3142

# Логирование
LogDir: /var/log/apt-cacher-ng

# Время жизни кэша (в днях)
ExThreshold: 4

# Разрешить удаление файлов через веб-интерфейс
AllowUserPorts: 0

# Стандартные зеркала (упрощенные)
Remap-debrep: file:deb_mirror*.gz /debian ; file:backends_debian
Remap-uburep: file:ubuntu_mirrors /ubuntu ; file:backends_ubuntu

# Разрешить HTTPS соединения
PassThroughPattern: .*

# Отчеты доступны по http://localhost:3142/acng-report.html
ReportPage: acng-report.html
