# depthai_ros_driver v2.12.2-humble configuration
# Node path: /camera/camera (namespace='camera', name='camera')
# Topics published with i_rs_compat:true:
#   /camera/camera/color/image_raw      — colour image
#   /camera/camera/color/camera_info    — colour camera_info
#   /camera/camera/depth/image_rect_raw — aligned depth (rs_compat renames stereo→depth)
#
# Bandwidth optimizations (2026-03-03):
#   i_low_bandwidth: true  — on-chip H.264 encoding on OAK-D before USB transfer
#                            reduces eth0 Vision Pi → Main Pi from ~89 MB/s to ~15-20 MB/s
#                            source: https://docs.luxonis.com/software/ros/depthai-ros/driver/
#   i_enable_lazy_publisher: true — skip publishing when no subscriber
#   i_max_q_size: 1               — minimal buffer = lower latency
#
# Resolution / FPS tradeoffs (2026-03-04):
#   RGB compressed (JPEG) over Zenoh: ~1.0 MB/s at 640px 10fps (снижено с 15fps)
#   RGB relay raw (local on Main Pi): 640*400*3*10fps = 7.7 MB/s
#   10fps: меньше нагрузка на CPU rtabmap (87%→~60%), меньше фризов в RViz
#   Visual SLAM works well at 640x400 — adequate for loop closure
#   NOTE: i_width/i_height requires i_output_isp: false (video output, not ISP)
#         For RGBD alignment: width + height MUST be divisible by 16! 640%16=0 ✓, 400%16=0 ✓

/camera/camera:
  ros__parameters:
    camera:
      i_nn_type: none              # КРИТИЧЕСКИ ВАЖНО: отключаем нейронную сеть
      i_pipeline_type: "RGBD"
      i_rgb_resolution: "720p"     # Capture 720p, video output crop до 640x400
      i_stereo_width: 640
      i_stereo_height: 400          # Aligned to RGB video output size (must match)
      i_fps: 10.0                  # 10fps: H.264 compressed ~1.0 MB/s over Zenoh, снижена с 15fps
      i_enable_ir: false
      i_laser_dot_brightness: 0
      i_floodlight_brightness: 0
      i_publish_tf_from_calibration: false
      i_tf_base_frame: "camera_link"
      i_enable_imu: true           # IMU для RTAB-Map ICP odometry
      i_enable_preview: false
      i_enable_diagnostics: false
      i_usb_speed: "SUPER"
      i_rs_compat: true              # RealSense-совместимые имена топиков:
                                   # rgb→color, stereo→depth
                                   # ВАЖНО: секции параметров тоже переименовываются!
    color:    # rs_compat: rgb→color (секция параметров)
      i_low_bandwidth: true            # H.264 on-chip encoding → ~300 KB/s s JPEG vs ~7 MB/s raw
      i_low_bandwidth_quality: 85      # 85% quality — достаточно для AprilTag + SLAM
      i_publish_compressed: false
      i_fps: 10.0                      # Снижено с 15fps: меньше CPU на rtabmap, меньше сетевая нагрузка
      i_max_q_size: 1                  # минимальный буфер, меньше задержка
      i_interleaved: false
      i_board_socket_id: 0
      i_enable_lazy_publisher: true    # не публиковать без подписчиков
      i_publish_topic: true
      i_keep_preview_aspect_ratio: false
      i_width: 640                     # Video output 640x400: relay 640*400*3*10fps = 7.7 MB/s
      i_height: 400                    # 10fps: снижена нагрузка CPU rtabmap и сети
      i_output_isp: false              # Must be false to use i_width/i_height (VIDEO output, not ISP)
                                       # NOTE: width+height must be div by 16 for RGBD alignment!
    depth:    # rs_compat: stereo→depth (секция параметров)
      i_low_bandwidth: true            # H.264 on-chip encoding для depth → ~1-2 MB/s compressed
      i_low_bandwidth_quality: 90      # depth: выше quality чем RGB — SLAM чувствителен к артефактам
      i_publish_compressed: false
      i_fps: 10.0                      # Sync с RGB fps (снижено с 15)
      i_max_q_size: 1
      i_enable_lazy_publisher: true
      i_publish_topic: true
      i_output_disparity: false
      i_get_base_device_timestamp: false
      i_add_exposure_offset: false
      i_exposure_offset: 0
      i_alpha_scaling: 0.0
      i_align_depth: true          # Align depth to RGB frame for RGBD SLAM
    infra1:    # rs_compat: left→infra1
      i_low_bandwidth: false
      i_fps: 10.0
      i_max_q_size: 1
      i_enable_lazy_publisher: true    # не используется rtabmap напрямую
      i_publish_topic: false           # отключаем left/right raw — не нужны для SLAM
    infra2:    # rs_compat: right→infra2
      i_low_bandwidth: false
      i_fps: 10.0
      i_max_q_size: 1
      i_enable_lazy_publisher: true
      i_publish_topic: false           # отключаем left/right raw — не нужны для SLAM
