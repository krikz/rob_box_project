# Vision Pi image versions — staging (develop branch)
# Managed automatically by CI (L-Build Single Service workflow)

OAK_D_TAG=dev
RTABMAP_SYNC_TAG=dev
LED_MATRIX_TAG=dev
CEILING_CAMERA_TAG=dev
VOICE_RESOURCES_TAG=dev
VOICE_ASSISTANT_TAG=dev
TELEGRAM_BOT_TAG=dev
