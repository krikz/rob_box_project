node_modules/
dist/
dist-types/
*.log
.DS_Store
.vite/
coverage/

# glTF asset pipeline (Phase 2.0 — see ADR-0032):
# raw glTF sources are produced externally (Blender export / Quaternius /
# Khronos Sample Assets). Only the *optimized* outputs may be committed.
# The gltf:verify CI guard will reject any *.glb that lacks Draco + Meshopt.
public/models/**/*.glb
public/models/**/*.gltf
# Phase 2.0: optimized outputs (Draco + Meshopt) may be committed.
!public/models/**/*.optimized.glb
# Phase 2.1: KTX2 texture outputs (Draco + Meshopt + Basis/KTX2) may be
# committed too — same rationale (CC0 source, optimizer pipeline).
!public/models/**/*.ktx2.glb