"""rob_box_quest — WebXR / Meta Quest telepresence service for Rob Box.

Phase 1.1: pure wire-protocol implementation (frame codec, topic registry).
Subsequent phases add the aiohttp WSS server, ROS/Zenoh bridge, streams,
and the Three.js web client.
"""

__version__ = "0.1.0"
