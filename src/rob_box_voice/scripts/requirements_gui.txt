# Зависимости для Silero TTS GUI
# Графический настройщик голоса

# Основные зависимости
# GUI пинует проверенную пару версий; держим ее синхронно с runtime floor в
# voice_base/voice_assistant requirements.
torch==2.9.0
torchaudio==2.9.0
sounddevice>=0.4.6
numpy>=1.24.0

# GUI (обычно уже установлен с Python)
# tkinter - встроен в Python, но на Linux может потребоваться:
# sudo apt-get install python3-tk
