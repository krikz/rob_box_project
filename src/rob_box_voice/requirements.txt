# Python dependencies для rob_box_voice

# === Аудио ===
pyaudio>=0.2.14
sounddevice>=0.5.5
numpy>=2.5.1
pydub>=0.25.1  # MP3 processing для sound effects

# === USB интерфейс для ReSpeaker ===
pyusb>=1.3.1
pixel-ring>=0.1.0  # LED control для ReSpeaker
spidev>=3.8  # SPI interface для pixel_ring

# === STT (Speech-to-Text) ===
# Vosk - Fast, offline, real-time (RECOMMENDED)
vosk>=0.3.45

# Whisper - High accuracy, offline (alternative)
openai-whisper>=20250625
faster-whisper>=1.2.1  # Faster inference via CTranslate2

# Yandex Cloud STT (fallback, online)
grpcio>=1.83.0
grpcio-tools>=1.83.0
protobuf>=7.35.1

# === TTS (Text-to-Speech) ===
# Piper - Fast, high quality, offline (RECOMMENDED)
piper-tts>=1.6.0

# Silero - Alternative offline TTS with multiple voices
torch>=2.13.0  # CPU only for ARM64
torchaudio>=2.0.0
omegaconf>=2.3.1

# Yandex Cloud TTS (fallback, online)
# Uses same grpcio deps as STT

# === Dialogue & LLM ===
# DeepSeek / OpenAI compatible API
openai>=2.53.0

# === Утилиты ===
pyyaml>=6.0.3
requests>=2.34.2
openpyxl>=3.1.5

# === Долгосрочная память (VoiceMemory) ===
# sqlite-vec: векторный поиск для SQLite (опционально, graceful degradation)
# Требует Ollama для генерации эмбеддингов: docker compose --profile ai up ollama
sqlite-vec>=0.1.9

# === Observability (issue #1160) ===
# Prometheus client: /metrics endpoint для Grafana. Optional — если пакет
# не установлен, rob_box_voice.observability превращается в no-op.
prometheus_client>=0.20.0

# === OpenTelemetry traces (issue #1234, этап 2) ===
# OTel SDK + OTLP-экспортёр + httpx auto-instrumentation. Optional — если
# пакетов нет, rob_box_voice.observability.tracing превращается в no-op.
# Endpoint задаётся через OTEL_EXPORTER_OTLP_ENDPOINT (default
# http://10.1.1.249:4317 — otel-collector на мониторинг-машине).
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp>=1.24.0
opentelemetry-instrumentation-httpx>=0.45b0
