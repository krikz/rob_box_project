---
phase: 06-harness-p0-finalization
plan: 07
type: execute
wave: 4
depends_on: ["06-05", "06-06"]
files_modified:
  - src/rob_box_harness/test/test_dialog_harness.py
  - src/rob_box_harness/test/test_telegram_harness.py
  - src/rob_box_harness/test/test_mcp_tools.py
  - src/rob_box_harness/test/test_dialogue_state_machine.py
  - src/rob_box_harness/test/test_ros2_transport.py
  - src/rob_box_harness/test/test_sqlite_voice_memory.py
autonomous: true
requirements:
  - TEST-DIALOG-14
  - TEST-TG-15
  - TEST-MCP-16

must_haves:
  truths:
    - "DialogueNode test coverage reaches 80%+ (via DialogHarness + DSM tests)"
    - "TelegramNode test coverage reaches 50%+ (via TelegramHarness tests)"
    - "MCP tools test coverage reaches 70%+"
    - "All new harness code from W8-W13 has corresponding unit tests"
  artifacts:
    - path: "src/rob_box_harness/test/test_dialog_harness.py"
      provides: "Unit tests for DialogHarness and DialogueStateMachine"
      min_lines: 100
    - path: "src/rob_box_harness/test/test_telegram_harness.py"
      provides: "Unit tests for TelegramHarness"
      min_lines: 80
    - path: "src/rob_box_harness/test/test_mcp_tools.py"
      provides: "Unit tests for MCP tool functions"
      min_lines: 60
    - path: "src/rob_box_harness/test/test_dialogue_state_machine.py"
      provides: "Pure unit tests for DSM state transitions"
      min_lines: 50
    - path: "src/rob_box_harness/test/test_ros2_transport.py"
      provides: "Unit tests for ROS2Transport"
      min_lines: 40
    - path: "src/rob_box_harness/test/test_sqlite_voice_memory.py"
      provides: "Unit tests for SQLiteVoiceMemory"
      min_lines: 50
  key_links:
    - from: "test_dialog_harness.py"
      to: "harnesses/dialog.py (DialogHarness)"
      via: "import + pytest fixtures"
      pattern: "from.*dialog import DialogHarness"
    - from: "test_telegram_harness.py"
      to: "harnesses/telegram.py (TelegramHarness)"
      via: "import + pytest fixtures"
      pattern: "from.*telegram import TelegramHarness"
---

<objective>
Close test coverage gaps: DialogueNode 9%→80%+ (via DialogHarness+DSM tests), TelegramNode 0%→50%+ (via TelegramHarness tests), MCP tools →70%+. Also add unit tests for new W8-W13 code (ROS2Transport, SQLiteVoiceMemory, DialogueStateMachine).

Purpose: Per SPEC_CURRENT C1–C3, all critical modules must reach minimum coverage thresholds. The harness framework provides fake ports (DummyLLMProvider, FakeToolProvider, InMemoryStore, FakeTransport, RecordingBus, NoopBus) that make unit testing cheap — no ROS2, no network, no Telegram bot needed.

Output: 6 test files with comprehensive coverage of all Phase 6 harness code.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-harness-p0-finalization/06-CONTEXT.md
@.planning/phases/06-harness-p0-finalization/06-RESEARCH.md
@src/rob_box_harness/README.md

<interfaces>
Existing test infrastructure (from src/rob_box_harness/test/):
- conftest.py — pytest fixtures: harness_config, echo_harness, etc.
- test_harness_lifecycle.py — lifecycle tests (existing pattern)
- Uses pytest (6.2.5), pytest-asyncio (≥0.21), pytest-cov (≥4.0)
- pytest.ini at repo root: asyncio_mode=auto

Available fakes for test injection (from rob_box_harness):
- DummyLLMProvider — returns canned responses, no API call
- FakeToolProvider — register/execute tools in-memory
- InMemoryStore — list-of-dicts memory for turns/facts
- FakeTransport — fire events programmatically
- RecordingBus — record dispatched side-effects
- NoopBus — silent side-effect sink
- MockClock — frozen time for deterministic tests
- EchoHarness, UpperHarness — reference test patterns

From RESEARCH.md §Test Coverage Strategy:
- W14: DialogHarness + DSM tests — use fake ports, test state transitions, tool dispatch, response formatting, error handling, barge-in
- W15: TelegramHarness tests — mock python-telegram-bot, fake ports for LLM/MCP/Memory, test command routing, message handling, auth middleware
- W16: MCP tools — test each tool function with mock ROS2, test discover/execute, error handling, timeout

Coverage targets:
- pytest.ini has coverage gate at 85% for rob_box_harness
- Run tests with: pytest src/rob_box_harness/test/ -v --cov=rob_box_harness --cov-report=term
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task W14: DialogHarness + DialogueStateMachine unit tests — target 80%+ coverage</name>
  <files>src/rob_box_harness/test/test_dialog_harness.py, src/rob_box_harness/test/test_dialogue_state_machine.py</files>
  <read_first>
    - src/rob_box_harness/test/conftest.py — existing fixtures, test patterns
    - src/rob_box_harness/test/test_harness_lifecycle.py — existing lifecycle test pattern
    - src/rob_box_harness/rob_box_harness/harnesses/dialog.py — DialogHarness implementation (W8)
    - src/rob_box_harness/rob_box_harness/core/dialogue_state_machine.py — DSM implementation (W9)
    - src/rob_box_harness/rob_box_harness/tools.py — FakeToolProvider
    - src/rob_box_harness/rob_box_harness/memory.py — InMemoryStore
    - src/rob_box_harness/rob_box_harness/effects.py — RecordingBus, NoopBus
    - pytest.ini — asyncio_mode, coverage config
  </read_first>
  <action>
Create test files for DialogHarness and DialogueStateMachine. Use ONLY fake ports — no ROS2, no network, no real LLM API.

**test_dialogue_state_machine.py** (pure sync tests, no asyncio needed):
- Test all 8 DSM transitions: IDLE+wake→LISTENING, LISTENING+stt→DIALOGUE, DIALOGUE+end→IDLE, any+silence→SILENCED, SILENCED+timeout→IDLE, LISTENING+timeout→IDLE, DIALOGUE+wake→LISTENING (barge-in), SILENCED+unsilence→IDLE
- Test on_user_input classification: "роббокс"→WAKE_WORD, "тихо"→SILENCE_COMMAND, "привет"→STT_RESULT, "молчи"→SILENCE_COMMAND, "замолчи"→SILENCE_COMMAND, "роб"→WAKE_WORD
- Test silence timeout: set silence_timeout=0.1, enter SILENCED, verify state changes after timeout
- Test initial state: DialogueStateMachine() starts in IDLE

**test_dialog_harness.py** (async tests, use pytest-asyncio):
- Fixture: `dialog_harness` — creates DialogHarness with DummyLLMProvider, FakeToolProvider, InMemoryStore, RecordingBus, FakeTransport, MockClock
- Test: `test_basic_turn` — step("hello") returns a string response, state.turn_count increments
- Test: `test_wake_word_transition` — step("роббокс") triggers IDLE→LISTENING transition
- Test: `test_silence_command` — step("тихо") triggers SILENCED state, subsequent messages ignored
- Test: `test_tool_dispatch` — register a fake tool, verify tool is called when LLM returns tool_call
- Test: `test_memory_persistence` — step("hello"), check InMemoryStore has 2 turns (user+assistant)
- Test: `test_side_effects_dispatched` — step("play music"), check RecordingBus has TTS effect
- Test: `test_idempotent_init` — call harness.init() twice, no error
- Test: `test_idempotent_teardown` — call harness.teardown() twice, no error
- Test: `test_llm_error_fallback` — inject a failing LLMProvider, verify graceful degradation
- Test: `test_lifecycle_context_manager` — `async with harness as h:` pattern works

Minimum: 12 test functions across both files. Target 80%+ line coverage on dialog.py + dialogue_state_machine.py.
  </action>
  <verify>
    <automated>pytest src/rob_box_harness/test/test_dialog_harness.py src/rob_box_harness/test/test_dialogue_state_machine.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - test_dialogue_state_machine.py: all 8 transitions tested, on_user_input classification tested, silence timeout tested
    - test_dialog_harness.py: 10+ async tests covering basic turn, state transitions, tool dispatch, memory, side-effects, lifecycle, error handling
    - All tests pass (pytest exit 0)
    - Coverage on dialog.py + dialogue_state_machine.py >= 80%
  </done>
</task>

<task type="auto">
  <name>Task W15: TelegramHarness unit tests — target 50%+ coverage</name>
  <files>src/rob_box_harness/test/test_telegram_harness.py</files>
  <read_first>
    - src/rob_box_harness/rob_box_harness/harnesses/telegram.py — TelegramHarness implementation (W11)
    - src/rob_box_harness/test/test_dialog_harness.py — test patterns from W14
    - src/rob_box_harness/rob_box_harness/tools.py — FakeToolProvider
    - src/rob_box_harness/rob_box_harness/memory.py — InMemoryStore
    - src/rob_box_harness/rob_box_harness/effects.py — RecordingBus
  </read_first>
  <action>
Create `src/rob_box_harness/test/test_telegram_harness.py` with async tests for TelegramHarness. Use fake ports.

Tests:
1. `test_basic_text_message` — send text "hello" via step(), verify LLM response returned
2. `test_command_dispatch` — send command="/start", verify command registry dispatches correctly
3. `test_command_with_args` — send command="/navigate kitchen", verify args parsed
4. `test_auth_anonymous` — user not in allowed list → returns "unauthorized"
5. `test_auth_authenticated` — user in allowed list → processes normally
6. `test_auth_admin` — admin user → processes with elevated access
7. `test_memory_persistence` — send 2 messages, verify InMemoryStore has 4 turns
8. `test_snapshot_store` — store image, retrieve by key, verify bytes match
9. `test_snapshot_expire` — store image, advance clock, verify expired image returns None
10. `test_command_registry_unknown` — unknown command → graceful fallback response
11. `test_telegram_state_tracking` — state tracks chat_id, user_id, message_count
12. `test_idempotent_init_teardown` — double init/teardown, no errors

Minimum: 10 test functions. Target 50%+ line coverage on telegram.py.
  </action>
  <verify>
    <automated>pytest src/rob_box_harness/test/test_telegram_harness.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 10+ async tests covering text messages, commands, auth, memory, snapshots, state tracking, lifecycle
    - All tests pass (pytest exit 0)
    - Coverage on telegram.py >= 50%
  </done>
</task>

<task type="auto">
  <name>Task W16: MCP tools + ROS2Transport + SQLiteVoiceMemory unit tests — target 70%+</name>
  <files>src/rob_box_harness/test/test_mcp_tools.py, src/rob_box_harness/test/test_ros2_transport.py, src/rob_box_harness/test/test_sqlite_voice_memory.py</files>
  <read_first>
    - src/rob_box_harness/rob_box_harness/tools.py — ToolProvider ABC, FakeToolProvider, ToolSpec, ToolCall, ToolResult
    - src/rob_box_harness/rob_box_harness/transport/ros2_transport.py — ROS2Transport (W12)
    - src/rob_box_harness/rob_box_harness/memory/sqlite_voice.py — SQLiteVoiceMemory (W13)
    - src/rob_box_mcp_tools/ — existing MCP tools (for reference on tool functions)
  </read_first>
  <action>
Create three test files for remaining Phase 6 code.

**test_mcp_tools.py:**
- Test FakeToolProvider.register() and execute() — verify tool is registered and callable
- Test ToolProvider.discover() — returns correct ToolSpec list
- Test tool execution with valid args — tool returns expected ToolResult
- Test tool execution error handling — invalid args → ToolError with message
- Test concurrent tool execution — two tools can be called independently
- Test tool timeout — long-running tool handled gracefully
- Target 70%+ coverage on tools.py

**test_ros2_transport.py:**
- Since rclpy may not be available in test env, test the ROS2_AVAILABLE flag
- Test import without rclpy: verify ROS2Transport class exists but flags unavailable
- Test FakeTransport can be used as drop-in replacement
- Test event bridging pattern: subscribe → fire event → callback invoked

**test_sqlite_voice_memory.py:**
- Test init with :memory: database (no file I/O needed)
- Test append_turn and load_recent: append 3 turns, load 2 most recent, verify order
- Test idempotent append: append same turn twice, verify only 1 stored
- Test save_fact and search_facts: save 3 facts, search by keyword, verify results
- Test fact upsert: save fact with same key → value updated, not duplicated
- Test scope isolation: turns in scope "A" not visible in scope "B"
- Test teardown: close DB, verify subsequent operations raise appropriate error
- Test init_idempotent: call init() twice, no duplicate tables created

Minimum: 6 tests in test_mcp_tools.py, 3 in test_ros2_transport.py, 8 in test_sqlite_voice_memory.py.
  </action>
  <verify>
    <automated>pytest src/rob_box_harness/test/test_mcp_tools.py src/rob_box_harness/test/test_ros2_transport.py src/rob_box_harness/test/test_sqlite_voice_memory.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - test_mcp_tools.py: 6+ tests covering tool registration, execution, discovery, error handling, concurrency, timeout
    - test_ros2_transport.py: 3+ tests covering ROS2 availability flag and FakeTransport drop-in
    - test_sqlite_voice_memory.py: 8+ tests covering CRUD, idempotency, upsert, scope isolation, lifecycle
    - All tests pass (pytest exit 0)
    - Coverage on tools.py >= 70%, sqlite_voice.py >= 80%
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fake ports → harness code | Fakes simulate production behavior; ensure fakes match real port contracts |
</threat_model>

<verification>
```bash
# Run all tests in this plan
pytest src/rob_box_harness/test/test_dialog_harness.py \
       src/rob_box_harness/test/test_dialogue_state_machine.py \
       src/rob_box_harness/test/test_telegram_harness.py \
       src/rob_box_harness/test/test_mcp_tools.py \
       src/rob_box_harness/test/test_ros2_transport.py \
       src/rob_box_harness/test/test_sqlite_voice_memory.py \
       -v --tb=short

# Coverage check
pytest src/rob_box_harness/test/ \
  --cov=rob_box_harness.harnesses.dialog \
  --cov=rob_box_harness.harnesses.telegram \
  --cov=rob_box_harness.harnesses.persistent \
  --cov=rob_box_harness.core.dialogue_state_machine \
  --cov=rob_box_harness.tools \
  --cov=rob_box_harness.memory.sqlite_voice \
  --cov=rob_box_harness.transport.ros2_transport \
  --cov-report=term
```
</verification>

<success_criteria>
- 6 test files created with 40+ total test functions
- All tests pass (pytest exit 0)
- DSM coverage: 80%+ (pure state machine, easy to cover)
- DialogHarness coverage: 80%+
- TelegramHarness coverage: 50%+
- MCP tools coverage: 70%+
- SQLiteVoiceMemory coverage: 80%+
- No ROS2, no network, no real LLM API used in any test
</success_criteria>

<output>
After completion, create `.planning/phases/06-harness-p0-finalization/06-07-SUMMARY.md`
</output>
