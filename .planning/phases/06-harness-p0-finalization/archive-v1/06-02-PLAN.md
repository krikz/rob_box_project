---
phase: 06-harness-p0-finalization
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - docs/architecture/minimax-tts-architecture.md
  - docs/architecture/minimax-tts-integration-design.md
  - SPEC_CURRENT.md
autonomous: true
requirements:
  - DOC-DEDUP-04
  - DOC-SPEC-05

must_haves:
  truths:
    - "docs/architecture/ contains only overview docs and cross-reference stubs — no duplicates of adr/ content"
    - "SPEC_CURRENT.md marks P0 as Done, describes P1, and has zero Hermes references"
  artifacts:
    - path: "docs/architecture/minimax-tts-architecture.md"
      provides: "Cross-reference stub (was merged into ADR-0003 in 06-01)"
      contains: "ADR-0003"
    - path: "docs/architecture/minimax-tts-integration-design.md"
      provides: "Cross-reference stub (was merged into ADR-0004 in 06-01)"
      contains: "ADR-0004"
    - path: "SPEC_CURRENT.md"
      provides: "Updated P0/P1 status with Hermes references removed"
      min_lines: 100
  key_links:
    - from: "docs/architecture/minimax-tts-architecture.md"
      to: "docs/adr/0003-minimax-tts-architecture.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(\\.\\.\\/adr\\/0003"
    - from: "SPEC_CURRENT.md"
      to: "docs/adr/0001-harness-architecture.md"
      via: "document reference"
      pattern: "0001-harness-architecture"
---

<objective>
Complete documentation consolidation: verify no duplicates remain in `docs/architecture/`, ensure cross-references between kept pairs, and update `SPEC_CURRENT.md` to reflect P0 completion and current P1 wave structure.

Purpose: Enforce D-01 single-source-of-truth (docs/adr/ canonical, docs/architecture/ overview-only). Execute D-06 SPEC update.

Output: Clean docs/architecture/ with cross-refs only, updated SPEC_CURRENT.md.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-harness-p0-finalization/06-CONTEXT.md
@.planning/phases/06-harness-p0-finalization/06-RESEARCH.md

<interfaces>
From CONTEXT.md D-01 keep pairs (NOT duplicates):
- docs/architecture/minimax-provider.md (overview) + docs/adr/0002-minimax-provider.md (ADR) — different docs, cross-link
- docs/guides/MINIMAX.md (LLM guide) + docs/guides/MINIMAX_TTS.md (TTS guide) + docs/guides/MINIMAX_TTS_GETTING_STARTED.md (quickstart) + docs/api/MINIMAX_TTS.md (API ref) — different docs, keep all

From CONTEXT.md D-06 (SPEC_CURRENT.md):
- Mark P0 as ✅ Done
- Describe P1: DialogHarness, PersistentHarness, TelegramHarness, ROS2Transport, SQLiteVoiceMemory (from ADR-0001 §2.7)
- Remove Hermes references: `hermes kanban create`, `kanban_complete`, `t_*` task IDs, worktree references
- Keep architectural diagram, constraints, and linked documents sections
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task W4: Remove duplicates from docs/architecture/, add cross-refs for kept pairs</name>
  <files>docs/architecture/minimax-tts-architecture.md, docs/architecture/minimax-tts-integration-design.md, docs/architecture/minimax-provider.md</files>
  <read_first>
    - docs/architecture/minimax-tts-architecture.md — stub created in 06-01 (W1)
    - docs/architecture/minimax-tts-integration-design.md — stub created in 06-01 (W2)
    - docs/architecture/minimax-provider.md — overview doc KEPT per D-01, needs cross-ref to ADR-0002
    - docs/adr/0002-minimax-provider.md — canonical ADR, needs cross-ref from architecture overview
    - docs/guides/MINIMAX.md — LLM guide, KEPT per D-01
    - docs/guides/MINIMAX_TTS.md — TTS guide, KEPT per D-01
  </read_first>
  <action>
Per D-01: docs/architecture/ holds ONLY overview docs with cross-references to adr/. NO duplicates.

Steps:
1. Verify stubs from W1/W2 are in place: `docs/architecture/minimax-tts-architecture.md` and `docs/architecture/minimax-tts-integration-design.md` already contain cross-refs to ADR-0003 and ADR-0004 respectively (created in 06-01). If not, create stubs now.
2. Read `docs/architecture/minimax-provider.md` (overview) and `docs/adr/0002-minimax-provider.md` (ADR). Per D-01 these are DIFFERENT documents — overview vs formal ADR. Add cross-reference at the top of `docs/architecture/minimax-provider.md`:

```markdown
> **ADR reference:** See [ADR-0002: MiniMax Provider](../adr/0002-minimax-provider.md) for the formal architecture decision record.
```

3. Add reciprocal cross-reference at the top of `docs/adr/0002-minimax-provider.md`:

```markdown
> **Overview:** See [MiniMax Provider Architecture](../architecture/minimax-provider.md) for a high-level architectural overview.
```

4. Verify no other file in `docs/architecture/` duplicates content from any `docs/adr/` file. Scan `docs/architecture/*.md` files for substantial content overlap with ADRs. If any found, convert to cross-reference stubs.

5. Confirm kept files (per D-01): `MINIMAX.md`, `MINIMAX_TTS.md`, `MINIMAX_TTS_GETTING_STARTED.md`, `api/MINIMAX_TTS.md` — these are guides/API docs, NOT architecture docs, NOT duplicates. Verify they exist. Do NOT modify them.
  </action>
  <verify>
    <automated>grep -c "ADR-0003\|ADR-0004" docs/architecture/minimax-tts-architecture.md docs/architecture/minimax-tts-integration-design.md && grep -c "ADR-0002" docs/architecture/minimax-provider.md && grep -c "architecture/minimax-provider" docs/adr/0002-minimax-provider.md && echo "W4 PASS"</automated>
  </verify>
  <done>
    - docs/architecture/minimax-tts-architecture.md is a cross-ref stub → ADR-0003
    - docs/architecture/minimax-tts-integration-design.md is a cross-ref stub → ADR-0004
    - docs/architecture/minimax-provider.md and docs/adr/0002-minimax-provider.md have reciprocal cross-refs
    - No other architecture/*.md files duplicate ADR content
    - Kept guides (MINIMAX.md, MINIMAX_TTS.md, MINIMAX_TTS_GETTING_STARTED.md, api/MINIMAX_TTS.md) are untouched
  </done>
</task>

<task type="auto">
  <name>Task W5: Update SPEC_CURRENT.md — P0→Done, describe P1, remove Hermes references</name>
  <files>SPEC_CURRENT.md</files>
  <read_first>
    - SPEC_CURRENT.md — current source of truth (version 1.1, 2026-07-24), ~200 lines
    - docs/adr/0001-harness-architecture.md — ADR-0001 §2.7 defines P1 harness scope
    - .planning/phases/06-harness-p0-finalization/06-CONTEXT.md — D-06/D-07 decisions, wave structure
  </read_first>
  <action>
Per D-06 and D-07: Update SPEC_CURRENT.md to reflect current reality. The file currently describes P0 as landed and P1 as future. Update it for Phase 6 finalization.

Changes to make:

1. **§1.1 Completed Stages table — add Phase 6 entry:**

```
| **Phase 6: Harness P0 Finalization** | Documentation consolidated (no duplicates, adr/ canonical), Docker integration, harness adapters (Dialog/Persistent/Telegram), port implementations (ROS2Transport/SQLiteVoiceMemory), test coverage gaps closed, PR #907 audit complete | ✅ Done |
```

2. **§1.2 Already Configured — remove Hermes references:**
- Remove: "18 профилей Hermes с backend: local, git: блоком, GITHUB_TOKEN"
- Remove: "Kanban-доска robbox привязана к /home/builder/hermes-share/rob_box_project"
- Remove: "Worktrees для активных воркеров (wt/t_2bf98118 ✅, wt/t_35cfe938 ✅ — оба слиты в wt/t_81622105 для документации)"
- Remove: "Ветка feature/harness-p0-foundation актуальна"
- Replace with: "**Ветка** `feature/harness-p0-foundation` — P0 завершён, готов к мержу в main (PR #907)"

3. **§2 Component Diagram — update P1 note:**
- Change "Adapters (P1 — следующий этап)" → "Adapters (P1 — Phase 6 implemented)"
- Change DA/PA/TA dashed edges (.uses.) → solid edges (-->)

4. **§3 ROADMAP — update statuses:**
- Mark A1 (PR comment) as ✅ Done
- Mark A2 as ✅ Done (review state APPROVE)
- Keep A3, A4 as pending (user action)
- Mark B1-B5 (harnesses) as ✅ Done
- Mark C1-C3 (test coverage) as ✅ Done
- Mark D1-D3 (documentation) as ✅ Done

5. **§4 Constraints — remove Hermes references:**
- Change "hermes kanban create ..." example → generic "gh issue create ..." equivalent
- Remove worktree constraint line ("Воркеры работают в своих worktree")
- Keep: "Не трогать main", "Не менять ADR-0001", "API-ключи — только через env"

6. **§5 How to work with this document — remove Hermes workflow:**
- Remove the `hermes kanban create` code block
- Replace with: "Work is tracked via GitHub Issues. Use `gh issue create` with appropriate labels."

7. **Update version:** `**Версия**: 2.0` and `**Дата**: 2026-07-27`

Do NOT remove: §2 diagram (update P1 status only), §6 linked documents (keep all), the architectural content.
  </action>
  <verify>
    <automated>grep -q "Phase 6: Harness P0 Finalization.*Done" SPEC_CURRENT.md && ! grep -qi "hermes" SPEC_CURRENT.md && ! grep -q "kanban_create\|kanban_complete" SPEC_CURRENT.md && ! grep -q "wt/t_" SPEC_CURRENT.md && grep -q "✅ Done" SPEC_CURRENT.md && echo "W5 PASS"</automated>
  </verify>
  <done>
    - P0 marked ✅ Done in SPEC_CURRENT.md §1.1 with Phase 6 entry
    - All Hermes references removed: no "hermes", "kanban", "t_*" task IDs, worktree paths
    - P1 harnesses marked as implemented in §2 diagram and §3 ROADMAP
    - Version updated to 2.0, date 2026-07-27
    - Constraints section: Hermes workflow replaced with gh CLI pattern
    - Linked documents section preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — documentation-only plan | No code, no network, no secrets |
</threat_model>

<verification>
Run from repo root:
```bash
# Verify no Hermes references remain
! grep -rqi "hermes" SPEC_CURRENT.md && echo "No Hermes refs OK"

# Verify cross-refs exist
grep -l "ADR-0002\|ADR-0003\|ADR-0004" docs/architecture/minimax-provider.md docs/architecture/minimax-tts-architecture.md docs/architecture/minimax-tts-integration-design.md

# Verify SPEC_CURRENT has Phase 6 Done
grep "Phase 6.*Done" SPEC_CURRENT.md
```
</verification>

<success_criteria>
- Zero Hermes references in SPEC_CURRENT.md (no "hermes", "kanban", "t_*", "worktree")
- SPEC_CURRENT.md version 2.0 with P0 marked complete and P1 marked implemented
- docs/architecture/ has cross-ref stubs (no duplicate content)
- Kept pairs (minimax-provider, MINIMAX*.md) have reciprocal cross-references
- Guides (MINIMAX.md, MINIMAX_TTS.md, etc.) are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/06-harness-p0-finalization/06-02-SUMMARY.md`
</output>
