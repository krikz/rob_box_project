---
phase: 06-harness-p0-finalization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/vision/voice_assistant/Dockerfile
autonomous: true
requirements:
  - DOCKER-06
  - DOCKER-07

must_haves:
  truths:
    - "Docker build for voice_assistant succeeds with rob_box_harness and rob_box_llm included"
    - "docker/vision/voice_assistant/Dockerfile follows all project Docker standards"
  artifacts:
    - path: "docker/vision/voice_assistant/Dockerfile"
      provides: "Voice Assistant Dockerfile with rob_box_harness + rob_box_llm integrated"
      contains: "rob_box_harness"
      contains: "rob_box_llm"
  key_links:
    - from: "docker/vision/voice_assistant/Dockerfile (COPY src/rob_box_harness)"
      to: "src/rob_box_harness/ (source)"
      via: "COPY layer"
      pattern: "COPY src/rob_box_harness"
    - from: "docker/vision/voice_assistant/Dockerfile (colcon build)"
      to: "rob_box_harness package"
      via: "--packages-select"
      pattern: "rob_box_harness"
---

<objective>
Integrate `rob_box_harness` into the Docker build for `voice_assistant` container and verify the build succeeds.

Purpose: Per D-04/D-06, `rob_box_harness` is currently NOT installed in any Dockerfile. It must be added to `docker/vision/voice_assistant/Dockerfile` so the harness framework is available in the production voice assistant container.

Output: Updated Dockerfile with rob_box_harness + rob_box_llm layers, verified `docker build` succeeds.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-harness-p0-finalization/06-CONTEXT.md
@.planning/phases/06-harness-p0-finalization/06-RESEARCH.md
@.github/copilot-instructions.md

<interfaces>
From RESEARCH.md §Docker Integration (W6) — exact additions needed:
- COPY src/rob_box_harness/package.xml → /ws/src/rob_box_harness/
- COPY src/rob_box_harness/setup.py, setup.cfg → /ws/src/rob_box_harness/
- COPY src/rob_box_harness/rob_box_harness → /ws/src/rob_box_harness/rob_box_harness
- COPY src/rob_box_llm/package.xml → /ws/src/rob_box_llm/
- COPY src/rob_box_llm/setup.py, setup.cfg → /ws/src/rob_box_llm/
- COPY src/rob_box_llm/rob_box_llm → /ws/src/rob_box_llm/rob_box_llm
- Update colcon build: add rob_box_llm rob_box_harness to --packages-select (before rob_box_voice)
- Update rosdep install: add rob_box_llm rob_box_harness paths

Project Docker standards (from .github/copilot-instructions.md):
- ❌ NEVER COPY config/ or COPY scripts/ in Dockerfile
- ✅ network_mode: host (docker-compose, not Dockerfile)
- Existing Dockerfile structure: multi-stage layer caching (package.xml → setup.py → code)

rob_box_harness dependency: rob_box_llm>=0.2.1 (from setup.py)
rob_box_llm dependencies: httpx, openai, PyYAML (must be in voice-base image)

Dockerfile currently at: docker/vision/voice_assistant/Dockerfile (~140 lines)
Existing --packages-select: rob_box_perception_msgs rob_box_mcp_tools rob_box_voice rob_box_animations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task W6: Add rob_box_harness + rob_box_llm to docker/vision/voice_assistant/Dockerfile</name>
  <files>docker/vision/voice_assistant/Dockerfile</files>
  <read_first>
    - docker/vision/voice_assistant/Dockerfile — target file, ~140 lines, multi-stage layer caching structure
    - src/rob_box_harness/package.xml — verify <depend>rob_box_llm</depend> exists
    - src/rob_box_harness/setup.py — verify install_requires=["rob_box_llm>=0.2.1", "PyYAML>=6.0"]
    - src/rob_box_llm/package.xml — verify dependencies for rosdep
    - .github/copilot-instructions.md — Docker standards: NO COPY config/, NO COPY scripts/
  </read_first>
  <action>
Per D-04/D-06 and RESEARCH.md §Docker Integration: Add `rob_box_harness` and its dependency `rob_box_llm` to the voice_assistant Dockerfile. Follow the existing multi-stage layer caching pattern exactly.

The Dockerfile has numbered steps. Insert new layers following the SAME pattern as existing packages:

**After the existing `RUN mkdir -p` line** (which creates dirs for src/rob_box_voice src/rob_box_animations src/rob_box_mcp_tools src/rob_box_perception_msgs):
- Add `src/rob_box_harness src/rob_box_llm` to the mkdir list

**In `# Шаг 1: Копируем package.xml для rosdep` section:**
- After `COPY src/rob_box_mcp_tools/package.xml /ws/src/rob_box_mcp_tools/`
- Add:
```
COPY src/rob_box_llm/package.xml /ws/src/rob_box_llm/
COPY src/rob_box_harness/package.xml /ws/src/rob_box_harness/
```

**In `# Шаг 2: rosdep install` section:**
- Add `src/rob_box_llm src/rob_box_harness` to the `--from-paths` argument

**In `# Шаг 3: Копируем setup.py и CMakeLists.txt` section:**
- After existing setup.py copies, add:
```
COPY src/rob_box_llm/setup.py /ws/src/rob_box_llm/
COPY src/rob_box_llm/setup.cfg /ws/src/rob_box_llm/
COPY src/rob_box_harness/setup.py /ws/src/rob_box_harness/
COPY src/rob_box_harness/setup.cfg /ws/src/rob_box_harness/
```

**In `# Шаг 8: Копируем исходный код Python` section:**
- After `COPY src/rob_box_mcp_tools/rob_box_mcp_tools /ws/src/rob_box_mcp_tools/rob_box_mcp_tools`
- Add:
```
COPY src/rob_box_llm/rob_box_llm /ws/src/rob_box_llm/rob_box_llm
COPY src/rob_box_harness/rob_box_harness /ws/src/rob_box_harness/rob_box_harness
```

**In `# Шаг 10: СБОРКА ROS2 ПАКЕТОВ` section:**
- Update `--packages-select` from:
  `rob_box_perception_msgs rob_box_mcp_tools rob_box_voice rob_box_animations`
- To:
  `rob_box_perception_msgs rob_box_mcp_tools rob_box_llm rob_box_harness rob_box_voice rob_box_animations`
- IMPORTANT: rob_box_llm MUST come before rob_box_harness (colcon builds in order; harness depends on llm)

**Verify NO violation of project Docker standards:**
- Do NOT add any `COPY config/` or `COPY scripts/` lines
- The existing Dockerfile already has no such COPY lines — maintain that
- `network_mode: host` is in docker-compose.yaml, not Dockerfile — no change needed
  </action>
  <verify>
    <automated>grep -q "rob_box_harness" docker/vision/voice_assistant/Dockerfile && grep -q "rob_box_llm" docker/vision/voice_assistant/Dockerfile && grep -q "rob_box_llm.*rob_box_harness" docker/vision/voice_assistant/Dockerfile && ! grep -q "COPY.*config/\|COPY.*scripts/" docker/vision/voice_assistant/Dockerfile && echo "W6 PASS"</automated>
  </verify>
  <done>
    - Dockerfile copies rob_box_llm package.xml, setup.py, setup.cfg, source code (in correct layer order)
    - Dockerfile copies rob_box_harness package.xml, setup.py, setup.cfg, source code (in correct layer order)
    - rosdep install includes rob_box_llm and rob_box_harness paths
    - colcon build --packages-select has rob_box_llm BEFORE rob_box_harness
    - NO COPY config/ or COPY scripts/ violations
    - Existing packages (rob_box_voice, rob_box_animations, etc.) unchanged
  </done>
</task>

<task type="auto">
  <name>Task W7: Verify Docker build with harness — docker build voice_assistant</name>
  <files>docker/vision/voice_assistant/Dockerfile</files>
  <read_first>
    - docker/vision/voice_assistant/Dockerfile — updated in W6, verify current state
    - docker/vision/voice_assistant/.image-versions.latest — check base image tag
    - src/rob_box_harness/package.xml — confirm <depend>rob_box_llm</depend> exists for colcon build order
    - src/rob_box_llm/setup.py — confirm dependencies (httpx, openai, PyYAML) for voice-base check
  </read_first>
  <action>
Per D-04/D-06: Verify the Docker build succeeds with the harness integrated.

Steps:
1. Check if voice-base image is available locally:
   ```bash
   docker images | grep voice-base
   ```
   If not available, note this — the build will need to pull from ghcr.io.

2. Run the Docker build:
   ```bash
   docker build \
     --build-arg BASE_IMAGE=ghcr.io/krikz/rob_box:voice-base-humble-latest \
     -f docker/vision/voice_assistant/Dockerfile \
     -t rob_box_voice:harness-test \
     docker/ 2>&1 | tail -50
   ```
   (Build from `docker/` context since COPY paths are relative to that)

3. Wait for build to complete. Expected outcome: SUCCESS or clear error message.

4. If build FAILS:
   - Check if voice-base image has httpx, openai, PyYAML (needed by rob_box_llm)
   - Check if rob_box_llm built before rob_box_harness (colcon build order)
   - Check if package.xml <depend>rob_box_llm</depend> exists in rob_box_harness
   - Document the error and root cause in SUMMARY.md
   - Do NOT modify voice-base image — that's out of scope

5. If build SUCCEEDS:
   - Verify the built image contains rob_box_harness:
     ```bash
     docker run --rm rob_box_voice:harness-test python3 -c "import rob_box_harness; print('OK:', rob_box_harness.__version__ if hasattr(rob_box_harness, '__version__') else 'imported')"
     ```

6. Clean up test image:
   ```bash
   docker rmi rob_box_voice:harness-test 2>/dev/null || true
   ```

**Note:** The Docker build may take 5-15 minutes depending on layer cache state. This is expected. Build context is `docker/` directory per existing project convention.
  </action>
  <verify>
    <automated>docker build --build-arg BASE_IMAGE=ghcr.io/krikz/rob_box:voice-base-humble-latest -f docker/vision/voice_assistant/Dockerfile -t rob_box_voice:harness-test docker/ 2>&1 && docker run --rm rob_box_voice:harness-test python3 -c "import rob_box_harness; print('VERIFIED')" && docker rmi rob_box_voice:harness-test && echo "W7 PASS"</automated>
  </verify>
  <done>
    - `docker build` for voice_assistant Dockerfile succeeds (exit code 0)
    - Built image imports rob_box_harness without ImportError
    - If build fails, root cause documented in SUMMARY.md
    - Test image cleaned up after verification
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build context → image | Source code from workspace copied into container image |
| voice-base image → voice_assistant image | Inherits Python dependencies; must include httpx, openai, PyYAML |
</threat_model>

<verification>
```bash
# Verify Dockerfile contains both packages
grep -c "rob_box_harness" docker/vision/voice_assistant/Dockerfile
grep -c "rob_box_llm" docker/vision/voice_assistant/Dockerfile

# Verify build order in colcon
grep "packages-select" docker/vision/voice_assistant/Dockerfile | grep -o "rob_box_llm.*rob_box_harness"

# Verify no COPY config/ or COPY scripts/
! grep -E "COPY.*(config|scripts)/" docker/vision/voice_assistant/Dockerfile
```
</verification>

<success_criteria>
- Dockerfile has rob_box_llm and rob_box_harness in all required layers (mkdir, package.xml, setup.py, source code)
- colcon build order: rob_box_llm BEFORE rob_box_harness
- rosdep install includes both new packages
- No COPY config/ or COPY scripts/ violations
- docker build succeeds OR failure root cause is documented
- Built image can `import rob_box_harness`
</success_criteria>

<output>
After completion, create `.planning/phases/06-harness-p0-finalization/06-03-SUMMARY.md`
</output>
