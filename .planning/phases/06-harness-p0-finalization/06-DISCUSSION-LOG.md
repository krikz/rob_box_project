# Phase 6: Harness P0 Finalization — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-27
**Phase:** 06-harness-p0-finalization
**Areas discussed:** Документация (дубли), ADR-соответствие, Оставшиеся изменения, SPEC_CURRENT, PR #907, Два MiniMax-провайдера, P1-скоуп, Docker-интеграция, dialogue_node-миграция, Полный список волн из ADR-0001

---

## Волновая структура (финальная)

**User's choice (free text):** "мне не нравится как ты определил волны, давай волна это одна задача, загляни в АДР и ты увидишь там много задач"
**Decision:** Каждая волна = 1 атомарная задача из ADR-0001/SPEC_CURRENT. Всего 22 волны: W1–W5 (документация), W6–W7 (Docker), W8–W9 (DialogHarness), W10 (PersistentHarness), W11 (TelegramHarness), W12–W13 (порты), W14–W17 (тесты), W18–W22 (PR+аудит).

**User's choice (free text):** "так а у нас разве все уже заимплеменчено по АДР? не вижу тут имплементации интеграции вместо диалог ноды телеграм бота и другой ноды сама замена и потом правка интеграционных тестов"
**Decision:** Все задачи реализации из ADR-0001 §2.7 (DialogHarness, PersistentHarness, TelegramHarness) + порты (ROS2Transport, SQLiteVoiceMemory) + тесты (C1–C3) включены в Фазу 6 как отдельные волны.

---

## Документация: дубли architecture/ ↔ adr/

| Option | Description | Selected |
|--------|-------------|----------|
| Удалить architecture/ копии | Оставить ADR как единственный источник | |
| Оставить architecture/, удалить adr/ | ADR-файлы переместить в architecture/ | |
| Оставить оба, добавить перекрёстные ссылки | architecture/ = обзор, adr/ = решение | |

**User's choice (free text):** "так нужно проанализировать все копии и составить одну со всеми деталями из всех, в общем сделать мерж"
**Decision:** Мержить всё в единый источник правды (`adr/`). `architecture/` — ссылки.

---

## Документация: фрагменты ADR-0007a/b/c

| Option | Description | Selected |
|--------|-------------|----------|
| Удалить фрагменты a/b/c | Финальный 0007 их полностью покрывает | |
| Переместить в archive/ | Сохранить для истории | |

**User's choice (free text):** "хз чо делать наверное слить все в один документ"
**Decision:** Смержить фрагменты a/b/c в финальный `0007-minimax-tts-integration-final.md`.

---

## Куда сливать документацию

| Option | Description | Selected |
|--------|-------------|----------|
| Всё в adr/ | adr/ — canonical location | |
| adr/ для решений, architecture/ для обзоров | Разделение по назначению | |

**User's choice (free text):** "Все в один источник правды"
**Decision:** Всё в `adr/`. `architecture/` — только обзоры со ссылками.

---

## Мерж сейчас или запланировать

| Option | Description | Selected |
|--------|-------------|----------|
| Запланировать | Зафиксировать в CONTEXT.md, выполнить на фазе execute | ✓ |
| Сделать сразу | Смержить прямо сейчас | |

**User's choice:** Запланировать
**Decision:** Конкретный мерж документации — на фазе execute (волна 6.1).

---

## ADR-соответствие кода

**Result:** Проверка пройдена. Все M1–M10 реализованы. 5 портов, lifecycle, auth — всё на месте. Расхождений нет.

---

## Оставшиеся незакоммиченные изменения

| Option | Description | Selected |
|--------|-------------|----------|
| Всё уже готово | git status чистый | |
| Нужна финальная проверка | Прогнать тесты, mypy, линтеры | |
| Есть несделанное | Конкретные незакрытые вопросы | |

**User's choice (free text):** "все что было сделано гермес агентами уже влито в ту ветку, теперь мы переходим на доделывание всего через ГСД"
**Decision:** Все изменения Hermes уже в ветке. Переходим на GSD для финализации.

---

## SPEC_CURRENT.md

| Option | Description | Selected |
|--------|-------------|----------|
| Обновить P0→Done, описать P1 | Пометить P0 завершённым, описать следующие шаги | ✓ |
| Удалить SPEC_CURRENT.md | P0 завершён — SPEC больше не нужен | |
| Оставить как есть | Уже отражает реальность | |

**User's choice:** Обновить P0→Done, описать P1
**Decision:** Обновить SPEC_CURRENT.md в волне 6.1.

---

## Дополнительные серые зоны

Выбраны: PR #907, Два MiniMax-провайдера, P1-скоуп.

---

## PR #907 — стратегия мержа

| Option | Description | Selected |
|--------|-------------|----------|
| Мерж после Фазы 6 | Сначала завершить документирование | |
| Мерж сейчас | PR готов (MERGEABLE) | |
| Squash merge | Все коммиты → один squash | |

**User's choice (free text):** "я буду мержить сам после того как произойдет тестирование, но до него нам надо много чего сделать, собственно чтоб все собралось итд"
**Decision:** Пользователь мержит сам после тестирования. Фаза 6 — подготовка к мержу.

---

## Два MiniMax-провайдера

| Option | Description | Selected |
|--------|-------------|----------|
| Оставить как есть | rob_box_llm = upstream, rob_box_harness = обёртка | |
| Нужно документировать яснее | Добавить схему в README/ADR | |

**User's choice (free text):** "яхз если эти провайдеры разные один для ллм другой для ттс то оставляем конечно"
**Decision:** Провайдеры разные (LLM vs TTS + harness-обёртка). Оставить как есть.

---

## P1-скоуп для следующей фазы

| Option | Description | Selected |
|--------|-------------|----------|
| ADR-0001 §2.7 как есть | DialogHarness, PersistentHarness, TelegramHarness | |
| Разбить P1 на подфазы | P1.1, P1.2, P1.3 независимые | |

**User's choice (free text):** "это старые фазы которые разбил гермес, мы можем сделать свои"
**Decision:** P1 — новые GSD-фазы (7+), не гермесовские. Своя структура.

---

## Docker-интеграция harress

| Option | Description | Selected |
|--------|-------------|----------|
| Добавить в эту фазу | В рамках Фазы 6 | ✓ |
| Отдельная GSD-фаза | Вынести отдельно | |
| Не нужно сейчас | Harness пока не в продакшене | |

**User's choice:** Добавить в эту фазу
**Decision:** Docker-интеграция — волна 6.2.

---

## dialogue_node миграция

| Option | Description | Selected |
|--------|-------------|----------|
| P1 (следующая фаза) | Полноценная отдельная фаза | |
| В рамках Фазы 6 | Начать миграцию сейчас | ✓ |

**User's choice:** В рамках Фазы 6
**Decision:** Начало миграции — волна 6.3. Полная миграция — Фаза 7.

---

## Волновая структура

**User's choice (free text):** "ну делаем волнами по маленьким логическим изменениям чтоб не было сложно для агента и он не терял контекст"
**Decision:** Фаза 6 разбита на волны: 6.1 (документация) → 6.2 (Docker) → 6.3 (dialogue_node). Каждая волна — атомарное логическое изменение.

---

## the agent's Discretion

- Порядок волн: 6.1 → 6.2 → 6.3
- Конкретный дизайн dialogue_node-адаптеров
- Выбор Docker-образа (voice_assistant или voice_base)

## Deferred Ideas

- P1-харнесы (DialogHarness, PersistentHarness, TelegramHarness) — Фаза 7+
- ROS2Transport — Фаза 7+
- SQLiteVoiceMemory / RedisStore — Фаза 7+
- Полная миграция dialogue_node — Фаза 7
- Capability-фильтрация в fallback wrapper — P1
- ADR-0008 судьба — при доку-аудите
