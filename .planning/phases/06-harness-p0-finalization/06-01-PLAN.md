---
phase: 06-harness-p0-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rob_box_harness/rob_box_harness/providers/minimax.py
  - src/rob_box_harness/rob_box_harness/executors/ros_mcp.py
  - src/rob_box_harness/rob_box_harness/core/dialogue_state_machine.py
  - src/rob_box_harness/rob_box_harness/memory/sqlite_voice.py
  - src/rob_box_voice/rob_box_voice/dialogue_node.py
autonomous: true
requirements:
  - HARNESS-LLM-01
  - HARNESS-TOOL-02
  - HARNESS-DSM-03
  - HARNESS-MEMORY-04

must_haves:
  truths:
    - "LLMProvider supports DeepSeek and MiMo alongside existing MiniMax"
    - "ToolProvider wraps all 29 tools + 5 skills from dialogue_node"
    - "DialogueStateMachine is integrated into DialogCore (not standalone)"
    - "MemoryStore handles VoiceMemory + FAQ + EventProfile"
  artifacts:
    - path: "src/rob_box_harness/rob_box_harness/providers/deepseek.py"
      provides: "DeepSeek LLMProvider adapter (extracted from dialogue_node._build_agent)"
    - path: "src/rob_box_harness/rob_box_harness/providers/mimo.py"
      provides: "MiMo LLMProvider adapter"
    - path: "src/rob_box_harness/rob_box_harness/core/dialog_core.py"
      provides: "DialogCore: harness-native dialogue logic with DSM"
---

<objective>
Extract dialogue logic from dialogue_node.py (2181 lines) into harness ports. Create new provider adapters for DeepSeek and MiMo. Integrate DialogueStateMachine into DialogCore. Ensure all harness-ported logic is testable without ROS2.

Purpose: Per D-02 and D-03 — ports must be ready BEFORE the dialogue shell is written. This plan builds the foundation that W5 (dialogue shell) will compose.

Output: 3 new provider files, 1 new DialogCore, updated DSM integration. All existing harness tests (88) stay green. New port tests added.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-harness-p0-finalization/06-CONTEXT.md
@.planning/phases/06-harness-p0-finalization/06-RESEARCH.md
@docs/adr/0001-harness-architecture.md
@.github/copilot-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task W1: DeepSeek LLMProvider adapter</name>
  <files>src/rob_box_harness/rob_box_harness/providers/deepseek.py (NEW), src/rob_box_harness/rob_box_harness/providers/__init__.py</files>
  <read_first>
    - src/rob_box_harness/rob_box_harness/providers/minimax.py — reference implementation (ADR-0001 M1–M10)
    - src/rob_box_harness/rob_box_harness/harness.py — LLMProvider ABC
    - src/rob_box_voice/rob_box_voice/dialogue_node.py §_build_agent (lines 524–590) — DeepSeek client construction
    - src/rob_box_voice/rob_box_voice/core/llm_config.py — PROVIDERS registry, DeepSeek config
  </read_first>
  <action>
Extract DeepSeek LLM client construction from dialogue_node._build_agent() into a new `DeepSeekProvider` class.

Steps:
1. Create `src/rob_box_harness/rob_box_harness/providers/deepseek.py`:
   - Class `DeepSeekProvider` following MiniMaxProvider pattern
   - `__init__`: api_key (env: DEEPSEEK_API_KEY), base_url (default: https://api.deepseek.com), model (default: deepseek-chat)
   - `chat(messages)` → str: single-turn chat shortcut
   - `stream(messages)` → AsyncIterator[str]: streaming
   - `model_info` → ModelInfo dataclass
   - Retry with exponential backoff (copy pattern from MiniMaxProvider)
   - ENV-only auth enforcement (no hardcoded keys)
   - Redaction of upstream error bodies (copy from dialogue_node._run_agent_with_retry)

2. Register in `providers/__init__.py`

3. Add tests: `src/rob_box_harness/test/test_deepseek_provider.py`
   - Test env-auth (missing key → RuntimeError)
   - Test chat() with mock AsyncOpenAI
   - Test retry on 5xx
   - Test redaction of Authorization header in error logs

4. Export from `rob_box_harness/__init__.py`
  </action>
  <verify>
    <automated>python -c "from rob_box_harness.providers.deepseek import DeepSeekProvider; print('W1 import OK')" && pytest src/rob_box_harness/test/test_deepseek_provider.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>
    - DeepSeekProvider created following ADR-0001 M1–M10 contract
    - Tests pass: env-auth, chat(), retry, redaction
    - Registered in providers/__init__.py
  </done>
</task>

<task type="auto">
  <name>Task W2: ToolProvider — extract 29 tools + 5 skills from dialogue_node</name>
  <files>src/rob_box_harness/rob_box_harness/executors/ros_mcp.py, src/rob_box_harness/rob_box_harness/core/tool_registry.py (NEW)</files>
  <read_first>
    - src/rob_box_harness/rob_box_harness/executors/ros_mcp.py — ROSMCPToolProvider (191 lines)
    - src/rob_box_harness/rob_box_harness/tools.py — ToolProvider ABC
    - src/rob_box_voice/rob_box_voice/dialogue_node.py §_make_tools (lines 591–1056) — 29 @function_tool
    - src/rob_box_voice/rob_box_voice/dialogue_node.py §_build_skills (lines 1238–1372) — 5 skills
  </read_first>
  <action>
Extract tool definitions into a harness-native ToolRegistry. The existing ROSMCPToolProvider already wraps LLMToolCallAdapter — ensure it supports all 29 tools.

Steps:
1. Create `src/rob_box_harness/rob_box_harness/core/tool_registry.py`:
   - `ToolDescriptor` dataclass (name, description, parameters JSON schema)
   - `ToolRegistry.register(tool: ToolDescriptor)` — register a tool
   - `ToolRegistry.list_tools()` → list[ToolDescriptor]
   - Pre-register all 29 tools from dialogue_node._make_tools():
     speak_text, play_sound, play_animation, memory_context, memory_save,
     memory_search, faq_search, get_current_time, get_robot_status,
     get_battery_level, navigate_to_waypoint, navigate_to_coordinates,
     move_direction, list_waypoints, save_waypoint, delete_waypoint,
     clear_waypoints, get_current_pose, voice_settings, search_samples,
     execute_music_code, stop_music, set_vibe_preset, get_music_state,
     set_dj_mode, list_tracks, save_track, load_track, delete_track

2. Pre-register 5 skill tools (compositor mode):
   handle_music, handle_navigation, handle_memory, handle_status, handle_faq

3. Update `ROSMCPToolProvider` to use `ToolRegistry` for `list_tools()`

4. Add tests: `src/rob_box_harness/test/test_tool_registry.py`
   - Test all 29 tools registered
   - Test 5 skills registered
   - Test list_tools() returns complete list
  </action>
  <verify>
    <automated>python -c "from rob_box_harness.core.tool_registry import ToolRegistry; r = ToolRegistry(); assert len(r.list_tools()) == 34; print('W2: 34 tools registered')" && pytest src/rob_box_harness/test/test_tool_registry.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ToolRegistry with all 34 tools (29 flat + 5 skills) created
    - ROSMCPToolProvider updated to use ToolRegistry
    - Tests pass: 34 tools registered, list_tools() works
  </done>
</task>

<task type="auto">
  <name>Task W3: DialogueStateMachine — integrate into DialogCore</name>
  <files>src/rob_box_harness/rob_box_harness/core/dialogue_state_machine.py, src/rob_box_harness/rob_box_harness/core/dialog_core.py (NEW)</files>
  <read_first>
    - src/rob_box_harness/rob_box_harness/core/dialogue_state_machine.py — DSM (316 lines)
    - src/rob_box_voice/rob_box_voice/core/dialogue_manager.py — DialogueManager (IDLE/LISTENING/DIALOGUE/SILENCED)
    - src/rob_box_voice/rob_box_voice/dialogue_node.py §_on_stt (lines 1401–1432) — state transitions
    - src/rob_box_voice/rob_box_voice/dialogue_node.py §_on_timeout_check (lines 1696–1702) — LISTENING→IDLE
  </read_first>
  <action>
Integrate the existing DSM into a new DialogCore class that will be composed by the dialogue shell.

Steps:
1. Review `dialogue_state_machine.py` — ensure states match dialogue_node:
   - IDLE (waiting for wake word)
   - LISTENING (wake word detected, waiting for speech)
   - DIALOGUE (agent processing)
   - SILENCED (user said "молчи")

2. Create `src/rob_box_harness/rob_box_harness/core/dialog_core.py`:
   - `DialogCore` class (pure Python, no ROS2)
   - `__init__(self, llm: LLMProvider, tools: ToolProvider, memory: MemoryStore, dsm: DialogueStateMachine)`
   - `async process_input(text: str, conversation_history: list) → DialogResult`
     - Returns: output_text, new_state, tools_called, spoken_texts
   - `handle_wake_word(text: str) → bool` — delegate to DSM
   - `handle_silence() → None` — transition to SILENCED
   - `check_timeout() → bool` — LISTENING→IDLE after inactivity
   - History trimming (delegate to MemoryStore)

3. Update DSM to be a clean state machine:
   - `transition(new_state)` — validate transition
   - `current_state` property
   - Timeout tracking

4. Add tests: `src/rob_box_harness/test/test_dialog_core.py`
   - Test state transitions: IDLE→LISTENING→DIALOGUE→LISTENING
   - Test silence: →SILENCED→LISTENING
   - Test timeout: LISTENING→IDLE after inactivity
   - Test process_input with fake LLM/Tool/Memory
  </action>
  <verify>
    <automated>python -c "from rob_box_harness.core.dialog_core import DialogCore; print('W3 import OK')" && pytest src/rob_box_harness/test/test_dialog_core.py -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    - DialogCore created with LLMProvider, ToolProvider, MemoryStore, DSM injection
    - DSM updated with clean state transitions
    - Tests pass: all state transitions, silence, timeout
  </done>
</task>

<task type="auto">
  <name>Task W4: MemoryStore — port VoiceMemory + FAQ + EventProfile</name>
  <files>src/rob_box_harness/rob_box_harness/memory/sqlite_voice.py, src/rob_box_harness/rob_box_harness/memory.py</files>
  <read_first>
    - src/rob_box_harness/rob_box_harness/memory.py — MemoryStore ABC
    - src/rob_box_harness/rob_box_harness/memory/sqlite_voice.py — SQLiteVoiceMemory
    - src/rob_box_voice/rob_box_voice/core/voice_memory_init.py — VoiceMemory init
    - src/rob_box_voice/rob_box_voice/core/voice_memory.py — VoiceMemory
    - src/rob_box_voice/rob_box_voice/dialogue_node.py §_init_voice_memory, _init_faq_store (lines 437–520)
  </read_first>
  <action>
Ensure MemoryStore ABC and SQLiteVoiceMemory cover all dialogue_node memory use-cases.

Steps:
1. Review MemoryStore ABC methods — ensure they cover:
   - `append_turn(role, text)` — log conversation turn
   - `load_recent(limit)` → list[Turn] — recent conversation
   - `save_fact(fact, category)` — long-term memory
   - `search_facts(query, limit)` → list[Fact] — semantic search
   - `save_waypoint(name, x, y, theta)` — navigation memory (NEW)
   - `list_waypoints()` → list[Waypoint] — navigation memory (NEW)
   - `delete_waypoint(name)` — navigation memory (NEW)
   - `clear_waypoints()` — navigation memory (NEW)

2. Update SQLiteVoiceMemory to implement all methods

3. Add FAQ support:
   - `load_faq(event_id, items)` — load FAQ for event
   - `search_faq(event_id, query, limit)` → list[FAQItem]

4. Add EventProfile support:
   - `set_event_profile(profile: dict)` — set active event
   - `get_event_profile()` → dict | None

5. Add tests: update existing memory tests
   - Test waypoint CRUD
   - Test FAQ search
   - Test event profile
  </action>
  <verify>
    <automated>pytest src/rob_box_harness/test/ -k "memory" -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    - MemoryStore ABC extended with waypoint + FAQ + EventProfile methods
    - SQLiteVoiceMemory implements all methods
    - Memory tests pass (existing + new waypoint/FAQ tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV vars → LLMProvider | API keys from env only, never hardcoded |
| Upstream error bodies → logger | Redact Authorization headers before logging |
</threat_model>

<verification>
```bash
# All harness tests must stay green
pytest src/rob_box_harness/test/ -v 2>&1 | tail -5

# New port imports must work
python -c "from rob_box_harness.providers.deepseek import DeepSeekProvider; print('OK')"
python -c "from rob_box_harness.core.dialog_core import DialogCore; print('OK')"
python -c "from rob_box_harness.core.tool_registry import ToolRegistry; print('OK')"

# mypy strict on new code
mypy --strict src/rob_box_harness/rob_box_harness/providers/deepseek.py src/rob_box_harness/rob_box_harness/core/dialog_core.py src/rob_box_harness/rob_box_harness/core/tool_registry.py 2>&1
```
</verification>
