# Phase 03.1.1: Apply Research Code Improvements — Context

**Gathered:** 2026-06-12
**Source:** 03.1-RESEARCH.md §1.1 "v1→v2 Migration Impact Analysis" (P0/P1 items)

<domain>
## Phase Boundary

Применить P0 и ключевые P1 улучшения из исследования 03.1 к коду `dialogue_node.py`. Никакого нового исследования — только реализация задокументированных рекомендаций.

**In scope:**
- Добавить `APITimeoutError`, `RateLimitError`, `APIStatusError` в обработку ошибок `_run_agent_with_retry()`
- Добавить `OPENAI_LOG=info` в docker-compose конфигурацию voice_assistant
- Добавить `request_id` в логи ошибок

**Out of scope:**
- Рефакторинг dialogue_node.py (Milestone 3)
- aiohttp backend (P2)
- HTTP proxy (P2)
- Типизированные ответы Pydantic (P1, но требует переписывания provider_manager.py)
</domain>

<decisions>
## Implementation Decisions

### Все решения из 03.1-RESEARCH.md (locked)

- **D-P0-1:** Добавить `APITimeoutError` → лог с request_id, cause, fallback на Qwen
- **D-P0-2:** Добавить `RateLimitError` → лог retry-after, ждать
- **D-P0-3:** Добавить `APIStatusError` → лог status_code + response.text[:200] + request_id
- **D-P0-4:** `OPENAI_LOG=info` в docker-compose (не debug — слишком шумно для продакшена)
- **D-P1-1:** `request_id` во всех логах ошибок для support

### the agent's Discretion
- Формат лог-сообщений
- fallback-стратегия при таймауте (пока просто лог + raise, без автоматического переключения)
</decisions>

<canonical_refs>
## Canonical References

- `03.1-RESEARCH.md` §1.1 — Feature-by-Feature Impact Analysis (P0/P1 таблица)
- `src/rob_box_voice/rob_box_voice/dialogue_node.py` — целевой файл
- `docker/vision/config/voice_assistant/voice_assistant.yaml` — конфиг
- `docker/vision/docker-compose.vision.yml` — docker-compose (для OPENAI_LOG env)
</canonical_refs>
