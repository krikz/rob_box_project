---
phase: 04-github-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/context-engineering/SKILL.md
  - .github/copilot-instructions.md
  - .agents/skills/github-issues-workflow/SKILL.md
autonomous: true
requirements:
  - GH-03
  - GH-04

must_haves:
  truths:
    - "context-engineering/SKILL.md flow diagram uses 'gh issue view {N}' instead of 'tasks.json'"
    - "copilot-instructions.md backlog table row references GitHub Issues instead of tasks.json"
    - "copilot-instructions.md contains section with gh issue list/develop/view/create/close commands"
    - "github-issues-workflow/SKILL.md exists and covers full lifecycle: start issue → branch → implement → PR → close"
  artifacts:
    - path: ".agents/skills/context-engineering/SKILL.md"
      provides: "Updated skill — GitHub Issues workflow, no tasks.json references"
      contains: "gh issue"
    - path: ".github/copilot-instructions.md"
      provides: "Updated instructions — gh issue commands section, backlog row updated"
      contains: "gh issue list"
    - path: ".agents/skills/github-issues-workflow/SKILL.md"
      provides: "New lifecycle skill: полный цикл от открытия Issue до close"
      min_lines: 100
  key_links:
    - from: "context-engineering/SKILL.md"
      to: "GitHub Issues"
      via: "flow diagram + research command description"
      pattern: "gh issue view"
    - from: "copilot-instructions.md"
      to: "GitHub Issues"
      via: "backlog table row + Трекер задач section"
      pattern: "gh issue"
    - from: "github-issues-workflow/SKILL.md"
      to: "GitHub Issues lifecycle"
      via: "Жизненный цикл Issue table + gh issue develop command"
      pattern: "gh issue develop"
---

<objective>
Переписываем «голову» системы — скиллы и copilot-instructions — так, чтобы агент с первого шага знал: трекер задач это GitHub Issues, а не tasks.json.

Purpose: GH-0 (locked decision) требует обновить знания агента ДО миграции данных. Агент должен с первого же действия в 04-03 использовать `gh issue`, а не искать tasks.json.
Output: Три файла: context-engineering/SKILL.md (rewritten), copilot-instructions.md (updated), github-issues-workflow/SKILL.md (new, ~150 строк).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-github-integration/04-CONTEXT.md
@.planning/phases/04-github-integration/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrative rewrite context-engineering/SKILL.md — replace tasks.json with GitHub Issues</name>
  <files>.agents/skills/context-engineering/SKILL.md</files>
  <read_first>
    - .agents/skills/context-engineering/SKILL.md — ПОЛНОСТЬЮ; пройтись по всем строкам, найти каждое упоминание tasks.json (ожидается: frontmatter description, flow diagram строка ~21, таблица "Когда применять", секция Фаза 1 Research)
    - .planning/phases/04-github-integration/04-CONTEXT.md — решение D-01 (deep update / narrative rewrite scope) и GH-9 (github-issues-workflow skill content)
    - .planning/phases/04-github-integration/04-RESEARCH.md — секция "User Constraints / Locked Decisions" → D-01 и GH-8 для точного target state
  </read_first>
  <action>
    Сделать narrative rewrite файла .agents/skills/context-engineering/SKILL.md. Это хирургическое изменение — структура документа, заголовки, таблицы и весь контент кроме упоминаний tasks.json остаются нетронутыми.

    Конкретные места замены (найти через `grep -n "tasks\.json" .agents/skills/context-engineering/SKILL.md` перед правкой):

    1. YAML frontmatter description (строки 3-7): найти фрагмент "над любой задачей из tasks.json" → заменить на "над любой задачей из GitHub Issues. Прочитай задачу через `gh issue view {N} --repo krikz/rob_box_project` перед стартом."

    2. Блок-схема workflow (строка содержащая `tasks.json → Research → Design → Plan → Implement`): заменить `tasks.json` на `gh issue view {N}`. Результат: `gh issue view {N} → Research → Design → Plan → Implement`.

    3. Комментарий рядом с блок-схемой — строка `(задача)`: обновить в скобках на `(открыть: gh issue view {N})`.

    4. В секции "## Фаза 1: Research", подстроку "**Команда:** `/research-codebase TASK-ID`": изменить `TASK-ID` на `{N}` (номер GitHub Issue). Добавить примечание следующей строкой (после команды): "Перед запуском прочитать задачу: `gh issue view {N} --repo krikz/rob_box_project`"

    5. Любые оставшиеся упоминания `tasks.json` в теле документа — заменить контекстно-правильным эквивалентом. Проверить: `grep -n "tasks\.json" .agents/skills/context-engineering/SKILL.md` должно вернуть 0 matches.

    NOT TO CHANGE: структура документа, таблицы, заголовки, описания фаз (Research/Design/Plan/Implement), команды /design-feature, /plan-feature, /implement-feature, секцию "Разница: фича vs баг".
  </action>
  <verify>
    <automated>grep -c "tasks\.json" .agents/skills/context-engineering/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "tasks\.json" .agents/skills/context-engineering/SKILL.md` → возвращает `0`
    - SOURCE: `grep -c "gh issue" .agents/skills/context-engineering/SKILL.md` → возвращает ≥ 3 (flow diagram + research command + at least one more)
    - SOURCE: YAML frontmatter description строка содержит подстроку "GitHub Issues"
    - SOURCE: строка с flow diagram содержит `gh issue view {N}`
    - BEHAVIOR: файл остаётся валидным Markdown — структура заголовков ## не нарушена, таблицы не сломаны
  </acceptance_criteria>
  <done>context-engineering/SKILL.md: 0 mentions of tasks.json; ≥3 mentions of gh issue; YAML description updated; flow diagram updated</done>
</task>

<task type="auto">
  <name>Task 2: Update copilot-instructions.md — backlog table row + gh issue commands section</name>
  <files>.github/copilot-instructions.md</files>
  <read_first>
    - .github/copilot-instructions.md — строки 10-25 (таблица "Где искать информацию", строка с tasks.json) + секция "⚡ Быстрые команды" (для определения точного места вставки нового раздела)
    - .planning/phases/04-github-integration/04-CONTEXT.md — решение GH-8 (scope: только бэклог table row + новая секция трекера, не переписывать весь файл) и GH-3 (команда просмотра: `gh issue list --label "source:gsd"`)
  </read_first>
  <action>
    Внести ровно два изменения в .github/copilot-instructions.md:

    ИЗМЕНЕНИЕ 1 — строка 16, в таблице "Где искать информацию":
    Найти строку: `| **Бэклог задач** | \`tasks.json\` | Задачи, acceptance criteria, зависимости, agent_instructions |`
    Заменить на: `| **Бэклог задач** | GitHub Issues | \`gh issue list/create/view/develop\`; labels \`source:gsd\`; milestones M1/M2/M3 |`

    ИЗМЕНЕНИЕ 2 — добавить новый раздел "### 🗂️ Трекер задач (GitHub Issues)" в секцию "## ⚡ Быстрые команды".
    Вставить его ПОСЛЕ существующего блока "### Диагностика" и ПЕРЕД "## 🚨 Критичные правила".
    Содержимое нового раздела (точный текст):

    ```
    ### 🗂️ Трекер задач (GitHub Issues)
    ```bash
    # Просмотр бэклога
    gh issue list --label "source:gsd" --repo krikz/rob_box_project

    # Начать работу (создаёт ветку и переключается)
    gh issue develop {N} --checkout --repo krikz/rob_box_project

    # Прочитать задачу
    gh issue view {N} --repo krikz/rob_box_project

    # Создать задачу
    gh issue create --title "[ID] description" --label "type:functional,priority:high,ai-generated,source:gsd" --repo krikz/rob_box_project

    # Закрыть задачу после выполнения
    gh issue close {N} --repo krikz/rob_box_project
    ```
    ```

    НЕ изменять никакие другие секции — только строку 16 и вставка одного нового раздела.
  </action>
  <verify>
    <automated>grep -c "gh issue" .github/copilot-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c "tasks\.json" .github/copilot-instructions.md` → возвращает `0`
    - SOURCE: `grep -c "gh issue" .github/copilot-instructions.md` → возвращает ≥ 5 (list, develop, view, create, close)
    - SOURCE: `grep -q "source:gsd" .github/copilot-instructions.md` → exit code 0
    - SOURCE: `grep -q "GitHub Issues" .github/copilot-instructions.md` → exit code 0
    - SOURCE: `grep -q "Трекер задач" .github/copilot-instructions.md` → exit code 0
    - BEHAVIOR: все прочие секции файла остаются без изменений — проверить diff size (только ~6-8 строк добавлено/изменено)
  </acceptance_criteria>
  <done>copilot-instructions.md: tasks.json строка заменена на GitHub Issues; новый раздел трекера с 5 gh-командами добавлен; файл остальное не тронуто</done>
</task>

<task type="auto">
  <name>Task 3: Create .agents/skills/github-issues-workflow/SKILL.md — полный lifecycle skill (~150 строк)</name>
  <files>.agents/skills/github-issues-workflow/SKILL.md</files>
  <read_first>
    - .agents/skills/github-actions-runner/SKILL.md — паттерн структуры: YAML frontmatter format, использование таблиц, команд, раздел-по-разделу стиль
    - .planning/phases/04-github-integration/04-CONTEXT.md — GH-1 (issue body template — дословно), GH-6 (branch pattern gh issue develop), GH-9 (skill content spec)
    - .planning/phases/04-github-integration/04-RESEARCH.md — секция "gh CLI: Ключевые команды" — точные команды с флагами и --jq фильтрами
  </read_first>
  <action>
    Создать новый файл .agents/skills/github-issues-workflow/SKILL.md. Структура документа строго по следующему outline (не отклоняться):

    YAML frontmatter:
    - name: github-issues-workflow
    - description: многострочное описание — "Полный цикл работы с задачами через GitHub Issues. Используй при начале работы над задачей, создании веток, оформлении PR и закрытии Issue. GitHub Issues = единственный источник правды; tasks.json не существует."

    Секции в строгом порядке:

    ## Репозиторий
    Одна строка: `krikz/rob_box_project` (флаг `--repo krikz/rob_box_project` во всех командах).

    ## Концепция
    Три коротких параграфа: (1) GitHub Issues = единственный источник правды для задач/багов/tech-debt. (2) tasks.json не существует — не создавать, не читать. (3) Каждая задача = Issue с labels `type:*`, `priority:*`, `milestone:M1/M2/M3`, `source:gsd`, `ai-generated`.

    ## Жизненный цикл Issue
    Таблица 4 строки: Статус | Label | Описание | Следующий шаг
    - Open | — | Задача открыта | `gh issue develop {N} --checkout`
    - In Progress | `status:in-progress` | Работа идёт | Commit + Push → PR
    - Blocked | `status:blocked` | Заблокирована | gh issue comment с причиной
    - Done | (closed) | Завершена | `gh issue close {N}`

    ## Начало работы
    Нумерованный список из 4 шагов:
    1. Просмотр бэклога: `gh issue list --label "source:gsd" --repo krikz/rob_box_project`
    2. Прочитать задачу: `gh issue view {N} --repo krikz/rob_box_project`
    3. Создать ветку: `gh issue develop {N} --checkout --repo krikz/rob_box_project` (создаёт ветку `{N}-{slug-from-title}`)
    4. Пометить как in-progress: `gh issue edit {N} --add-label "status:in-progress" --repo krikz/rob_box_project`

    ## Формат тела Issue (при создании)
    Вставить ДОСЛОВНО template из GH-1 (04-CONTEXT.md):
    ```
    ## Description
    {description}

    ## Acceptance Criteria
    {acceptance_criteria as checklist}

    ## File References
    {file/line if present}

    ## Source
    Migrated from: {source} (tasks.json / TECH_DEBT.md)
    Original ID: {TASK-ID or TD-ID}

    ---
    > ⚠️ _This issue was created by AI (GSD workflow). Label: `ai-generated`_
    ```

    ## Завершение работы
    1. Убедиться что тесты проходят (colcon build + pytest)
    2. Создать PR: `gh pr create --title "feat: [description]" --body "Closes #{N}" --repo krikz/rob_box_project`
    3. После merge: `gh issue close {N} --repo krikz/rob_box_project`
    4. Удалить ветку: `git branch -d {N}-{slug}`

    ## Быстрые команды (cheatsheet)
    Таблица: Действие | Команда
    - Список бэклога: `gh issue list --label "source:gsd" --repo krikz/rob_box_project`
    - Список по приоритету: `gh issue list --label "priority:high" --repo krikz/rob_box_project`
    - Начать работу: `gh issue develop {N} --checkout --repo krikz/rob_box_project`
    - Просмотр задачи: `gh issue view {N} --repo krikz/rob_box_project`
    - Добавить label: `gh issue edit {N} --add-label "status:in-progress" --repo krikz/rob_box_project`
    - Создать задачу: `gh issue create --title "[ID] title" --label "type:functional,priority:high,ai-generated,source:gsd" --repo krikz/rob_box_project`
    - Закрыть задачу: `gh issue close {N} --repo krikz/rob_box_project`
    - Создать PR: `gh pr create --title "feat: title" --body "Closes #{N}" --repo krikz/rob_box_project`

    ## Label taxonomy
    Мини-таблица 3 колонки: Категория | Labels | Пример
    - Type: `type:tech-debt`, `type:stub`, `type:security`, `type:performance`, `type:functional`, `type:infrastructure`, `type:testing`, `bug`, `documentation`
    - Priority: `critical`, `priority:high`, `priority:medium`, `priority:low`
    - Status: `status:in-progress`, `status:blocked`
    - Milestone: `milestone:M1`, `milestone:M2`, `milestone:M3`
    - Origin: `ai-generated`, `source:gsd`

    ## Связь с context-engineering skill
    Одна строка: "Этот скилл дополняет `context-engineering/SKILL.md`. Research phase начинается с `gh issue view {N}`, не с tasks.json."

    Итоговый размер: ~150 строк. Не раздувать content.
  </action>
  <verify>
    <automated>wc -l .agents/skills/github-issues-workflow/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - FILE EXISTS: `test -f .agents/skills/github-issues-workflow/SKILL.md` → exit code 0
    - SIZE: `wc -l .agents/skills/github-issues-workflow/SKILL.md` → число между 100 и 200
    - SOURCE: `grep -q "name: github-issues-workflow" .agents/skills/github-issues-workflow/SKILL.md` → exit code 0
    - SOURCE: `grep -c "gh issue" .agents/skills/github-issues-workflow/SKILL.md` → ≥ 6 (list, view, develop, edit, close, create)
    - SOURCE: `grep -q "Жизненный цикл Issue" .agents/skills/github-issues-workflow/SKILL.md` → exit code 0
    - SOURCE: `grep -q "## Description" .agents/skills/github-issues-workflow/SKILL.md` → exit code 0 (issue body template)
    - SOURCE: `grep -q "gh issue develop" .agents/skills/github-issues-workflow/SKILL.md` → exit code 0
    - SOURCE: `grep -q "tasks\.json" .agents/skills/github-issues-workflow/SKILL.md` → exit code 1 (не содержит)
  </acceptance_criteria>
  <done>github-issues-workflow/SKILL.md создан, 100-200 строк; YAML frontmatter с name; full lifecycle: start→branch→implement→PR→close; GH-1 issue body template; label taxonomy; cheatsheet</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Файловая система → git репозиторий | Изменения в .agents/skills/ и .github/ коммитятся в репо |
| copilot-instructions.md → GitHub Copilot | Файл загружается в контекст ИИ автоматически |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | copilot-instructions.md | accept | Файл публичный; содержит только команды и ссылки на документы — не секреты. Упоминание label names не создаёт риска. |
| T-04-01-02 | Tampering | github-issues-workflow/SKILL.md | accept | Файл в git, все изменения отслеживаются через git history. Права доступа к репо контролируются GitHub. |
| T-04-01-03 | Elevation of Privilege | copilot-instructions.md → AI instructions | mitigate | Не добавлять в инструкции команды с `--admin` или bypass флагами. Инструкции ограничиваются чтением/созданием Issues. |
</threat_model>

<verification>
После завершения всех трёх задач запустить:

```bash
# GH-03: copilot-instructions содержит gh issue
grep -c "gh issue" .github/copilot-instructions.md     # ≥ 5
grep -c "tasks\.json" .github/copilot-instructions.md  # 0

# GH-04: github-issues-workflow skill создан
test -f .agents/skills/github-issues-workflow/SKILL.md && echo "PASS"  # PASS
wc -l .agents/skills/github-issues-workflow/SKILL.md                   # 100-200

# context-engineering обновлён
grep -c "tasks\.json" .agents/skills/context-engineering/SKILL.md      # 0
grep -c "gh issue" .agents/skills/context-engineering/SKILL.md         # ≥ 3
```
</verification>

<success_criteria>
- GH-03: copilot-instructions.md содержит `gh issue` команды ≥5 раз; tasks.json упоминаний = 0
- GH-04: .agents/skills/github-issues-workflow/SKILL.md существует, 100-200 строк, lifecycle documented
- context-engineering/SKILL.md: tasks.json = 0 mentions; gh issue = ≥3 mentions
</success_criteria>

<output>
После завершения создать .planning/phases/04-github-integration/04-01-SUMMARY.md
</output>
