---
phase: "03.2-music-quality-testing"
plan: "06"
type: execute
wave: 4
depends_on: ["03.2-05"]
files_modified:
  - src/rob_box_voice/prompts/skills/music_skill_prompt.txt
  - src/rob_box_voice/prompts/compositor_prompt.txt
  - src/rob_box_voice/rob_box_voice/dialogue_node.py
  - .planning/phases/03.2-music-quality-testing/03.2-ITERATION-1.md
autonomous: false
requirements: [MUS-01, MUS-02, MUS-03, MUS-04]
must_haves:
  truths:
    - "Top 3 UAT issues identified and fixed in prompts/code"
    - "Changes deployed to robot via GitHub Actions"
    - "Re-test completed with improved scores"
    - "Iteration 1 results documented"
  artifacts:
    - path: ".planning/phases/03.2-music-quality-testing/03.2-ITERATION-1.md"
      provides: "Iteration 1 results with before/after comparison"
      contains: "iteration"
  key_links:
    - from: "UAT scores"
      to: "prompt/code fixes"
      via: "issue identification → targeted fix"
      pattern: "fix.*issue"
---

<objective>
Iterate on prompt/code fixes based on UAT feedback. Fix top 3 scoring issues, re-deploy,
re-test, and document results. Max 2 iterations allowed (this is iteration 1).

Purpose: If UAT weighted average < 70%, the top-scoring issues need targeted fixes.
Iteration is prompt-first (fast), code-only for critical bugs.

Output: Updated prompts/code with iteration fixes, re-test results, iteration document.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-music-quality-testing/03.2-CONTEXT.md
@.planning/phases/03.2-music-quality-testing/03.2-UAT.md
@.planning/phases/03.2-music-quality-testing/03.2-TEST-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze UAT scores and fix top 3 issues</name>
  <files>src/rob_box_voice/prompts/skills/music_skill_prompt.txt, src/rob_box_voice/prompts/compositor_prompt.txt, src/rob_box_voice/rob_box_voice/dialogue_node.py</files>
  <action>
    1. **Read UAT scores** from `03.2-UAT.md` — identify the 3 lowest-scoring criteria.

    2. **For each low-scoring criterion**, apply targeted fixes:

       - **If Musical Quality < 3/5**: Add more specific examples to music_skill_prompt.txt
         (e.g., add a new EXAMPLE G/H with different genre). Or strengthen the RANDOMIZATION
         MANDATE.

       - **If Constraint Compliance < 3/5**: Tighten hardware constraints in prompt.
         Add more explicit amp limits, ban specific patterns that caused issues.

       - **If DJ Routing < 3/5**: Strengthen set_dj_mode instructions. Add more explicit
         call sequence examples.

       - **If MC Quality < 3/5**: Reduce MC phrase limits further (1 phrase max instead of 2).
         Add more "done" terminator examples.

       - **If search_samples < 3/5**: Make search_samples even more prominent. Add a
         "BEFORE EVERY spack=1 USE" check at the very top of the prompt.

       - **If Tool Call Correctness < 3/5**: Add more specific parameter examples.
         Fix any hallucinated parameter names in the prompt.

    3. **Prompt-first approach**: All fixes go to prompts first. Only modify dialogue_node.py
       if a code-level safety net is needed (e.g., filtering bad tool calls).

    4. **Commit and push** to feature branch.

    Per D-07 (CONTEXT): User decides after each iteration. Max 2 hard limit.
  </action>
  <verify>
    <automated>git diff --stat HEAD~1 src/rob_box_voice/prompts/ src/rob_box_voice/rob_box_voice/dialogue_node.py</automated>
    Expected: Changes in prompt files (and optionally dialogue_node.py)
  </verify>
  <done>
    - Top 3 UAT issues identified
    - Targeted fixes applied to prompts (and optionally code)
    - Changes committed and pushed to feature branch
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-deploy and re-test (iteration 1)</name>
  <files>.planning/phases/03.2-music-quality-testing/03.2-ITERATION-1.md</files>
  <what-built>
    Iteration 1 fixes applied. Need to re-deploy and re-test the top-scoring scenario.
  </what-built>
  <how-to-verify>
    1. **Trigger build**:
       ```bash
       gh workflow run "L-Build Single Service.yml" \
         --field service=voice-assistant \
         --field branch=feature/phase-3.2-music-testing \
         --repo krikz/rob_box_project
       ```

    2. **Wait for deployment** (monitor with `gh run list`)

    3. **Re-test the worst scenario**: Run the test that scored lowest in UAT.
       For example, if DJ set scored worst → "ты диджей, играй сет из 3 треков"

    4. **Score the re-test** using the same UAT criteria.

    5. **Create 03.2-ITERATION-1.md** with:
       - Issues fixed (from UAT analysis)
       - Changes made (prompt/code)
       - Re-test scores (before/after comparison)
       - Decision: pass (≥70%) or iterate again (max 2 iterations)
  </how-to-verify>
  <resume-signal>Type "approved" if re-test passed ≥70%, or "iterate" for iteration 2, or "stop" to accept current state</resume-signal>
</task>

</tasks>

<verification>
1. UAT issues analyzed and top 3 identified
2. Targeted fixes applied to prompts/code
3. Changes deployed via GitHub Actions
4. Re-test completed with scores
5. Iteration 1 document created
</verification>

<success_criteria>
- Top 3 issues fixed based on UAT feedback
- Re-test shows improvement over baseline
- Iteration 1 results documented with before/after scores
- Decision made: pass → final report, iterate → iteration 2, or stop → accept
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-music-quality-testing/03.2-06-SUMMARY.md`
</output>
