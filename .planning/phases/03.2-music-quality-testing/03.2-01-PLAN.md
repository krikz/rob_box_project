---
phase: "03.2-music-quality-testing"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rob_box_voice/prompts/skills/music_skill_prompt.txt
  - local_test/RENARDO_REFERENCE.md
autonomous: true
requirements: [MUS-01, MUS-04, MUS-05]
must_haves:
  truths:
    - "music_skill_prompt.txt no longer contains Star Wars bias (~80 lines removed)"
    - "search_samples instruction is prominent and non-ambiguous for spack=1 usage"
    - "set_dj_mode reminder appears at the TOP of DJ mode instructions"
    - "Prompt references TrackLibrary list_tracks/load_track for known melodies instead of inline code"
  artifacts:
    - path: "src/rob_box_voice/prompts/skills/music_skill_prompt.txt"
      provides: "Clean music prompt without Star Wars bias, with strong spack=1/search_samples rules"
      contains: "list_tracks"
    - path: "local_test/RENARDO_REFERENCE.md"
      provides: "Sample packs section with spack=0/spack=1 letters"
      contains: "spack=1"
  key_links:
    - from: "music_skill_prompt.txt"
      to: "TrackLibrary (list_tracks / load_track)"
      via: "instruction to use list_tracks for known melodies"
      pattern: "list_tracks|load_track"
---

<objective>
Fix 4 prompt issues in music_skill_prompt.txt found during MiMo DJ log analysis (2026-06-13).

Purpose: The current prompt biases MiMo toward Star Wars Imperial March (~80 lines of inline code),
causes it to skip search_samples for spack=1, never calls set_dj_mode, and produces identical
transitions. These are the root causes of baseline scores: search_samples=1/5, set_dj_mode=1/5,
Constraint Compliance=2/5.

Output: Updated music_skill_prompt.txt with: (1) Star Wars code moved to TrackLibrary reference,
(2) search_samples made mandatory for spack=1, (3) set_dj_mode reminder at top, (4) diverse
style examples replacing copy-paste prone examples.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-music-quality-testing/03.2-CONTEXT.md

@src/rob_box_voice/prompts/skills/music_skill_prompt.txt
@local_test/RENARDO_REFERENCE.md

NOTE: Plan 02 creates docs/research/MIMO_API_RESEARCH.md with MiMo API parameter reference.
If executing in parallel, read MiMo API docs directly: https://mimo.mi.com/docs/en-US/api/chat/openai-api
Use findings to inform prompt parameter recommendations (frequency_penalty, temperature, etc.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Star Wars bias and restructure melody references in music_skill_prompt.txt</name>
  <files>src/rob_box_voice/prompts/skills/music_skill_prompt.txt</files>
  <action>
    In music_skill_prompt.txt, make these changes:

    1. **Remove the inline Imperial March code block** (~lines 700-780 area): Delete the entire
       "KNOWN MELODY LIBRARY" section that contains hardcoded Imperial March reference fragments,
       canonical arrangement, folk-style remix, and extended variant. Replace with a SHORT
       instruction (5-8 lines max):
       ```
       🎵 KNOWN MELODY LIBRARY:
       For specific songs (Star Wars, Happy Birthday, Katyusha, Nokia Tune, etc.):
       1. Call list_tracks(tag="melody") to find if a preset exists
       2. If found → load_track(name) to get the code, then execute_music_code
       3. If NOT found → play an improvisation in the appropriate key/style
       ❌ NEVER write 50+ lines of inline midinote code — use TrackLibrary!
       ✅ Star Wars / Imperial March = one preset among many, NOT the default style!
       ```

    2. **Keep the SHORT melody examples** (Happy Birthday, Jingle Bells, Smoke on the Water,
       Für Elise, Katyusha, Nokia Tune, Kalinka, Stranger Things) — these are fine as quick
       reference. But add a header: "⚡ QUICK REFERENCE ONLY — prefer list_tracks() for full
       arrangements".

    3. **In the DJ TRANSITION STRUCTURE section**, add after the mandatory structure template:
       ```
       🎲 RANDOMIZATION MANDATE — each transition MUST differ in at least 3 of:
       - BPM (pick from 85-145 range, never same as last)
       - Root (rotate: C/D/E/F/G/A/B)
       - Scale (rotate: minor/dorian/mixolydian/lydian/phrygian)
       - Bass synth (rotate: wobblebass/dub/fuzz/tb303/moogbass/karp)
       - Lead synth (rotate: varsaw/blip/pianovel/sitar/marimba/cs80lead)
       - Drum pattern (rotate from DRUM PATTERN LIBRARY)
       ❌ If BPM=125 + F dorian + wobblebass + blip appeared in LAST transition → pick DIFFERENT!
       ```

    Per D-01 (CONTEXT): All melodies move out of prompt into TrackLibrary. Star Wars becomes
    one preset among many.
  </action>
  <verify>
    <automated>grep -c "imperialbrass\|Star Wars.*bias\|KNOWN MELODY LIBRARY" src/rob_box_voice/prompts/skills/music_skill_prompt.txt</automated>
    Expected: The old "KNOWN MELODY LIBRARY" heading with 80+ lines of Imperial March code is gone.
    The word "imperialbrass" may appear only in SHORT reference examples (Happy Birthday etc. section), not as a standalone 80-line block.
    grep -c "list_tracks" music_skill_prompt.txt → ≥ 2 (instruction to use TrackLibrary)
  </verify>
  <done>
    music_skill_prompt.txt no longer contains a large inline Imperial March code block.
    Instead it references list_tracks() for known melodies. Star Wars is mentioned as one
    preset among many, not as the dominant example.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strengthen search_samples, set_dj_mode, and spack=1 rules in music_skill_prompt.txt</name>
  <files>src/rob_box_voice/prompts/skills/music_skill_prompt.txt</files>
  <action>
    In music_skill_prompt.txt, make these changes:

    1. **set_dj_mode MANDATORY REMINDER** — move the existing reminder block to appear
       IMMEDIATELY after "DJ MODE — RULE #1" (at the very top of DJ instructions, before
       anything else). Ensure it says:
       ```
       🔴 set_dj_mode MANDATORY — AFTER EVERY execute_music_code in DJ mode!
       ✅ Pattern: execute_music_code(...) → set_dj_mode(enabled=True, theme="...")
       ❌ LOG BUG: LLM played 3 tracks but NEVER called set_dj_mode → DJ mode was OFF!
       ```

    2. **search_samples MANDATORY for spack=1** — the existing "SPACK=1 RULE" block is good
       but needs to be EARLIER in the prompt (move it near the top, right after "SAMPLE
       SELECTION STRATEGY"). Add:
       ```
       🔴 BEFORE any spack=1 usage (except letter 'c'):
       1. Call search_samples("*", pack="1_pitchglitch_samples")
       2. Use ONLY letters returned by search_samples
       3. Never guess A, i, y, B, Q — they may not exist!
       ```

    3. **Add frequency_penalty / presence_penalty instruction** at the very end of the prompt
       (before the final "Your music tools:" line):
       ```
       🎲 DIVERSITY API SETTINGS (set by code, not by prompt):
       The system sets frequency_penalty=0.3 and presence_penalty=0.2 automatically.
       This encourages variety — lean into it by picking DIFFERENT instruments each time!
       ```

    Per D-02 (CONTEXT): frequency_penalty=0.3 / presence_penalty=0.2 added to MiMo API calls.
    Per D-04 (CONTEXT): spack=1 letters always verified via search_samples.
  </action>
  <verify>
    <automated>grep -c "set_dj_mode MANDATORY\|search_samples.*MANDATORY\|frequency_penalty" src/rob_box_voice/prompts/skills/music_skill_prompt.txt</automated>
    Expected: ≥ 3 (set_dj_mode reminder, search_samples mandatory, frequency_penalty mention)
  </verify>
  <done>
    - set_dj_mode reminder appears at the TOP of DJ instructions
    - search_samples mandatory rule for spack=1 is prominent near prompt top
    - frequency_penalty/presence_penalty context added
    - MiMo should now call set_dj_mode after first execute_music_code in DJ mode
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "imperialbrass" src/rob_box_voice/prompts/skills/music_skill_prompt.txt` — count should be ≤ 5 (only in short references, not 80-line blocks)
2. `grep -c "list_tracks" src/rob_box_voice/prompts/skills/music_skill_prompt.txt` — ≥ 2
3. `grep -c "set_dj_mode" src/rob_box_voice/prompts/skills/music_skill_prompt.txt` — count should increase vs baseline
4. `wc -l src/rob_box_voice/prompts/skills/music_skill_prompt.txt` — should be ~800-850 lines (down from ~900, Star Wars block removed)
5. Diff review: all changes are prompt-only, no code changes
</verification>

<success_criteria>
- Star Wars bias removed from prompt (inline code block replaced with list_tracks reference)
- set_dj_mode mandatory reminder at top of DJ section
- search_samples mandatory for spack=1, prominently placed
- RANDOMIZATION MANDATE added to DJ transition structure
- Prompt still fits in context window (~800-850 lines)
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-music-quality-testing/03.2-01-SUMMARY.md`
</output>
