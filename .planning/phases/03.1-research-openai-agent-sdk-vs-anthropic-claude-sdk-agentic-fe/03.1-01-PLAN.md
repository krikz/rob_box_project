---
phase: 03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/vision/voice_assistant/requirements.txt
  - docker/vision/voice_base/requirements.txt
  - .planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-RESEARCH.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "All 5 requirements.txt files have pinned openai: 2 Docker voice files with >=2.37.0,<3.0.0; 3 other files with >=2.37.0"
    - "Both Docker voice requirements.txt files have openai-agents>=0.17.0,<1.0.0 pinned"
    - "RESEARCH.md has been validated: all D-01 through D-12 decisions are covered, Open Questions marked (RESOLVED)"
    - "ROADMAP.md Phase 03.1 entry reflects the completed research with actual goal and decision outcome"
    - "No unpinned openai or openai-agents references remain anywhere in requirements.txt files"
  artifacts:
    - path: "docker/vision/voice_assistant/requirements.txt"
      provides: "Pinned openai and openai-agents for voice_assistant Docker image"
      contains: "openai>=2.37.0,<3.0.0"
    - path: "docker/vision/voice_base/requirements.txt"
      provides: "Pinned openai and openai-agents for voice_base Docker image"
      contains: "openai-agents>=0.17.0,<1.0.0"
    - path: ".planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-RESEARCH.md"
      provides: "Validated research document with all D-XX decisions covered"
      min_lines: 500
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 03.1 entry with research outcome and decision"
      contains: "OpenAI Agents SDK"
  key_links:
    - from: "docker/vision/voice_assistant/requirements.txt"
      to: "Docker build voice_assistant image"
      via: "pip install -r requirements.txt during docker build"
      pattern: "openai>=2\\.37\\.0"
    - from: "docker/vision/voice_base/requirements.txt"
      to: "Docker build voice_base image"
      via: "pip install -r requirements.txt during docker build"
      pattern: "openai-agents>=0\\.17\\.0"
    - from: "03.1-RESEARCH.md recommendation"
      to: "ROADMAP.md Phase 03.1"
      via: "Decision recorded in ROADMAP for future phases"
      pattern: "Stay with OpenAI Agents SDK"
---

<objective>
Act on the completed Phase 03.1 research findings: validate the RESEARCH.md against all D-XX decisions, pin openai and openai-agents versions in all requirements.txt files, and document the final decision (stay with OpenAI Agents SDK, skip Anthropic) in the project roadmap.

Purpose: The research is complete — the 900-line RESEARCH.md proves OpenAI Agents SDK is the correct choice and Anthropic adds zero value for DeepSeek-backed Rob Box. But the research findings aren't actionable until (1) versions are pinned so Docker builds are reproducible, and (2) the decision is recorded in ROADMAP.md so future phases don't revisit this question.

Output: Pinned requirements files, validated RESEARCH.md, updated ROADMAP.md with decision recorded, and a gap report if any D-XX decisions lack coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-CONTEXT.md
@.planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-RESEARCH.md
@docker/vision/voice_assistant/requirements.txt
@docker/vision/voice_base/requirements.txt
@src/rob_box_perception/requirements.txt
@src/rob_box_voice/requirements.txt
@docker/main/perception/requirements.txt

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From 03.1-RESEARCH.md Section 7 — Recommendations:
- Primary recommendation: Stay with OpenAI Agents SDK (confidence: HIGH)
- Immediate action: Pin `openai>=2.37.0,<3.0.0` and `openai-agents>=0.17.0,<1.0.0`
- What NOT to do: Don't install Anthropic SDK, don't attempt Managed Agents, don't migrate to Anthropic API format

From 03.1-CONTEXT.md — Locked Decisions (D-01 through D-12):
- D-01: Full stack comparison (SDK → agents → tools/MCP → sandbox → voice)
- D-02: OpenAI: openai SDK v2.37.0→v2.41.1 + openai-agents v0.17.x
- D-03: Anthropic: anthropic SDK v0.109.x + Managed Agents + Claude Code
- D-04: Multi-provider support (CRITICAL) — DeepSeek compatibility
- D-05: Voice + Realtime pipeline applicability
- D-06: Sandbox + Workspace feasibility
- D-07: Agent loop architecture comparison
- D-08: Overall comparison with Rob Box recommendation
- D-09: Full RESEARCH.md output
- D-10: Deep research with DeepSeek verification
- D-11: Feature × DeepSeek compatibility matrix
- D-12: Agent loop architecture focus

Current requirements.txt state (verified 2026-06-12):
- docker/vision/voice_assistant/requirements.txt: `openai` (UNPINNED), `openai-agents` (UNPINNED) — LINES 62-63
- docker/vision/voice_base/requirements.txt: `openai` (UNPINNED), `openai-agents` (UNPINNED) — LINES 63-64
- src/rob_box_perception/requirements.txt: `openai>=2.37.0` — ALREADY PINNED
- src/rob_box_voice/requirements.txt: `openai>=2.37.0` — ALREADY PINNED
- docker/main/perception/requirements.txt: `openai>=2.37.0` — ALREADY PINNED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate RESEARCH.md against all D-XX decisions</name>
  <files>.planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-RESEARCH.md</files>
  <action>
Cross-reference each CONTEXT.md decision (D-01 through D-12) against RESEARCH.md sections. For each decision, verify:
- Is there a dedicated section or subsection addressing it?
- Is the analysis backed by citations (GitHub releases, PyPI, api-docs.deepseek.com, openai.github.io)?
- Are DeepSeek compatibility claims verified (per D-04, D-11)?

Specific checks:
- D-01 (full stack): Sections 1-4 cover SDK, agents, tools/MCP, sandbox, voice — verify each layer present
- D-02 (OpenAI SDK changes): Section 1.1 has version table v2.37.0→v2.41.1 — verify each version has impact assessment
- D-03 (Anthropic): Sections 1.3, 3 cover SDK, Managed Agents, Claude Code — verify all three
- D-04 (DeepSeek compat): Section 5 has detailed verification tables — verify both OpenAI-compatible and Anthropic-compatible APIs assessed
- D-05 (Voice/Realtime): Section 2.4 + 6.2 — verify conclusion documented
- D-06 (Sandbox): Sections 2.3, 3.2, 6.4 — verify conclusion documented
- D-07 (Agent loops): Section 6.1 — verify architecture diagrams present, comparison table complete
- D-08 (Overall comparison): Section 7 — verify recommendation present with rationale
- D-09 (Full RESEARCH.md): This document itself — verify all required sections exist
- D-10 (Deep research): Verify release notes cited, code examples present, DeepSeek API tested
- D-11 (Feature matrix): Section 4 feature matrix exists, Section 5 DeepSeek verification exists
- D-12 (Agent loop focus): Section 6.1 has both architecture diagrams

If any decision is MISSING or INSUFFICIENTLY covered, add the missing analysis to RESEARCH.md. Add a "## 12. Validation Checklist" section at the end of RESEARCH.md with a D-01 through D-12 coverage table showing status (✅ COVERED / ⚠️ PARTIAL — [what's missing] / ❌ GAP — [what needs adding]) for each decision.

If the section numbering in RESEARCH.md shifts (e.g., adding Section 12 pushes code examples from Section 11), update cross-references in the Phase Requirements table at the top of RESEARCH.md.
  </action>
  <verify>
    <automated>grep -c "^| D-0[1-9]" /home/ros2/rob_box_project/.planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-RESEARCH.md && grep -c "✅ COVERED" /home/ros2/rob_box_project/.planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-RESEARCH.md</automated>
  </verify>
  <done>All 12 D-XX decisions have a ✅ COVERED status in the validation checklist. No ❌ GAP entries remain. Section cross-references in the Phase Requirements table are accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Pin openai and openai-agents versions in all requirements.txt files</name>
  <files>
    docker/vision/voice_assistant/requirements.txt,
    docker/vision/voice_base/requirements.txt
  </files>
  <action>
Pin versions in the two Docker voice requirements files that currently have unpinned `openai` and `openai-agents`. The other three requirements files already have `openai>=2.37.0` and need no changes — verify and skip.

For docker/vision/voice_assistant/requirements.txt (lines 62-63):
- Change `openai` → `openai>=2.37.0,<3.0.0`
- Change `openai-agents` → `openai-agents>=0.17.0,<1.0.0`

For docker/vision/voice_base/requirements.txt (lines 63-64):
- Change `openai` → `openai>=2.37.0,<3.0.0`
- Change `openai-agents` → `openai-agents>=0.17.0,<1.0.0`

Upper bounds rationale:
- `<3.0.0` on openai: v3.0.0 would be another major breaking change (v2 was the Stainless SDK rewrite). Upper bound prevents automatic breakage.
- `<1.0.0` on openai-agents: pre-1.0 follows semver conventions where minor versions can break. Upper bound gives control over when to absorb breaking changes.

Verify final state with grep across all requirements.txt files:
- `grep -n "openai" **/requirements.txt` — every occurrence must have a version pin
- `grep -n "openai-agents" **/requirements.txt` — every occurrence must have a version pin
- No bare `openai` or `openai-agents` without version constraint

Do NOT install/upgrade packages on the dev machine — that's a separate concern for the developer to handle manually (needs to verify Docker build works after pinning, and dev machine upgrade is a `pip install` one-liner documented in RESEARCH.md Section 7.2).
  </action>
  <verify>
    <automated>grep -rn "openai" /home/ros2/rob_box_project/docker/vision/voice_assistant/requirements.txt /home/ros2/rob_box_project/docker/vision/voice_base/requirements.txt | grep -v "^.*#" | grep -v "openai-whisper" && echo "---" && grep -rn "openai-agents" /home/ros2/rob_box_project/docker/vision/voice_assistant/requirements.txt /home/ros2/rob_box_project/docker/vision/voice_base/requirements.txt | grep -v "^.*#"</automated>
  </verify>
  <done>All 5 requirements.txt files have openai pinned to >=2.37.0,<3.0.0 (or >=2.37.0 for the 3 already-pinned files). The 2 Docker voice files additionally have openai-agents>=0.17.0,<1.0.0. No bare unpinned openai or openai-agents remain.</done>
</task>

<task type="auto">
  <name>Task 3: Document final decision in ROADMAP.md and close out the phase</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update ROADMAP.md Phase 03.1 entry to reflect the completed research. Current placeholder text:
```
**Goal:** [Urgent work - to be planned]
**Requirements**: TBD
**Plans:** 0 plans
```

Replace with the actual research outcome:
- **Goal:** "Determine whether OpenAI Agents SDK or Anthropic Claude SDK better serves Rob Box agentic features, with DeepSeek API compatibility verification"
- **Requirements:** List D-01 through D-12 (the CONTEXT decisions that drove the research)
- **Success Criteria:** Add 3 measurable criteria:
  1. RESEARCH.md covers all 12 D-XX decisions with DeepSeek compatibility verified for each
  2. All requirements.txt files have pinned openai>=2.37.0 and openai-agents>=0.17.0
  3. Decision recorded: stay with OpenAI Agents SDK, Anthropic SDK adds zero value for DeepSeek-only setup
- **Decision:** Add to ROADMAP phase entry: "**Outcome:** Stay with OpenAI Agents SDK. Anthropic SDK skipped — Managed Agents is cloud-only, DeepSeek Anthropic API has critical gaps (no MCP, no images, no code execution). See 03.1-RESEARCH.md Section 7 for full decision matrix."
- **Plans:** Update to "1 plan" with the plan list item

Update the Plans list from `[ ] TBD (run /gsd-plan-phase 03.1 to break down)` to:
```
Plans:
- [ ] 03.1-01-PLAN.md — Validate research, pin versions, document decision
```

Also update the Progress table at the bottom of ROADMAP.md to include Phase 03.1:
```
| 03.1. OpenAI vs Anthropic SDK Research | 0/1 | Not started | - |
```

Do NOT create a separate DECISION.md file — the ROADMAP.md is the single source of truth for phase decisions, and RESEARCH.md Section 7 already contains the full decision matrix. Duplicating decisions across files creates maintenance burden.
  </action>
  <verify>
    <automated>grep -c "Stay with OpenAI Agents SDK" /home/ros2/rob_box_project/.planning/ROADMAP.md && grep -c "03.1-01-PLAN.md" /home/ros2/rob_box_project/.planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md Phase 03.1 entry has actual goal, requirements (D-01 through D-12), success criteria, decision outcome, and plan list. Progress table includes Phase 03.1 row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → requirements.txt | Malicious package could be installed if version ranges are too loose |
| Docker build → pip install | Unpinned versions could pull breaking changes mid-development |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Tampering | requirements.txt version pins | mitigate | Pin with upper bounds (`<3.0.0`, `<1.0.0`) to prevent automatic major version bumps that could introduce breaking changes or compromised packages |
| T-03.1-02 | Information Disclosure | DeepSeek API key in code | accept | API key already managed via ROS params and Docker secrets; no changes in this phase affect key management |
| T-03.1-03 | Elevation of Privilege | openai-agents SDK tracing | mitigate | `set_tracing_disabled(True)` already enforced in Rob Box setup (per RESEARCH.md Pitfall 3); verify not accidentally re-enabled during version upgrade |
</threat_model>

<verification>
## Phase Verification

After all tasks complete, run these checks:

```bash
# 1. All requirements.txt files have version-pinned openai (no bare names)
grep -rn "^openai$" docker/vision/voice_assistant/requirements.txt docker/vision/voice_base/requirements.txt
# EXPECTED: no output (all have version constraints)

# 2. RESEARCH.md validation checklist is complete
grep -c "✅ COVERED" .planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-RESEARCH.md
# EXPECTED: 12

# 3. ROADMAP.md has decision recorded
grep "Stay with OpenAI Agents SDK" .planning/ROADMAP.md
# EXPECTED: 1+ matches

# 4. No unpinned openai-agents anywhere
grep -rn "^openai-agents$" docker/vision/voice_assistant/requirements.txt docker/vision/voice_base/requirements.txt
# EXPECTED: no output
```
</verification>

<success_criteria>
## Success Criteria

1. **Pinned everywhere:** `grep -rn "^openai$\|^openai-agents$" **/requirements.txt` returns zero matches — no bare unpinned package names
2. **Research validated:** RESEARCH.md Section 12 has a D-01 through D-12 checklist with all ✅ COVERED
3. **Decision recorded:** ROADMAP.md Phase 03.1 entry states "Stay with OpenAI Agents SDK" and references RESEARCH.md Section 7
4. **Phase close-out ready:** Phase 03.1 can be marked complete in STATE.md after this plan executes
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-research-openai-agent-sdk-vs-anthropic-claude-sdk-agentic-fe/03.1-01-SUMMARY.md`
</output>
