---
phase: 05-lyrical-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Base images
  - docker/base/Dockerfile.ros2-zenoh
  - docker/base/Dockerfile.rtabmap
  - docker/base/Dockerfile.pcl
  # .env files
  - docker/.env.main
  - docker/.env.develop
  - docker/.env.feature
  - docker/main/.env
  - docker/vision/.env
  - .env.example
  - docker/vision/.env.example
  # docker-compose
  - docker/main/docker-compose.yaml
  - docker/vision/docker-compose.yaml
  # Main Pi Dockerfiles (non-nav2)
  - docker/main/lslidar/Dockerfile
  - docker/main/robot_state_publisher/Dockerfile
  - docker/main/rtabmap/Dockerfile
  - docker/main/teleop/Dockerfile
  - docker/main/twist_mux/Dockerfile
  - docker/main/micro_ros_agent/Dockerfile
  - docker/main/ros2_control/Dockerfile
  - docker/main/vesc_nexus/Dockerfile
  - docker/main/perception/Dockerfile
  - docker/main/zenoh-router/Dockerfile
  # Vision Pi Dockerfiles (non-depthai)
  - docker/vision/oak-d/Dockerfile
  - docker/vision/apriltag/Dockerfile
  - docker/vision/ceiling-camera/Dockerfile
  - docker/vision/led_matrix/Dockerfile
  - docker/vision/voice_assistant/Dockerfile
  - docker/vision/voice_base/Dockerfile
  - docker/vision/voice_resources/Dockerfile
  - docker/vision/telegram_bot/Dockerfile
  - docker/vision/zenoh-router/Dockerfile
  # CI/CD workflows
  - .github/workflows/G-Build Base Images.yml
  - .github/workflows/G-Build Main Pi Services.yml
  - .github/workflows/G-Build Vision Pi Services.yml
  - .github/workflows/G-Build All Services.yml
  - .github/workflows/G-Auto-merge Feature to Develop.yml
  - .github/workflows/G-Auto-merge to Main.yml
  - .github/workflows/G-Run Tests.yml
  - .github/workflows/G-Lint Code.yml
  - .github/workflows/L-Build Base Images.yml
  - .github/workflows/L-Build Main Pi Services.yml
  - .github/workflows/L-Build Vision Pi Services.yml
  - .github/workflows/L-Build All Services.yml
  - .github/workflows/L-Build Single Service.yml
  - .github/workflows/L-Integration Tests.yml
  - .github/workflows/L-Deploy and Verify.yml
  # Scripts with humble paths
  - docker/main/scripts/patch_rtabmap_launch.py
  - docker/main/scripts/switch_version.sh
  - docker/vision/scripts/switch_version.sh
  - docker/build/workflow-example.yml
  - docker/build/scripts/delete_robot_state_publisher.sh
  - docker/build/scripts/push_to_local_registry.sh
  # Scripts with /opt/ros/humble references (~20 files, see Task 4 action)
  # docker/scripts/start_*.sh, docker/{main,vision}/scripts/**/start_*.sh
  # scripts/utils/*.sh, scripts/diagnostics/*.sh, local_test/*.sh
  # docker/vision/scripts/diagnose.sh
  # scripts/testing/test_docker_smoke.sh
autonomous: true
requirements: [MIG-01, MIG-02, MIG-05]
# MIG-03 (Nav2 source-build) and MIG-04 (DepthAI source-build) deferred to 05-02-PLAN.md (Wave 2)

must_haves:
  truths:
    - "Все Dockerfile'ы (кроме nav2 и depthai) используют ros:lyrical-ros-base или lyrical-варианты базовых образов"
    - "Все ros-humble-* пакеты заменены на ros-lyrical-* (кроме behaviotree-cpp-v3 → behaviotree-cpp)"
    - "Все pip install команды имеют --break-system-packages и python3-pip в apt-зависимостях"
    - "Все CI/CD workflow используют ROS_DISTRO: lyrical"
    - "Все .env файлы имеют ROS_DISTRO=lyrical"
    - "Все source /opt/ros/humble заменены на source /opt/ros/lyrical"
    - "Все LD_LIBRARY_PATH ссылаются на /opt/ros/lyrical/"
    - "behaviortree-cpp-v3 → behaviortree-cpp (единственное нестандартное переименование)"
  artifacts:
    - path: "docker/base/Dockerfile.ros2-zenoh"
      provides: "Базовый образ ROS 2 Lyrical + Zenoh"
      contains: "FROM ros:lyrical-ros-base"
    - path: "docker/base/Dockerfile.rtabmap"
      provides: "Базовый образ RTAB-Map Lyrical"
      contains: "FROM introlab3it/rtabmap_ros:lyrical-latest"
    - path: "docker/base/Dockerfile.pcl"
      provides: "Базовый образ PCL Lyrical"
      contains: "ros-lyrical-pcl-conversions"
    - path: "docker/.env.main"
      provides: "Production .env"
      contains: "ROS_DISTRO=lyrical"
    - path: ".github/workflows/G-Build Base Images.yml"
      provides: "CI/CD base images workflow"
      contains: "ROS_DISTRO: lyrical"
    - path: ".github/workflows/G-Run Tests.yml"
      provides: "CI/CD tests workflow"
      contains: "required-ros-distributions: lyrical"
  key_links:
    - from: "docker/main/docker-compose.yaml"
      to: "docker/.env.main"
      via: "${ROS_DISTRO:-humble} → ${ROS_DISTRO:-lyrical}"
      pattern: "\\$\\{ROS_DISTRO:-lyrical\\}"
    - from: "docker/vision/docker-compose.yaml"
      to: "docker/vision/.env"
      via: "${ROS_DISTRO:-humble} → ${ROS_DISTRO:-lyrical}"
      pattern: "\\$\\{ROS_DISTRO:-lyrical\\}"
    - from: "All Dockerfiles with pip3 install"
      to: "python3-pip apt package"
      via: "apt-get install python3-pip before pip3 install"
      pattern: "python3-pip"
    - from: "All Dockerfiles with pip3 install"
      to: "--break-system-packages flag"
      via: "pip3 install --break-system-packages"
      pattern: "--break-system-packages"
---

<objective>
Выполнить механическую миграцию ROS 2 Humble → Lyrical Luth для ВСЕХ файлов проекта, КРОМЕ Nav2 и DepthAI (эти два компонента — Wave 2).

**Purpose:** Заменить все строковые вхождения `humble`→`lyrical`, `jammy`→`resolute`, `22.04`→`26.04`, адаптировать Python 3.14 (pip `--break-system-packages`), обновить CI/CD, скрипты и .env. Единственное нестандартное переименование: `behaviortree-cpp-v3`→`behaviortree-cpp`.

**Output:** ~200 изменённых файлов, готовых к PR и CI-сборке (base-образы должны собраться).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
# Source artifacts
@.planning/phases/05-lyrical-migration/05-CONTEXT.md
@.planning/phases/05-lyrical-migration/05-RESEARCH.md
@docs/plans/lyrical-migration-plan.md
@docs/development/DOCKER_STANDARDS.md
@docs/CI_CD_PIPELINE.md
@.planning/STATE.md

# Key decisions (all LOCKED, do not revisit)
# D-01: Humble→Lyrical напрямую от develop (не Kilted→Lyrical)
# D-02: DepthAI — свой Dockerfile (Wave 2, НЕ трогать docker/base/Dockerfile.depthai)
# D-03: Zenoh — внешний пакет ros-lyrical-rmw-zenoh-cpp, архитектура zenoh-router НЕ меняется
# D-04: Lyrical подтверждён (Ubuntu 26.04 ✅, 2154 пакета, rtabmap ✅)
# D-05: Приоритет — перед Milestone 2
# D-06: База — develop (Humble), не feature/kilted

<interfaces>
<!-- Key patterns for executors. Grepped from existing files. -->

**Mechanical replacement rules (STRICT ORDER — apply in this sequence):**

1. `behaviortree-cpp-v3` → `behaviortree-cpp` (MUST be first! Otherwise s/humble/lyrical/ creates invalid `behaviortree-cpp-v3`)
2. `humble` → `lyrical` (covers: ros-humble-*, /opt/ros/humble/, *-humble-*, FROM ros:humble-*, introlab3it/rtabmap_ros:humble-latest)
3. `jammy` → `resolute` (in apt sources/references)
4. `22.04` → `26.04` (Ubuntu version in comments/configs)

**Python 3.14 adaptation signature:**
- BEFORE: `pip3 install ...` (without --break-system-packages)
- AFTER: `pip3 install --break-system-packages ...`
- MUST also add `python3-pip` to apt dependencies if pip3 is used (not preinstalled in ros:lyrical-ros-base)

**Files EXCLUDED from Wave 1 (DO NOT TOUCH):**
- docker/base/Dockerfile.depthai — Wave 2: source-build depthai-core
- docker/main/nav2/Dockerfile — Wave 2: source-build Nav2
- docker/main/nav2/** — любые nav2-специфичные конфиги/launch
- docker/main/scripts/nav2/** — nav2 launch скрипты (только path замена допустима)
- docker/vision/oak-d/config/** — oak-d launch файлы (только path замена допустима)

**Files where source /opt/ros/humble → /opt/ros/lyrical is ALLOWED but nav2 behavior changes are NOT:**
- docker/main/scripts/nav2/start_nav2_direct.sh — только замена пути, логика не меняется
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foundation — Base Dockerfiles + .env + docker-compose</name>
  <files>
    docker/base/Dockerfile.ros2-zenoh
    docker/base/Dockerfile.rtabmap
    docker/base/Dockerfile.pcl
    docker/.env.main
    docker/.env.develop
    docker/.env.feature
    docker/main/.env
    docker/vision/.env
    .env.example
    docker/vision/.env.example
    docker/main/docker-compose.yaml
    docker/vision/docker-compose.yaml
  </files>
  <action>
Выполнить механическую миграцию базовых образов и конфигурационных файлов (фундамент, от которого зависят все сервисы).

**Шаг 1.1 — docker/base/Dockerfile.ros2-zenoh:**
- `FROM ros:humble-ros-base` → `FROM ros:lyrical-ros-base`
- `ros-humble-rmw-zenoh-cpp` → `ros-lyrical-rmw-zenoh-cpp`
- `LD_LIBRARY_PATH=/opt/ros/humble/...` → `LD_LIBRARY_PATH=/opt/ros/lyrical/...` (3 occurrences в одной строке)
- `description="Base ROS 2 Humble + Zenoh image"` → `description="Base ROS 2 Lyrical + Zenoh image"`
- `python3-pip` уже присутствует — оставить как есть

**Шаг 1.2 — docker/base/Dockerfile.rtabmap:**
- `FROM introlab3it/rtabmap_ros:humble-latest` → `FROM introlab3it/rtabmap_ros:lyrical-latest` (per D-04: ✅ доступен)
- `ros-humble-image-transport-plugins` → `ros-lyrical-image-transport-plugins`
- `ros-humble-compressed-image-transport` → `ros-lyrical-compressed-image-transport`
- `ros-humble-theora-image-transport` → `ros-lyrical-theora-image-transport`
- `ros-humble-rmw-zenoh-cpp` → `ros-lyrical-rmw-zenoh-cpp`
- `LD_LIBRARY_PATH=/opt/ros/humble/...` → `LD_LIBRARY_PATH=/opt/ros/lyrical/...`

**Шаг 1.3 — docker/base/Dockerfile.pcl:**
- `ros-humble-pcl-conversions` → `ros-lyrical-pcl-conversions`
- `ros-humble-pcl-ros` → `ros-lyrical-pcl-ros`
- `ros-humble-diagnostic-updater` → `ros-lyrical-diagnostic-updater`

**Шаг 1.4 — docker/base/Dockerfile.depthai: ❌ НЕ ТРОГАТЬ (Wave 2)**

**Шаг 1.5 — Все .env файлы (5 файлов):**
- `ROS_DISTRO=humble` → `ROS_DISTRO=lyrical`
- Комментарии `*-humble-*` → `*-lyrical-*` (в примерах тегов образов)

**Шаг 1.6 — docker-compose.yaml (оба: main + vision):**
- `${ROS_DISTRO:-humble}` → `${ROS_DISTRO:-lyrical}` (во всех image: строках и environment:)
- `LD_LIBRARY_PATH=/opt/ros/humble/...` → `LD_LIBRARY_PATH=/opt/ros/lyrical/...` (все вхождения)
- `ROS_DISTRO=humble` → `ROS_DISTRO=lyrical` (в environment: блоках, например voice-assistant)
- `${BASE_IMAGE:-ghcr.io/krikz/rob_box_base:ros2-zenoh-humble-latest}` → заменить на вариант без hardcoded humble — использовать `ros2-zenoh-${ROS_DISTRO:-lyrical}-latest` если такой паттерн есть, иначе просто `lyrical`

**ПОРЯДОК ЗАМЕН (критично!):**
1. Сначала `behaviortree-cpp-v3` → `behaviortree-cpp` (в этих файлах отсутствует, но проверка не помешает)
2. Затем `humble` → `lyrical`
3. Затем `jammy` → `resolute` (если есть)
4. Затем `22.04` → `26.04` (если есть)

**Для .env файлов:** только `humble`→`lyrical`, так как `jammy`/`22.04` в них отсутствуют.
  </action>
  <verify>
    <automated>
# Verify no humble left in base Dockerfiles
grep -n 'humble' docker/base/Dockerfile.ros2-zenoh docker/base/Dockerfile.rtabmap docker/base/Dockerfile.pcl && echo "FAIL: humble found" || echo "PASS: no humble"
# Verify lyrical present
grep -c 'lyrical' docker/base/Dockerfile.ros2-zenoh docker/base/Dockerfile.rtabmap docker/base/Dockerfile.pcl
# Verify .env files (5 active + 2 example)
grep 'ROS_DISTRO' docker/.env.main docker/.env.develop docker/.env.feature docker/main/.env docker/vision/.env .env.example docker/vision/.env.example | grep -v lyrical && echo "FAIL: non-lyrical ROS_DISTRO" || echo "PASS"
# Verify docker-compose
grep -c 'humble' docker/main/docker-compose.yaml docker/vision/docker-compose.yaml
    </automated>
  </verify>
  <done>
Все 3 базовых Dockerfile (ros2-zenoh, rtabmap, pcl) используют lyrical.
Dockerfile.depthai НЕ тронут.
Все 7 .env файлов (5 активных + 2 example): ROS_DISTRO=lyrical.
Оба docker-compose.yaml: ${ROS_DISTRO:-lyrical}, LD_LIBRARY_PATH=/opt/ros/lyrical/...
  </done>
</task>

<task type="auto">
  <name>Task 2: Main Pi Dockerfiles (10 сервисов, кроме nav2)</name>
  <files>
    docker/main/lslidar/Dockerfile
    docker/main/robot_state_publisher/Dockerfile
    docker/main/rtabmap/Dockerfile
    docker/main/teleop/Dockerfile
    docker/main/twist_mux/Dockerfile
    docker/main/micro_ros_agent/Dockerfile
    docker/main/ros2_control/Dockerfile
    docker/main/vesc_nexus/Dockerfile
    docker/main/perception/Dockerfile
    docker/main/zenoh-router/Dockerfile
  </files>
  <action>
Выполнить механическую миграцию всех Main Pi Dockerfile'ов КРОМЕ nav2.

**Общий алгоритм для КАЖДОГО Dockerfile:**
1. Проверить наличие `behaviortree-cpp-v3` → если есть, заменить на `behaviortree-cpp` ДО основной замены
2. `s/humble/lyrical/g` — все вхождения (ros-humble-* → ros-lyrical-*, source /opt/ros/humble → /opt/ros/lyrical, BASE_IMAGE=...humble... → ...lyrical...)
3. Если есть `pip3 install` / `pip install`:
   a. Добавить `python3-pip` в apt-зависимости (если ещё нет)
   b. Добавить `--break-system-packages` ко ВСЕМ pip install командам
4. Если есть `${ROS_DISTRO}` переменная — она уже расширится правильно после замены `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`

**Пофайловые указания:**

**lslidar/Dockerfile:**
- 6 пакетов `ros-humble-*` → `ros-lyrical-*`
- `source /opt/ros/humble/setup.bash` → `source /opt/ros/lyrical/setup.bash`
- Комментарий `# LSLIDAR N10 Driver для ROS 2 Humble` → `Lyrical`

**robot_state_publisher/Dockerfile:**
- `ros-humble-robot-state-publisher` → `ros-lyrical-robot-state-publisher`
- `ros-humble-xacro` → `ros-lyrical-xacro`

**rtabmap/Dockerfile:**
- Может не содержать прямых humble-строк (использует ARG BASE_IMAGE). Проверить grep, применить s/humble/lyrical/ при необходимости.

**teleop/Dockerfile:**
- `FROM ghcr.io/krikz/rob_box_base:ros2-zenoh-humble-latest` → `FROM ghcr.io/krikz/rob_box_base:ros2-zenoh-lyrical-latest`
- `ros-${ROS_DISTRO}-joy` → после замены ARG ROS_DISTRO=lyrical подставится правильно
- `ros-${ROS_DISTRO}-joy-linux` → аналогично
- Нет pip install — python3-pip не нужен

**twist_mux/Dockerfile:**
- `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`
- `ros-${ROS_DISTRO}-twist-mux` — подставится правильно
- Нет pip install

**micro_ros_agent/Dockerfile:**
- `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`
- Уже содержит `python3-pip` в apt-зависимостях

**ros2_control/Dockerfile:**
- 12+ пакетов `ros-humble-*` → `ros-lyrical-*`
- Комментарий про Humble можно обновить
- Нет pip install

**vesc_nexus/Dockerfile:**
- `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`
- Все ros-${ROS_DISTRO}-* подставятся правильно
- Нет pip install

**perception/Dockerfile:**
- `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`
- 5 пакетов `ros-humble-*` → `ros-lyrical-*`
- **КРИТИЧНО: pip3 install:** добавить `--break-system-packages` к строке 43 (`pip3 install --retries 5 --timeout 120 -r /tmp/perception_requirements.txt`)
- `python3-pip` уже есть в apt-зависимостях — оставить

**zenoh-router/Dockerfile:**
- `FROM introlab3it/rtabmap_ros:humble-latest` → `FROM introlab3it/rtabmap_ros:lyrical-latest` (per D-04)
- `ros-humble-rmw-zenoh-cpp` → `ros-lyrical-rmw-zenoh-cpp`
- `source /opt/ros/humble/setup.bash` → `source /opt/ros/lyrical/setup.bash`

**nav2/Dockerfile: ❌ НЕ ТРОГАТЬ (Wave 2)**

**ПОРЯДОК ЗАМЕН ДЛЯ КАЖДОГО ФАЙЛА:**
1. `behaviortree-cpp-v3` → `behaviortree-cpp` (если встречается)
2. `humble` → `lyrical`
3. Добавить `--break-system-packages` где нужно
4. Добавить `python3-pip` в apt где нужно (если есть pip install но нет python3-pip)
  </action>
  <verify>
    <automated>
# Verify no humble in Main Pi Dockerfiles (excluding nav2)
for f in docker/main/lslidar/Dockerfile docker/main/robot_state_publisher/Dockerfile docker/main/rtabmap/Dockerfile docker/main/teleop/Dockerfile docker/main/twist_mux/Dockerfile docker/main/micro_ros_agent/Dockerfile docker/main/ros2_control/Dockerfile docker/main/vesc_nexus/Dockerfile docker/main/perception/Dockerfile docker/main/zenoh-router/Dockerfile; do
  if grep -n 'humble' "$f" 2>/dev/null; then echo "FAIL: humble found in $f"; else echo "PASS: $f"; fi
done
# Verify --break-system-packages in perception
grep -c -- '--break-system-packages' docker/main/perception/Dockerfile
# Verify nav2 NOT touched (should still have humble)
grep -c 'humble' docker/main/nav2/Dockerfile 2>/dev/null && echo "WARNING: nav2 still has humble (expected for Wave 1)" || echo "CHECK: nav2 has no humble (unexpected)"
    </automated>
  </verify>
  <done>
Все 10 Main Pi Dockerfile'ов используют lyrical. nav2/Dockerfile НЕ тронут.
Все pip install имеют --break-system-packages. Все ARG ROS_DISTRO=lyrical.
zenoh-router использует rtabmap_ros:lyrical-latest.
  </done>
</task>

<task type="auto">
  <name>Task 3: Vision Pi Dockerfiles (9 сервисов, кроме depthai base)</name>
  <files>
    docker/vision/oak-d/Dockerfile
    docker/vision/apriltag/Dockerfile
    docker/vision/ceiling-camera/Dockerfile
    docker/vision/led_matrix/Dockerfile
    docker/vision/voice_assistant/Dockerfile
    docker/vision/voice_base/Dockerfile
    docker/vision/voice_resources/Dockerfile
    docker/vision/telegram_bot/Dockerfile
    docker/vision/zenoh-router/Dockerfile
    docker/vision/test/mock_llm/Dockerfile
    docker/vision/test/scenario_runner/Dockerfile
  </files>
  <action>
Выполнить механическую миграцию всех Vision Pi Dockerfile'ов.

**Пофайловые указания:**

**oak-d/Dockerfile:**
- `ros-humble-apriltag-ros` → `ros-lyrical-apriltag-ros`
- `ros-humble-image-transport-plugins` → `ros-lyrical-image-transport-plugins`
- `ros-humble-cv-bridge` → `ros-lyrical-cv-bridge`
- НЕ менять ARG BASE_IMAGE=ghcr.io/krikz/rob_box_base:depthai (сам Dockerfile.depthai — Wave 2)

**apriltag/Dockerfile:**
- `ros-humble-apriltag-ros` → `ros-lyrical-apriltag-ros`
- `ros-humble-image-transport-plugins` → `ros-lyrical-image-transport-plugins`
- `ros-humble-cv-bridge` → `ros-lyrical-cv-bridge`
- `dpkg -s ros-humble-apriltag-ros` → `dpkg -s ros-lyrical-apriltag-ros`

**ceiling-camera/Dockerfile:**
- `ros-humble-usb-cam` → `ros-lyrical-usb-cam`
- `ros-humble-image-transport-plugins` → `ros-lyrical-image-transport-plugins`
- `ros-humble-cv-bridge` → `ros-lyrical-cv-bridge`
- `dpkg -s ros-humble-usb-cam` → `dpkg -s ros-lyrical-usb-cam`

**led_matrix/Dockerfile:**
- `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`
- `ros-${ROS_DISTRO}-sensor-msgs` → подставится правильно
- `ros-${ROS_DISTRO}-std-msgs` → подставится правильно
- **КРИТИЧНО: pip3 install:** добавить `--break-system-packages` к строке 40-41 (`pip3 install --no-cache-dir --upgrade pip` и последующий `pip3 install`)
- `python3-pip` уже есть в apt-зависимостях

**voice_assistant/Dockerfile:**
- `ARG BASE_IMAGE=ghcr.io/krikz/rob_box:voice-base-humble-latest` → `voice-base-lyrical-latest`
- `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`
- Комментарий `Sourcing /opt/ros/humble/setup.sh` → обновить на lyrical

**voice_base/Dockerfile:**
- `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`
- Все `ros-${ROS_DISTRO}-*` подставятся правильно
- **КРИТИЧНО: pip3 install:** строки 73, 81, 90 — добавить `--break-system-packages` ко ВСЕМ pip3 install командам (их три)
- `python3-pip` уже есть в apt-зависимостях

**voice_resources/Dockerfile:**
- УЖЕ содержит `--break-system-packages` (строка 14)
- `FROM python:3.11-slim-bookworm` — ОСТАВИТЬ (это не ROS-образ, отдельный Python для one-shot инициализации)
- humble не упоминается — без изменений

**telegram_bot/Dockerfile:**
- `ARG ROS_DISTRO=humble` → `ARG ROS_DISTRO=lyrical`
- **КРИТИЧНО: pip3 install:** строка 23 — добавить `--break-system-packages`
- `python3-pip` уже есть в apt-зависимостях

**zenoh-router/Dockerfile (Vision Pi):**
- `FROM luxonis/depthai-ros:humble-latest` → `FROM ros:lyrical-ros-base` (per D-03: zenoh-router нужен только ROS + rmw_zenoh_cpp, незачем тащить depthai)
- `ros-humble-rmw-zenoh-cpp` → `ros-lyrical-rmw-zenoh-cpp`
- `source /opt/ros/humble/setup.bash` → `source /opt/ros/lyrical/setup.bash`

**test/mock_llm/Dockerfile:**
- `pip install --no-cache-dir fastapi uvicorn pyyaml` → добавить `--break-system-packages`
- humble не упоминается — только pip fix

**test/scenario_runner/Dockerfile:**
- `ARG BASE_IMAGE=ghcr.io/krikz/rob_box:voice-assistant-humble-latest` → `voice-assistant-lyrical-latest`
- `pip install --no-cache-dir requests pyyaml` → добавить `--break-system-packages`
- `ENV PYTHONPATH=/opt/ros/humble/lib/python3.10/site-packages` → `PYTHONPATH=/opt/ros/lyrical/lib/python3.14/site-packages`

**supercollider/Dockerfile:** 
- Уже `FROM ubuntu:26.04` — humble не упоминается, БЕЗ ИЗМЕНЕНИЙ

**ПОРЯДОК ЗАМЕН для каждого файла:**
1. `behaviortree-cpp-v3` → `behaviortree-cpp` (проверить, маловероятно в vision)
2. `humble` → `lyrical`
3. Добавить `--break-system-packages` ко всем pip командам
4. vision/zenoh-router особый случай: сменить FROM образ на ros:lyrical-ros-base
   **(D-03-OVERRIDE):** D-03 говорит "архитектура НЕ меняется", но текущий Dockerfile использует `FROM luxonis/depthai-ros:humble-latest` как базовый образ, что архитектурно некорректно — zenoh-router не зависит от depthai. Поскольку `depthai-ros:lyrical` не существует, меняем базу на `ros:lyrical-ros-base`. Роль контейнера (standalone zenoh router) сохраняется. Архитектурная роль zenoh-router НЕ меняется — меняется только базовый образ.
  </action>
  <verify>
    <automated>
# Verify no humble in Vision Pi Dockerfiles
for f in docker/vision/oak-d/Dockerfile docker/vision/apriltag/Dockerfile docker/vision/ceiling-camera/Dockerfile docker/vision/led_matrix/Dockerfile docker/vision/voice_assistant/Dockerfile docker/vision/voice_base/Dockerfile docker/vision/telegram_bot/Dockerfile docker/vision/zenoh-router/Dockerfile; do
  if grep -n 'humble' "$f" 2>/dev/null; then echo "FAIL: humble found in $f"; else echo "PASS: $f"; fi
done
# Verify --break-system-packages in all pip-using files
for f in docker/vision/led_matrix/Dockerfile docker/vision/voice_base/Dockerfile docker/vision/telegram_bot/Dockerfile docker/vision/test/mock_llm/Dockerfile docker/vision/test/scenario_runner/Dockerfile; do
  echo -n "$f: "
  grep -c -- '--break-system-packages' "$f" 2>/dev/null || echo "0 (MISSING!)"
done
# Verify zenoh-router uses ros:lyrical-ros-base
head -1 docker/vision/zenoh-router/Dockerfile
# Verify voice_resources and supercollider still correct
head -1 docker/vision/voice_resources/Dockerfile
head -1 docker/vision/supercollider/Dockerfile
    </automated>
  </verify>
  <done>
Все 9 Vision Pi Dockerfile'ов используют lyrical (или без изменений для non-ROS).
Все pip install имеют --break-system-packages.
vision/zenoh-router переведён на ros:lyrical-ros-base.
voice_resources и supercollider не имеют humble-зависимостей.
  </done>
</task>

<task type="auto">
  <name>Task 4: CI/CD Workflows + Scripts + Прочие файлы</name>
  <files>
    .github/workflows/G-Build Base Images.yml
    .github/workflows/G-Build Main Pi Services.yml
    .github/workflows/G-Build Vision Pi Services.yml
    .github/workflows/G-Build All Services.yml
    .github/workflows/G-Auto-merge Feature to Develop.yml
    .github/workflows/G-Auto-merge to Main.yml
    .github/workflows/G-Run Tests.yml
    .github/workflows/G-Lint Code.yml
    .github/workflows/L-Build Base Images.yml
    .github/workflows/L-Build Main Pi Services.yml
    .github/workflows/L-Build Vision Pi Services.yml
    .github/workflows/L-Build All Services.yml
    .github/workflows/L-Build Single Service.yml
    .github/workflows/L-Integration Tests.yml
    .github/workflows/L-Deploy and Verify.yml
    docker/main/scripts/patch_rtabmap_launch.py
    docker/main/scripts/switch_version.sh
    docker/vision/scripts/switch_version.sh
    docker/build/workflow-example.yml
    docker/build/scripts/delete_robot_state_publisher.sh
    docker/build/scripts/push_to_local_registry.sh
    docker/main/scripts/ros2_control/start_ros2_control.sh
    # Plus all scripts with /opt/ros/humble (see action for find command)
  </files>
  <action>
Выполнить механическую миграцию CI/CD workflow и скриптов.

**Шаг 4.1 — CI/CD Workflows (15 файлов):**

Для КАЖДОГО workflow файла в `.github/workflows/`:
- `ROS_DISTRO: humble` → `ROS_DISTRO: lyrical` (env: переменные)
- `required-ros-distributions: humble` → `required-ros-distributions: lyrical` (в G-Run Tests)
- `ros-humble-*` → `ros-lyrical-*` (в apt-get install)
- `source /opt/ros/humble/setup.bash` → `source /opt/ros/lyrical/setup.bash`
- `*-humble-latest` → `*-lyrical-latest` (в комментариях PR body)
- `python-version: '3.10'` → `python-version: '3.14'` (в G-Lint Code)
- `ubuntu-22.04` → `ubuntu-26.04` (в G-Run Tests: runs-on)
- `Unit Tests (ROS2 Humble)` → `Unit Tests (ROS2 Lyrical)`
- `Set up ROS2 Humble` → `Set up ROS2 Lyrical`

**Особые случаи CI/CD:**
- `G-Run Tests.yml`: самый сложный — много humble-строк, включая `required-ros-distributions`, тестовые зависимости, source paths
- `L-Deploy and Verify.yml`: `docker exec ... source /opt/ros/humble/setup.bash` → lyrical
- `G-Lint Code.yml`: `python-version: '3.10'` → `'3.14'`

**Шаг 4.2 — Скрипты с /opt/ros/humble:**

Выполнить массовую замену во ВСЕХ скриптах проекта. Использовать find + sed:

```bash
# 1. Сначала behaviotree-cpp-v3 → behaviotree-cpp (везде)
find . -type f \( -name '*.sh' -o -name '*.py' -o -name '*.yml' -o -name '*.yaml' -o -name '*.bash' \) \
  -not -path './docker/base/Dockerfile.depthai' \
  -not -path './docker/main/nav2/*' \
  -exec grep -l 'behaviortree-cpp-v3' {} \; | while read f; do
    sed -i 's/behaviortree-cpp-v3/behaviortree-cpp/g' "$f"
done

# 2. Затем humble → lyrical (везде кроме nav2 и depthai)
find . -type f \( -name '*.sh' -o -name '*.py' -o -name '*.yml' -o -name '*.yaml' -o -name '*.bash' \) \
  -not -path './docker/base/Dockerfile.depthai' \
  -not -path './docker/main/nav2/*' \
  -not -path './docker/main/scripts/nav2/*' \
  -exec grep -l 'humble' {} \; | while read f; do
    sed -i 's/humble/lyrical/g' "$f"
done
```

(Это batch-подход для массовых файлов; Dockerfile'ы уже обработаны в Task 1-3.)

**Конкретные файлы с нестандартными заменами:**

**docker/main/scripts/patch_rtabmap_launch.py:**
- `LAUNCH_FILE = "/opt/ros/humble/share/rtabmap_launch/..."` → `/opt/ros/lyrical/share/rtabmap_launch/...`

**docker/main/scripts/switch_version.sh + docker/vision/scripts/switch_version.sh:**
- `humble|jazzy|kilted)` → `lyrical|jazzy|kilted)` (во всех case-блоках)
- `humble, jazzy, kilted` → `lyrical, jazzy, kilted` (в usage-сообщениях)
- Примеры использования: `$0 humble latest` → `$0 lyrical latest`

**docker/main/scripts/ros2_control/start_ros2_control.sh:**
- Комментарий `# В Humble нет ros2_control_node.launch.py` → `# В Lyrical может быть ros2_control_node.launch.py`

**docker/vision/scripts/diagnose.sh:**
- 5 строк `source /opt/ros/humble/setup.bash` → `/opt/ros/lyrical/setup.bash`
- `docker exec oak-d` остаётся без изменений

**docker/build/workflow-example.yml:**
- `ROS_DISTRO: humble` → `ROS_DISTRO: lyrical`

**docker/build/scripts/delete_robot_state_publisher.sh:**
- `*-humble-dev`, `*-humble-latest`, `*-humble-local` → `*-lyrical-*`

**docker/build/scripts/push_to_local_registry.sh:**
- Примеры в usage: `*-humble-latest` → `*-lyrical-latest`

**Остальные ~25 скриптов** с `source /opt/ros/humble/setup.bash` → обрабатываются batch-заменой выше.

**Шаг 4.3 — Проверка исключений:**

Убедиться, что НЕ заменены:
- `docker/base/Dockerfile.depthai` (Wave 2)
- `docker/main/nav2/Dockerfile` (Wave 2)
- `docker/main/nav2/` (все файлы в директории)
- `docker/main/scripts/nav2/` (скрипты навигации)
  </action>
  <verify>
    <automated>
# Verify CI/CD workflows
grep -rn 'humble' .github/workflows/ && echo "FAIL: humble found in CI/CD" || echo "PASS: CI/CD clean"
grep -rn 'ROS_DISTRO.*lyrical' .github/workflows/ | head -10
# Verify scripts (batch check)
echo "=== Checking for remaining humble in scripts (excluding nav2, depthai) ==="
find . -type f \( -name '*.sh' -o -name '*.py' \) \
  -not -path './docker/base/Dockerfile.depthai' \
  -not -path './docker/main/nav2/*' \
  -not -path './docker/main/scripts/nav2/*' \
  -not -path './.git/*' \
  -exec grep -l 'humble' {} \; 2>/dev/null | head -20
echo "=== If only nav2/depthai files above, PASS ==="
# Verify exclusions intact
grep -c 'humble' docker/base/Dockerfile.depthai 2>/dev/null && echo "PASS: depthai base excluded (has humble)" || echo "CHECK: depthai base"
grep -c 'humble' docker/main/nav2/Dockerfile 2>/dev/null && echo "PASS: nav2 excluded (has humble)" || echo "CHECK: nav2"
    </automated>
  </verify>
  <done>
Все 15 CI/CD workflow используют ROS_DISTRO: lyrical.
Все скрипты (кроме nav2) используют /opt/ros/lyrical/.
G-Run Tests: ubuntu-26.04 + required-ros-distributions: lyrical.
switch_version.sh: поддерживает lyrical вместо humble.
nav2 и depthai файлы НЕ тронуты.
  </done>
</task>

<task type="auto">
  <name>Task 5: Final Verification — Сборка base-образов + Diff Audit</name>
  <files>
    # Verification-only — no file modifications
    # Reads: все изменённые Dockerfile'ы для diff-check
  </files>
  <action>
Финальная верификация Wave 1 перед коммитом. НЕ вносит изменений в код — только проверяет.

**Шаг 5.1 — Diff audit: проверка исключений:**
```bash
# Убедиться что nav2 и depthai НЕ изменены
git diff --name-only | grep -E '(nav2|depthai)' && echo "FAIL: nav2/depthai modified!" || echo "PASS: nav2/depthai untouched"
```

**Шаг 5.2 — Проверка behaviotree-cpp:**
```bash
# Убедиться что нет behaviotree-cpp-v3 (сломанный sed)
grep -rn 'behaviortree-cpp-v3' --include='Dockerfile' --include='*.sh' --include='*.py' --include='*.yml' . && echo "FAIL: behaviotree-cpp-v3 still present" || echo "PASS"
# Убедиться что нет behaviotree-cpp-lyrical (двойная замена)
grep -rn 'behaviortree-cpp-lyrical' . && echo "FAIL: double-replace artifact" || echo "PASS"
```

**Шаг 5.3 — Проверка --break-system-packages coverage:**
```bash
# Найти все Dockerfile'ы с pip install но БЕЗ --break-system-packages
for f in $(grep -rl 'pip.*install' --include='Dockerfile' docker/); do
  if ! grep -q -- '--break-system-packages' "$f"; then
    echo "MISSING --break-system-packages in: $f"
  fi
done
```

**Шаг 5.4 — Проверка python3-pip coverage:**
```bash
# Найти Dockerfile'ы с pip install но БЕЗ python3-pip в apt
for f in $(grep -rl 'pip.*install' --include='Dockerfile' docker/); do
  if ! grep -q 'python3-pip' "$f"; then
    echo "MISSING python3-pip in: $f"
  fi
done
```

**Шаг 5.5 — Проверка LD_LIBRARY_PATH:**
```bash
grep -rn 'LD_LIBRARY_PATH.*humble' docker/ && echo "FAIL: old LD_LIBRARY_PATH" || echo "PASS"
```

**Шаг 5.6 — Проверка source /opt/ros/humble:**
```bash
grep -rn 'source /opt/ros/humble' --include='*.sh' --include='*.py' docker/ scripts/ .github/ && echo "FAIL: old source paths" || echo "PASS"

# Verify Zenoh LD_LIBRARY_PATH structure in Lyrical (RESEARCH.md Pitfall 6)
docker run --rm ros:lyrical-ros-base bash -c 'apt-get update -qq && apt-get install -qq -y ros-lyrical-rmw-zenoh-cpp && find /opt/ros/lyrical -name "libzenoh*" -o -name "*zenoh_cpp*" 2>/dev/null | head -20' || echo "WARNING: Could not verify Zenoh paths — check manually after CI build"
```

**Шаг 5.7 — Dry-run сборка ros2-zenoh base (опционально, только на dev-машине с Docker):**
```bash
# Проверить что Dockerfile.ros2-zenoh синтаксически корректен
docker build --check -f docker/base/Dockerfile.ros2-zenoh docker/base/ 2>&1 | head -20 || true
# docker build --check validates Dockerfile syntax without full build (requires BuildKit)
# Fallback if --check not available: docker build -f docker/base/Dockerfile.ros2-zenoh docker/base/ --target 0 2>&1 | head -30 || true
```

**Шаг 5.8 — Итоговый diff summary:**
```bash
echo "=== Files changed ==="
git diff --stat
echo "=== Summary ==="
echo "Total files: $(git diff --name-only | wc -l)"
echo "Dockerfiles: $(git diff --name-only | grep Dockerfile | wc -l)"
echo "Workflows: $(git diff --name-only | grep workflows | wc -l)"
echo "Scripts: $(git diff --name-only | grep -E '\.(sh|py)$' | wc -l)"
echo "Config (.env, yaml): $(git diff --name-only | grep -E '\.(env|yaml|yml)$' | wc -l)"
```
  </action>
  <verify>
    <automated>
# Run all verification steps from Task 5 action
echo "=== STEP 5.1: Exclusion check ==="
git diff --name-only | grep -E '(nav2|depthai)' && echo "FAIL" || echo "PASS"
echo "=== STEP 5.2: behaviortree-cpp check ==="
grep -rn 'behaviortree-cpp-v3' --include='Dockerfile' --include='*.sh' --include='*.py' --include='*.yml' docker/ .github/ 2>/dev/null && echo "FAIL" || echo "PASS"
grep -rn 'behaviortree-cpp-lyrical' docker/ .github/ 2>/dev/null && echo "FAIL: double-replace" || echo "PASS: no double-replace"
echo "=== STEP 5.3: --break-system-packages coverage ==="
for f in $(grep -rl 'pip.*install' --include='Dockerfile' docker/ 2>/dev/null); do
  grep -q -- '--break-system-packages' "$f" || echo "MISSING: $f"
done
echo "=== STEP 5.5: LD_LIBRARY_PATH check ==="
grep -rn 'LD_LIBRARY_PATH.*humble' docker/ 2>/dev/null && echo "FAIL" || echo "PASS"
echo "=== STEP 5.6: source path check ==="
grep -rn 'source /opt/ros/humble' --include='*.sh' --include='*.py' docker/ scripts/ .github/ 2>/dev/null && echo "FAIL" || echo "PASS"
echo "=== STEP 5.8: Diff summary ==="
git diff --stat | tail -5
    </automated>
  </verify>
  <done>
Все проверки пройдены:
- nav2 и depthai файлы не тронуты
- behaviotree-cpp-v3 → behaviotree-cpp (без артефактов двойной замены)
- Все pip install имеют --break-system-packages
- Все pip-using Dockerfile'ы имеют python3-pip в apt
- Ни одного humble в LD_LIBRARY_PATH
- Ни одного source /opt/ros/humble в скриптах
- ~200 файлов изменено, готово к коммиту
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dockerfile → APT repositories | apt-get install из packages.ros.org и archive.ubuntu.com |
| Dockerfile → PyPI | pip install из pypi.org (с --break-system-packages) |
| CI/CD workflow → GitHub Actions runner | Выполнение сборочных скриптов на ubuntu-latest/self-hosted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | APT package integrity | mitigate | Все ros-lyrical-* пакеты подписываются ключом ROS (ros.asc уже в ros:lyrical-ros-base). Проверка signatures включена по умолчанию. |
| T-05-02 | Tampering | pip packages via --break-system-packages | mitigate | --break-system-packages только отключает EXTERNALLY-MANAGED проверку, не снижает безопасность pip. Хеши пакетов можно зафиксировать в requirements.txt (отдельная задача). |
| T-05-03 | Elevation of Privilege | Docker containers (privileged: true) | accept | Существующая архитектура: oak-d, led-matrix, micro-ros-agent требуют privileged для доступа к USB/SPI/UART. Не меняется Wave 1. |
| T-05-04 | Denial of Service | CI/CD: ubuntu-22.04 → ubuntu-26.04 runner | mitigate | ubuntu-26.04 пока может быть недоступен в GitHub Actions. Fallback: использовать ubuntu-latest (24.04) + setup-ros с required-ros-distributions: lyrical (ros-tooling/setup-ros@v0.7 поддерживает произвольные дистрибутивы). |
| T-05-05 | Information Disclosure | CI/CD logs | accept | Сборочные логи не содержат секретов. Переменные окружения маскируются GitHub Actions. |

**Wave 1 Security Conclusion:** Все изменения — механическая замена строк, не вводят новых trust boundaries. Риски связаны с доступностью пакетов в новых репозиториях (mitigated: проверено в RESEARCH.md, 2154 пакета доступны).
</threat_model>

<verification>
## Overall Phase Verification (после выполнения всех tasks)

```bash
# 1. Полный grep на humble (должен найти ТОЛЬКО nav2 и depthai)
echo "=== Remaining 'humble' references (should be ONLY nav2 and depthai) ==="
grep -rn 'humble' --include='Dockerfile' --include='*.sh' --include='*.py' \
  --include='*.yml' --include='*.yaml' --include='*.env' \
  docker/ .github/ scripts/ local_test/ \
  --exclude-dir=.git 2>/dev/null | grep -v nav2 | grep -v depthai

# 2. Полный grep на /opt/ros/humble (должен быть пуст)
echo "=== Remaining '/opt/ros/humble' paths ==="
grep -rn '/opt/ros/humble' --include='*.sh' --include='*.py' --include='*.yml' \
  docker/ .github/ scripts/ local_test/ \
  --exclude-dir=.git 2>/dev/null

# 3. Статистика изменений
echo "=== Files modified ==="
git diff --name-only | wc -l
echo "=== Lines changed ==="
git diff --stat | tail -1
```

**Критерий успеха:** 
- `grep -rn humble | grep -v nav2 | grep -v depthai` возвращает ПУСТО (или только comments/examples где humble — historical reference)
- Все Dockerfile'ы для non-nav2, non-depthai сервисов используют lyrical
- CI/CD готов к запуску сборки (сборка может фейлиться на Nav2/DepthAI — это ожидаемо для Wave 1)
</verification>

<success_criteria>
## Measurable Completion

- [ ] 3 базовых Dockerfile'а (ros2-zenoh, rtabmap, pcl) мигрированы на lyrical
- [ ] Dockerfile.depthai НЕ тронут (Wave 2)
- [ ] 5 .env файлов: ROS_DISTRO=lyrical
- [ ] 2 docker-compose.yaml: ${ROS_DISTRO:-lyrical}, LD_LIBRARY_PATH=/opt/ros/lyrical/...
- [ ] 10 Main Pi Dockerfile'ов мигрированы (nav2 исключён)
- [ ] 9 Vision Pi Dockerfile'ов мигрированы
- [ ] Все pip install имеют --break-system-packages (6 Dockerfile'ов)
- [ ] Все pip-using Dockerfile'ы имеют python3-pip в apt
- [ ] 15 CI/CD workflow используют ROS_DISTRO: lyrical
- [ ] G-Run Tests: ubuntu-26.04 + required-ros-distributions: lyrical
- [ ] ~25 скриптов: source /opt/ros/lyrical/setup.bash
- [ ] behaviotree-cpp-v3 → behaviotree-cpp (единственное нестандартное)
- [ ] vision/zenoh-router: FROM ros:lyrical-ros-base (не depthai)
- [ ] main/zenoh-router: FROM introlab3it/rtabmap_ros:lyrical-latest
- [ ] patch_rtabmap_launch.py: /opt/ros/lyrical/share/...
- [ ] switch_version.sh (оба): lyrical в списке поддерживаемых
- [ ] ~200 файлов изменено, готово к git commit
</success_criteria>

<output>
After completion, create `.planning/phases/05-lyrical-migration/05-01-SUMMARY.md`

**Commit message format:**
```
feat(lyrical): Wave 1 — mechanical humble→lyrical migration (~200 files)

- Base Dockerfiles: ros2-zenoh, rtabmap, pcl → lyrical
- Main Pi Dockerfiles: 10 services (nav2 excluded for Wave 2)
- Vision Pi Dockerfiles: 9 services (depthai excluded for Wave 2)
- CI/CD: 15 workflows → ROS_DISTRO: lyrical
- Scripts: ~25 files → /opt/ros/lyrical/
- .env: 5 files → ROS_DISTRO=lyrical
- Python 3.14: --break-system-packages for all pip install
- Special: behaviortree-cpp-v3 → behaviortree-cpp

Refs: D-01 through D-06 (05-CONTEXT.md)
Excludes: Nav2 source-build, DepthAI source-build (→ Wave 2)
```
</output>
