# Plan 01-02: Обновление docs/architecture/ — Docker-топология и ROADMAP

**Phase**: 1 — Аудит документации  
**Goal**: `docs/architecture/SYSTEM_OVERVIEW.md` точно отражает реальную Docker-топологию (11 Main Pi + 10 Vision Pi сервисов); `ROADMAP.md` синхронизирован с фактическим статусом фич  
**Requirements**: DOCS-03, DOCS-04

---

## Must-Haves (from success criteria)

- [ ] SYSTEM_OVERVIEW.md перечисляет все 11 сервисов Main Pi включая `teleop`
- [ ] SYSTEM_OVERVIEW.md перечисляет все 10 сервисов Vision Pi включая `ceiling-camera`, `supercollider`, `voice-resources-init`, `telegram-bot`, `ollama`
- [ ] SYSTEM_OVERVIEW.md содержит таблицы сервисов (Main Pi, Vision Pi, Monitoring)
- [ ] SYSTEM_OVERVIEW.md НЕ упоминает `apriltag` как отдельный активный сервис Vision Pi
- [ ] SYSTEM_OVERVIEW.md явно указывает что Monitoring = отдельная машина
- [ ] `grep "container_name:" docker/main/docker-compose.yaml | wc -l` совпадает с количеством строк сервисов Main Pi в SYSTEM_OVERVIEW.md
- [ ] `grep "container_name:" docker/vision/docker-compose.yaml | wc -l` совпадает с количеством строк сервисов Vision Pi в SYSTEM_OVERVIEW.md
- [ ] ROADMAP.md отражает реальный статус реализованных фич (не только planned)

---

## Tasks

### Wave 1: Читать источники данных перед обновлением

#### Task 01-02-01: Проверить актуальные compose-файлы и зафиксировать полный список сервисов

**Type**: read  
**Autonomous**: true  
**Files modified**: (нет — только чтение для подготовки Task 01-02-02)

**Description**:

Прочитать оба compose-файла и зафиксировать полный список `container_name` перед обновлением документации:

```bash
echo "=== Main Pi services ==="
grep "container_name:" docker/main/docker-compose.yaml

echo "=== Vision Pi services ==="
grep "container_name:" docker/vision/docker-compose.yaml

echo "=== Main Pi: service depends_on chains ==="
grep -A5 "depends_on" docker/main/docker-compose.yaml | head -60

echo "=== Vision Pi: service depends_on chains ==="
grep -A5 "depends_on" docker/vision/docker-compose.yaml | head -60
```

Ожидаемые результаты (из research):

**Main Pi (11 сервисов)**:
- `zenoh-router` — Zenoh Router (peer mode)
- `twist-mux` — мультиплексирование cmd_vel
- `micro-ros-agent` — UART↔ROS2 bridge для ESP32
- `robot-state-publisher` — TF дерево из URDF
- `rtabmap` — LiDAR-only SLAM
- `ros2-control` — ros2_control manager + VESC CAN
- `lslidar` — LS LiDAR N10 driver
- `perception` — reflection, context_aggregator, health_monitor
- `nav2` — Nav2 navigation stack
- `teleop` — Joystick teleoperation (SBUS/serial)
- `cadvisor` (profile: monitoring) — Container metrics

**Vision Pi (10 сервисов)**:
- `zenoh-router-vision` — Zenoh Router (client mode)
- `oak-d` — OAK-D Lite driver + встроенный AprilTag detector
- `led-matrix` — LED matrix controller (381 NeoPixel LEDs)
- `ceiling-camera` — USB потолочная камера 720p
- `supercollider` — Audio synthesis server (JACK/ALSA)
- `voice-resources-init` — One-shot: загрузка renardo samples
- `voice-assistant` — Vosk STT, TTS, LLM dialogue
- `telegram-bot` — Telegram operator interface
- `cadvisor-vision` (profile: monitoring) — Container metrics
- `promtail-vision` (profile: monitoring) — Log shipping

Если список отличается от ожидаемого — использовать реальные данные из compose-файлов.

**Verification**:
```bash
echo "Main Pi container count:"
grep "container_name:" docker/main/docker-compose.yaml | wc -l

echo "Vision Pi container count:"
grep "container_name:" docker/vision/docker-compose.yaml | wc -l
```

---

### Wave 2: Обновление документации

#### Task 01-02-02: Обновить SYSTEM_OVERVIEW.md — Docker-секция (разделы 6.x)

**Type**: edit  
**Autonomous**: true  
**Files modified**:
- `docs/architecture/SYSTEM_OVERVIEW.md`

**Description**:

Файл `docs/architecture/SYSTEM_OVERVIEW.md` (~811 строк, версия v1.1.0 / 2025-10-24). Обновить следующие секции:

**1. Обновить раздел со структурой `docker/` (обычно раздел 6.1)**

Заменить список Vision Pi директорий на актуальный. Убрать `apriltag/` как отдельный сервис. Добавить:
- `ceiling-camera/` — USB потолочная камера
- `supercollider/` — audio synthesis server  
- `telegram_bot/` — Telegram operator interface
Примечание: AprilTag интегрирован в `oak-d` контейнер, отдельного сервиса нет.

В секцию с описанием `docker/vision/` добавить явную заметку:
> **Примечание**: `docker/vision/apriltag/` — orphaned директория. AprilTag detection работает **внутри `oak-d` контейнера**. Отдельного `apriltag` сервиса нет.

**2. Заменить/дополнить таблицы сервисов**

Если в документе есть секция со списком контейнеров — полностью заменить на актуальную. Если нет — добавить перед диаграммами зависимостей.

Добавить три таблицы:

```markdown
#### Main Pi — сервисы (11)

| Контейнер | Назначение | Зависит от |
|-----------|-----------|------------|
| `zenoh-router` | Zenoh Router (peer mode), мост к облаку | — |
| `twist-mux` | Мультиплексирование cmd_vel с приоритетами | `zenoh-router` |
| `micro-ros-agent` | UART↔ROS2 bridge для ESP32 Sensor Hub | `zenoh-router` |
| `robot-state-publisher` | TF дерево из URDF | `zenoh-router` |
| `rtabmap` | LiDAR-only SLAM + ICP odometry | `zenoh-router`, `lslidar` |
| `ros2-control` | ros2_control manager + VESC CAN драйвер | `zenoh-router` |
| `lslidar` | LS LiDAR N10 driver | `zenoh-router` |
| `perception` | reflection_node, context_aggregator, health_monitor | `zenoh-router` |
| `nav2` | Nav2 navigation stack | `zenoh-router` |
| `teleop` | Joystick teleoperation (SBUS/serial) | `zenoh-router` |
| `cadvisor` *(profile: monitoring)* | Container metrics → Prometheus | — |

#### Vision Pi — сервисы (10)

| Контейнер | Назначение | Зависит от |
|-----------|-----------|------------|
| `zenoh-router-vision` | Zenoh Router (client mode), подключается к Main Pi | — |
| `oak-d` | OAK-D Lite driver + встроенный AprilTag detector | `zenoh-router-vision` |
| `led-matrix` | LED matrix controller (381 NeoPixel LEDs) | `zenoh-router-vision` |
| `ceiling-camera` | USB потолочная камера 720p, локализация | `zenoh-router-vision` |
| `supercollider` | Audio synthesis server (JACK/ALSA) | — |
| `voice-resources-init` | One-shot: загрузка renardo samples в volume | — |
| `voice-assistant` | Vosk STT, TTS, LLM dialogue | `zenoh-router-vision`, `supercollider`, `voice-resources-init` |
| `telegram-bot` | Telegram operator interface с LLM и голосом | `zenoh-router-vision` |
| `cadvisor-vision` *(profile: monitoring)* | Container metrics → Prometheus | — |
| `promtail-vision` *(profile: monitoring)* | Log shipping → Loki | — |

#### Monitoring Machine — отдельная машина

> **Важно**: Grafana, Prometheus и Loki развёртываются на **отдельной машине** (не на Main Pi и не на Vision Pi). Конфигурация: `docker/monitoring/docker-compose.yaml`. Агенты `cAdvisor` и `Promtail` на каждом Pi отправляют данные на неё.

| Сервис | Назначение |
|--------|-----------|
| `prometheus` | Сбор метрик с cAdvisor на обоих Pi |
| `loki` | Агрегация логов от promtail |
| `grafana` | Дашборды: CPU, RAM, container health, logs |
```

**3. Обновить диаграмму зависимостей Vision Pi**

Найти Mermaid диаграмму для Vision Pi (содержит `zenoh-router-vision`). Добавить в неё пропущенные узлы:
- `ceiling-camera`
- `supercollider`  
- `voice-resources-init`
- `telegram-bot`
- `ollama` (помечен как `[profile: ai]`)

Стрелки зависимостей для `voice-assistant`: добавить `supercollider --> voice-assistant` и `voice-resources-init --> voice-assistant`.

**4. Обновить метаданные документа**

Изменить в заголовке/frontmatter:
- Версию: `v1.1.0` → `v1.2.0`
- Дату: `2025-10-24` → `2026-05-15`

**Verification**:
```bash
# Проверить наличие всех Vision Pi сервисов
for service in "telegram-bot" "ceiling-camera" "supercollider" "voice-resources-init" "ollama" "led-matrix"; do
  grep -q "$service" docs/architecture/SYSTEM_OVERVIEW.md \
    && echo "✅ $service found" \
    || echo "❌ FAIL: $service not found"
done

# Убедиться что apriltag не указан как отдельный сервис
grep -n "apriltag.*service\|сервис.*apriltag" docs/architecture/SYSTEM_OVERVIEW.md \
  && echo "⚠️ Check apriltag references" \
  || echo "✅ No active apriltag service references"

# Убедиться что teleop есть в Main Pi
grep -q "teleop" docs/architecture/SYSTEM_OVERVIEW.md \
  && echo "✅ teleop present" \
  || echo "❌ FAIL: teleop missing"

# Проверить Monitoring = отдельная машина
grep -q "отдельн\|separate" docs/architecture/SYSTEM_OVERVIEW.md \
  && echo "✅ Monitoring separate machine mentioned" \
  || echo "❌ FAIL: monitoring scope unclear"
```

---

#### Task 01-02-03: Синхронизировать ROADMAP.md с фактическим статусом фич (DOCS-04)

**Type**: edit  
**Autonomous**: true  
**Files modified**:
- `ROADMAP.md`

**Description**:

Прочитать файл `ROADMAP.md` и сопоставить с реальным состоянием проекта (май 2026):

**Фичи которые реализованы и должны быть помечены как DONE/✅**:
- LiDAR SLAM (rtabmap LiDAR-only mode)
- ICP Odometry
- AprilTag initial pose (интегрирован в oak-d)
- Voice assistant (Vosk STT + TTS + LLM dialogue)
- LED matrix animations (381 NeoPixel, 5 панелей)
- Joystick teleop (SBUS/serial — BLE заблокирован kernel bug)
- Zenoh DDS коммуникация (Main Pi ↔ Vision Pi)
- Telegram operator interface (`telegram-bot` на Vision Pi)
- MCP tools (`rob_box_mcp_tools`)
- Docker-based deployment через GitHub Actions CI/CD

**Фичи которые запланированы на Milestone 2 (не реализованы)**:
- OAK-D depth → Nav2 3D voxel costmap
- Потолочная камера как основной локализатор (ceiling-camera запущена, но интеграция с Nav2 — Milestone 2)
- AI HAT+ (Hailo-8L) object detection
- Voice commands → реальные навигационные действия

**Конкретные изменения**:
1. Для каждого реализованного пункта изменить статус с `[ ]` на `[x]` (или аналогичный маркер в формате файла)
2. Добавить дату если не указана: `(реализовано: до мая 2026)`
3. Если ROADMAP.md содержит фичи в формате milestone — перенести реализованные в "Milestone 1 ✅ COMPLETE" секцию
4. Добавить примечание о joystick: `⚠️ BLE заблокирован kernel bug 6.14.0-raspi, используется SBUS/serial`

Сначала прочитать полный ROADMAP.md, затем обновить только статусы — не переписывать структуру документа.

**Verification**:
```bash
# Проверить что telegram-bot упомянут как реализованное
grep -n "telegram\|Telegram" ROADMAP.md  # должен быть с маркером ✅ или [x]

# Проверить что voice assistant помечен реализованным
grep -n "voice\|голос" ROADMAP.md | head -10  # наличие статуса

# Убедиться что BLE предупреждение есть
grep -n "BLE\|kernel bug\|6.14.0" ROADMAP.md \
  && echo "✅ BLE warning present" \
  || echo "⚠️ Consider adding BLE kernel bug note"
```

---

## Verification

```bash
# === Plan 01-02 Final Verification ===

echo "=== SYSTEM_OVERVIEW.md completeness ==="

# Сверить container_name в compose с упоминаниями в SYSTEM_OVERVIEW.md
echo "Main Pi containers in compose:"
grep "container_name:" docker/main/docker-compose.yaml | awk '{print $2}'

echo "Vision Pi containers in compose:"
grep "container_name:" docker/vision/docker-compose.yaml | awk '{print $2}'

echo ""
echo "Checking Vision Pi services in SYSTEM_OVERVIEW.md..."
for service in "telegram-bot" "ceiling-camera" "supercollider" "voice-resources-init" "ollama" "led-matrix" "oak-d" "voice-assistant" "zenoh-router-vision"; do
  grep -q "$service" docs/architecture/SYSTEM_OVERVIEW.md \
    && echo "  ✅ $service" \
    || echo "  ❌ MISSING: $service"
done

echo ""
echo "Checking Main Pi services in SYSTEM_OVERVIEW.md..."
for service in "teleop" "rtabmap" "nav2" "perception" "lslidar" "ros2-control" "twist-mux" "zenoh-router" "micro-ros-agent"; do
  grep -q "$service" docs/architecture/SYSTEM_OVERVIEW.md \
    && echo "  ✅ $service" \
    || echo "  ❌ MISSING: $service"
done

echo ""
echo "=== ROADMAP.md sync check ==="
grep -c "\[x\]\|✅\|DONE\|done" ROADMAP.md \
  && echo "✅ Some items marked as done" \
  || echo "⚠️ No items marked done in ROADMAP.md"
```
