# Plan 01-03: Создание и обновление README для пакетов src/

**Phase**: 1 — Аудит документации  
**Goal**: Каждый пакет в `src/` имеет актуальный README с описанием нод, топиков и параметров  
**Requirements**: DOCS-05

---

## Must-Haves (from success criteria)

- [ ] `for d in src/*/; do [ -f "$d/README.md" ] && echo OK || echo MISSING; done` — все строки начинаются с `OK:`
- [ ] `grep -rn "jazzy" src/rob_box_voice/README.md` возвращает 0 результатов
- [ ] `rob_box_telegram/README.md` описывает ноды, подписанные и публикуемые топики
- [ ] `rob_box_bringup/README.md` описывает все launch-файлы и их назначение
- [ ] `rob_box_animations/README.md` использует имя контейнера `led-matrix` (не `animation-player`)
- [ ] `rob_box_perception/README.md` не содержит "Q1 2025 рефакторинг" как будущие планы
- [ ] `rob_box_mcp_tools/README.md` содержит список MCP tools

---

## Tasks

### Wave 1: Создание новых README (5 пакетов без документации)

Все задачи в Wave 1 независимы и могут выполняться параллельно.

---

#### Task 01-03-01: Создать README для rob_box_telegram

**Type**: create  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_telegram/README.md`

**Description**:

Создать `src/rob_box_telegram/README.md`. Информация получена из `telegram_node.py` docstring.

Содержимое README:

```markdown
# rob_box_telegram

Telegram-интерфейс оператора для Rob Box: управление роботом, просмотр камер, голосовые сообщения и выполнение MCP-команд через Telegram-бот.

## Описание

Пакет реализует Telegram-бота, работающего на Vision Pi, который соединяет Telegram Bot API с ROS 2 топиками. Оператор может отправлять текстовые команды, получать снимки с камер, запрашивать статус системы и управлять роботом движением через чат. Бот поддерживает LLM-диалог через MCP инструменты.

## Ноды

| Нода | Назначение |
|------|-----------|
| `telegram_node` | Главная нода: Telegram Bot API ↔ ROS 2 мост |
| `mcp_bridge` | Вспомогательный компонент: выполнение MCP tool calls с таймаутом |

## Топики

### Подписывается

| Топик | Тип | Описание |
|-------|-----|---------|
| `/camera/camera/color/image_raw/compressed` | `sensor_msgs/CompressedImage` | Фронтальная камера OAK-D |
| `/camera/camera/depth/image_rect_raw/compressedDepth` | `sensor_msgs/CompressedImage` | Карта глубины OAK-D |
| `/ceiling_camera/image_raw/compressed` | `sensor_msgs/CompressedImage` | Потолочная USB-камера |
| `/rtabmap/grid_prob_map` | `nav_msgs/OccupancyGrid` | 2D SLAM карта |
| `/mcp/result` | `std_msgs/String` | Результаты выполнения MCP инструментов |
| `/mcp/tools` | `std_msgs/String` | Список доступных MCP инструментов |

### Публикует

| Топик | Тип | Описание |
|-------|-----|---------|
| `/voice/tts/request` | `std_msgs/String` | Запросы Text-to-Speech |
| `/cmd_vel_web` | `geometry_msgs/Twist` | Команды движения (приоритет 50 в twist-mux) |
| `/mcp/execute` | `std_msgs/String` | Запросы на выполнение MCP инструментов |

## Параметры

| Параметр | По умолчанию | Описание |
|----------|-------------|---------|
| `TELEGRAM_BOT_TOKEN` | *(env var)* | Токен бота из BotFather |
| `TELEGRAM_ALLOWED_USERS` | *(env var)* | Список разрешённых Telegram user_id через запятую |

## Запуск

Запускается в Docker контейнере `telegram-bot` на Vision Pi:

```bash
# Статус
docker logs telegram-bot -f

# Перезапуск
docker compose -f docker/vision/docker-compose.yaml restart telegram-bot
```

Токен бота задаётся через переменную окружения `TELEGRAM_BOT_TOKEN` в файле `docker/vision/.env`.

## Зависимости

- `python-telegram-bot >= 20.0` (async API)
- `rob_box_mcp_tools` (MCP инструменты для выполнения команд)
- Vision Pi: `zenoh-router-vision` должен быть запущен до старта ноды
```

**Verification**:
```bash
[ -f src/rob_box_telegram/README.md ] && echo "✅ README created" || echo "❌ FAIL"
grep -q "telegram_node\|mcp_bridge" src/rob_box_telegram/README.md && echo "✅ Nodes documented" || echo "❌ FAIL"
grep -q "/mcp/execute\|/cmd_vel_web" src/rob_box_telegram/README.md && echo "✅ Topics documented" || echo "❌ FAIL"
```

---

#### Task 01-03-02: Создать README для rob_box_bringup

**Type**: create  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_bringup/README.md`

**Description**:

Создать `src/rob_box_bringup/README.md`. Launch-файлы: `display.launch.py`, `display_simple.launch.py`, `rob_box_control.launch.py`, `complete_system.launch.py`.

Перед созданием README — прочитать каждый launch-файл в `src/rob_box_bringup/launch/` чтобы понять что запускает, какие параметры принимает. Затем написать README по шаблону:

```markdown
# rob_box_bringup

Launch-пакет для запуска системы Rob Box: отображение, контроль движения и полная система.

## Описание

Пакет содержит launch-файлы для запуска различных конфигураций системы Rob Box вне Docker (например, для разработки, RViz-визуализации или тестирования компонентов). В production среде компоненты запускаются через Docker Compose (`docker/main/`, `docker/vision/`).

## Launch-файлы

| Файл | Назначение | Ключевые параметры |
|------|-----------|-------------------|
| `display.launch.py` | RViz + robot_state_publisher для визуализации URDF | `urdf_file` |
| `display_simple.launch.py` | Упрощённая визуализация без дополнительных нод | — |
| `rob_box_control.launch.py` | ros2_control + VESC драйвер | `port`, `can_id` |
| `complete_system.launch.py` | Полная система: navigation + control + perception | `use_sim_time` |

## Запуск

```bash
# Визуализация URDF в RViz (bare metal / dev machine)
ros2 launch rob_box_bringup display.launch.py

# Полная система (для разработки без Docker)
ros2 launch rob_box_bringup complete_system.launch.py

# Production: используйте Docker Compose вместо launch-файлов
cd docker/main && docker compose up -d
```

## Примечание

В production-среде эти launch-файлы НЕ используются напрямую. Каждый Docker-контейнер запускает соответствующие ноды через собственные entrypoint-скрипты. Launch-файлы предназначены для:
- Разработки и тестирования на dev-машине
- RViz-визуализации модели робота
- Bare metal запуска для отладки компонентов
```

При создании README — точно описать что делает каждый launch-файл (прочитать его содержимое).

**Verification**:
```bash
[ -f src/rob_box_bringup/README.md ] && echo "✅ README created" || echo "❌ FAIL"
grep -q "display.launch\|complete_system.launch\|rob_box_control.launch" src/rob_box_bringup/README.md \
  && echo "✅ Launch files documented" || echo "❌ FAIL"
```

---

#### Task 01-03-03: Создать README для rob_box_description

**Type**: create  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_description/README.md`

**Description**:

Создать `src/rob_box_description/README.md`. Пакет содержит: `urdf/`, `meshes/` (.stl файлы), `worlds/` (Gazebo), `rviz/` конфигурации.

Прочитать список файлов в `src/rob_box_description/urdf/` и `src/rob_box_description/meshes/` для точного описания.

```markdown
# rob_box_description

URDF/Xacro описание робота Rob Box: геометрия, TF-дерево, Gazebo-модель и mesh-файлы.

## Описание

Пакет содержит полное описание физической модели Rob Box в форматах URDF/Xacro. Используется для публикации TF-дерева, визуализации в RViz и симуляции в Gazebo. В Docker среде TF-дерево публикует контейнер `robot-state-publisher` на Main Pi.

## Структура

| Директория | Содержимое |
|-----------|-----------|
| `urdf/` | URDF/Xacro файлы описания робота |
| `meshes/` | 3D mesh-файлы (.stl) компонентов |
| `worlds/` | Gazebo world файлы для симуляции |
| `rviz/` | Конфигурации RViz для визуализации |

## Используемые TF-фреймы

| Фрейм | Описание |
|-------|---------|
| `base_link` | Основание робота (центр) |
| `laser_frame` | LiDAR сенсор (LS LiDAR N10) |
| `camera_link` | OAK-D Lite камера |
| `imu_link` | IMU сенсор (ESP32 Sensor Hub) |

## Запуск

```bash
# Публикация TF-дерева (bare metal)
ros2 launch rob_box_bringup display.launch.py

# В Docker (Main Pi) — автоматически через контейнер robot-state-publisher
docker logs robot-state-publisher
```

## Зависимости

- `robot_state_publisher` (ROS 2 kilted)
- `joint_state_publisher` (для RViz визуализации)
- `rviz2` (для отображения модели)
```

**Verification**:
```bash
[ -f src/rob_box_description/README.md ] && echo "✅ README created" || echo "❌ FAIL"
grep -q "urdf\|URDF\|TF\|mesh" src/rob_box_description/README.md \
  && echo "✅ URDF description present" || echo "❌ FAIL"
```

---

#### Task 01-03-04: Создать README для led_matrix_driver

**Type**: create  
**Autonomous**: true  
**Files modified**:
- `src/led_matrix_driver/README.md`

**Description**:

Создать `src/led_matrix_driver/README.md`. Нода `led_matrix_simple` управляет 381 NeoPixel LED через SPI (pi5neo библиотека). Параметры из `led_matrix_driver.py`: `num_leds=253`, `spi_speed_khz=1300`, `spi_device=/dev/spidev0.0`, `input_topic=led_matrix/data`. Подписывается на `ByteMultiArray`.

**Примечание**: Высокоуровневые анимации реализованы в `rob_box_animations` (пакет `ros2leds`). `led_matrix_driver` — низкоуровневый SPI драйвер.

```markdown
# led_matrix_driver

Низкоуровневый SPI-драйвер для управления матрицей NeoPixel LED (381 LED, 5 панелей).

## Описание

Пакет реализует ROS 2 ноду для управления матрицей NeoPixel LED через SPI-интерфейс Raspberry Pi 5. Использует библиотеку `pi5neo` для работы с `rpi_ws281x`. Нода получает данные о состоянии LED в виде массива байт и передаёт их напрямую через SPI. Высокоуровневые анимации реализованы в пакете `rob_box_animations`.

## Ноды

| Нода | Назначение |
|------|-----------|
| `led_matrix_simple` | SPI-драйвер: получает ByteMultiArray → отправляет в pi5neo |

## Топики

### Подписывается

| Топик | Тип | Описание |
|-------|-----|---------|
| `led_matrix/data` | `std_msgs/ByteMultiArray` | Массив байт RGB-данных для всей матрицы |

## Параметры

| Параметр | По умолчанию | Описание |
|----------|-------------|---------|
| `num_leds` | `253` | Количество LED (аппаратное ограничение одной цепи) |
| `spi_speed_khz` | `1300` | Скорость SPI в кГц |
| `spi_device` | `/dev/spidev0.0` | SPI устройство на Raspberry Pi 5 |
| `input_topic` | `led_matrix/data` | Топик для приёма LED-данных |

## Запуск

Запускается в Docker контейнере `led-matrix` на Vision Pi:

```bash
# Статус и логи
docker logs led-matrix -f

# Перезапуск
docker compose -f docker/vision/docker-compose.yaml restart led-matrix
```

## Зависимости

- `pi5neo` — Python библиотека для NeoPixel на Raspberry Pi 5 через SPI
- `rob_box_animations` — высокоуровневые анимации (используют этот драйвер)

## Аппаратура

- **LED**: 381 NeoPixel WS2812B (5 панелей 5×5 пикселей + 1 центральная)
- **Интерфейс**: SPI (`/dev/spidev0.0`)
- **Подключение**: Vision Pi GPIO
```

**Verification**:
```bash
[ -f src/led_matrix_driver/README.md ] && echo "✅ README created" || echo "❌ FAIL"
grep -q "led_matrix_simple\|ByteMultiArray\|spi_device" src/led_matrix_driver/README.md \
  && echo "✅ Node and topics documented" || echo "❌ FAIL"
grep -q "381\|NeoPixel" src/led_matrix_driver/README.md \
  && echo "✅ LED count documented" || echo "❌ FAIL"
```

---

#### Task 01-03-05: Создать README для rob_box_perception_msgs

**Type**: create  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_perception_msgs/README.md`

**Description**:

Создать `src/rob_box_perception_msgs/README.md`. Пакет содержит одно сообщение: `PerceptionEvent.msg`.

Поля `PerceptionEvent.msg` (из прочитанного файла):
- `stamp` (builtin_interfaces/Time)
- `vision_context` (string) — JSON от vision processing
- `pose` (geometry_msgs/Pose), `velocity` (geometry_msgs/Twist), `is_moving` (bool)
- `battery_voltage` (float32), `temperature` (float32)
- `apriltag_ids` (int32[])
- `system_health_status` (string) — "healthy" / "degraded" / "critical"
- `health_issues` (string[])
- `current_time_human` (string), `time_period` (string), `time_context_json` (string)
- `internet_available` (bool)
- `active_nodes`, `failed_nodes`, `missing_nodes` (string[])
- `equipment_summary_json` (string)
- `mapping_mode` (string) — "mapping" / "localization" / "unknown"
- `memory_summary` (string)
- `speech_summaries`, `robot_response_summaries`, `robot_thought_summaries`, `vision_summaries`, `system_summaries` (string)

```markdown
# rob_box_perception_msgs

Custom ROS 2 messages для пакета `rob_box_perception` — агрегированный контекст состояния робота.

## Описание

Пакет содержит кастомные ROS 2 типы сообщений, используемые perception системой Rob Box. Основное сообщение `PerceptionEvent` — это агрегированный снапшот состояния всей системы, который публикуется `context_aggregator_node` и потребляется `dialogue_node` для формирования контекста LLM.

## Messages

### PerceptionEvent

Агрегированный контекст состояния робота. Публикуется в топик `/perception/context`.

| Поле | Тип | Описание |
|------|-----|---------|
| `stamp` | `builtin_interfaces/Time` | Временная метка события |
| `vision_context` | `string` | JSON: описание визуальной сцены от OAK-D |
| `pose` | `geometry_msgs/Pose` | Текущая поза робота (из SLAM) |
| `velocity` | `geometry_msgs/Twist` | Текущая скорость |
| `is_moving` | `bool` | Флаг движения |
| `battery_voltage` | `float32` | Напряжение батареи (Вольт) |
| `temperature` | `float32` | Температура (°C) |
| `apriltag_ids` | `int32[]` | ID видимых AprilTag маркеров |
| `system_health_status` | `string` | "healthy" / "degraded" / "critical" |
| `health_issues` | `string[]` | Список текущих проблем системы |
| `current_time_human` | `string` | Время в читаемом формате: "2025-10-21 12:30:00" |
| `time_period` | `string` | "morning" / "day" / "evening" / "night" |
| `time_context_json` | `string` | Полный временной контекст (JSON) |
| `internet_available` | `bool` | Доступность интернета |
| `active_nodes` | `string[]` | Активные ROS 2 ноды |
| `failed_nodes` | `string[]` | Упавшие ноды |
| `missing_nodes` | `string[]` | Ожидаемые но отсутствующие ноды |
| `equipment_summary_json` | `string` | JSON: статус камер, LiDAR, моторов, SLAM |
| `mapping_mode` | `string` | "mapping" / "localization" / "unknown" |
| `memory_summary` | `string` | Краткое резюме недавних событий |
| `speech_summaries` | `string` | JSON array: суммаризованные вопросы пользователя |
| `robot_response_summaries` | `string` | JSON array: суммаризованные ответы робота |
| `robot_thought_summaries` | `string` | JSON array: внутренние мысли робота |
| `vision_summaries` | `string` | JSON array: суммаризованные визуальные наблюдения |
| `system_summaries` | `string` | JSON array: суммаризованные системные события |

## Использование

```python
from rob_box_perception_msgs.msg import PerceptionEvent

# В ROS 2 ноде
self.create_subscription(PerceptionEvent, '/perception/context', self.callback, 10)
```

## Зависимости

- `geometry_msgs` (pose, twist)
- `builtin_interfaces` (time)
- Используется: `rob_box_perception` (publisher), `rob_box_voice` (subscriber)
```

**Verification**:
```bash
[ -f src/rob_box_perception_msgs/README.md ] && echo "✅ README created" || echo "❌ FAIL"
grep -q "PerceptionEvent\|system_health_status\|mapping_mode" src/rob_box_perception_msgs/README.md \
  && echo "✅ Message fields documented" || echo "❌ FAIL"
```

---

### Wave 2: Обновление существующих README (4 пакета)

Все задачи Wave 2 независимы между собой.

---

#### Task 01-03-06: Обновить rob_box_voice/README.md — fix jazzy, добавить новые фичи

**Type**: edit  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_voice/README.md`

**Description**:

В файле `src/rob_box_voice/README.md`:

1. **Заменить все `ros-jazzy-*` на `ros-kilted-*`**:
   - `ros-jazzy-audio-common-msgs` → `ros-kilted-audio-common-msgs`
   - Любые другие `jazzy` зависимости

2. **Обновить секцию установки**: убрать инструкции bare metal установки (или добавить примечание что в production используется Docker). Добавить:
   ```
   > **Production**: используется Docker контейнер `voice-assistant` на Vision Pi.
   > Bare metal установка нужна только для разработки.
   ```

3. **Добавить секцию "Новые возможности (2026)"** или обновить список функций:
   - `USE_SKILLS` — флаг включения системы скиллов голосового ассистента
   - `VoiceMemory` — долгосрочная память разговоров
   - `MusicSkill` — воспроизведение музыки через SuperCollider/Renardo
   - Ollama интеграция — поддержка локального LLM (запускается через profile `ai`)
   - SuperCollider/Renardo — audio synthesis вместо простых sound файлов

4. **Если есть deprecated раздел** о "Q1 2025 планах" — убрать или обновить статус.

5. **Исправить имя Docker контейнера** если упомянуто: `voice-assistant` (не `voice`).

**Verification**:
```bash
grep -c "jazzy" src/rob_box_voice/README.md  # должно быть 0
grep -q "kilted" src/rob_box_voice/README.md && echo "✅ kilted referenced" || echo "⚠️ Check ROS distro references"
grep -q "USE_SKILLS\|VoiceMemory\|SuperCollider\|Ollama" src/rob_box_voice/README.md \
  && echo "✅ New features documented" || echo "❌ FAIL: new features missing"
```

---

#### Task 01-03-07: Обновить rob_box_perception/README.md — обновить статус рефакторинга

**Type**: edit  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_perception/README.md`

**Description**:

В файле `src/rob_box_perception/README.md`:

1. **Найти все ссылки на "Q1 2025 рефакторинг"** или "плановый рефакторинг Q1 2025". Заменить на описание текущего состояния:
   - Если код в `src/rob_box_perception/` уже рефакторирован — убрать "план" и написать "реализовано"
   - Удалить формулировки типа "будет рефакторирован", "планируется переработать"

2. **Обновить описание `vision_stub_node`**: убрать пометку "TODO" или "заглушка". Это рабочая реализация-прокси для получения визуального контекста от Vision Pi.

3. **Добавить параметры нод** если они отсутствуют. Прочитать `src/rob_box_perception/rob_box_perception/health_monitor.py` и `context_aggregator_node.py` для получения списка параметров (`self.declare_parameter`).

4. **Обновить дату** в заголовке документа на 2026-05-15.

**Verification**:
```bash
grep -n "Q1 2025\|рефакторинг.*план\|планируется" src/rob_box_perception/README.md \
  && echo "⚠️ Found stale Q1 2025 references — review needed" \
  || echo "✅ No stale refactoring plans found"
grep -q "vision_stub_node" src/rob_box_perception/README.md \
  && echo "✅ vision_stub_node documented" || echo "⚠️ vision_stub_node not mentioned"
```

---

#### Task 01-03-08: Обновить rob_box_mcp_tools/README.md — список tools и обновление статуса

**Type**: edit  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_mcp_tools/README.md`

**Description**:

В файле `src/rob_box_mcp_tools/README.md`:

1. **Найти и устранить "Q1 2025 рефакторинг"** аналогично Task 01-03-07.

2. **Добавить список MCP tools**. Сначала прочитать файлы в `src/rob_box_mcp_tools/rob_box_mcp_tools/` для получения актуального списка tools (искать `@server.tool` или `def tool_`). Затем добавить таблицу:
   ```markdown
   ## MCP Tools
   
   | Tool | Назначение |
   |------|-----------|
   | `get_robot_status` | Текущее состояние робота (здоровье, позиция, батарея) |
   | ... | ... |
   ```

3. **Описать топики MCP**:
   - `/mcp/execute` (`std_msgs/String`) — входящие tool call запросы (JSON)
   - `/mcp/result` (`std_msgs/String`) — результаты выполнения (JSON)
   - `/mcp/tools` (`std_msgs/String`) — список доступных tools (публикуется при старте)

4. **Исправить ноды** если перечислены неверно. Проверить реальный список Python файлов в пакете.

**Verification**:
```bash
grep -n "Q1 2025" src/rob_box_mcp_tools/README.md \
  && echo "⚠️ Stale Q1 2025 reference found" \
  || echo "✅ No stale references"
grep -q "/mcp/execute\|/mcp/result\|MCP Tools\|mcp tools" src/rob_box_mcp_tools/README.md \
  && echo "✅ MCP tools/topics documented" || echo "❌ FAIL"
```

---

#### Task 01-03-09: Обновить rob_box_animations/README.md — исправить имя контейнера

**Type**: edit  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_animations/README.md`

**Description**:

В файле `src/rob_box_animations/README.md`:

1. Найти все вхождения `animation-player` и заменить на `led-matrix`:
   - `docker exec -it animation-player ros2 service call ...` → `docker exec -it led-matrix ros2 service call ...`
   - `docker logs animation-player` → `docker logs led-matrix`
   - Любые другие упоминания в примерах команд

2. Убедиться что в документе нет других несуществующих имён контейнеров.

3. Если есть ссылка на `docker/vision/led-matrix/` — проверить что путь верный.

**Verification**:
```bash
grep -c "animation-player" src/rob_box_animations/README.md  # должно быть 0
grep -q "led-matrix" src/rob_box_animations/README.md && echo "✅ Correct container name" || echo "⚠️ No led-matrix reference"
```

---

#### Task 01-03-10: Обновить rob_box_teleop/README.md — добавить BLE kernel bug note

**Type**: edit  
**Autonomous**: true  
**Files modified**:
- `src/rob_box_teleop/README.md`

**Description**:

В файле `src/rob_box_teleop/README.md` в раздел "Известные проблемы" или "Troubleshooting" добавить параграф:

```markdown
### ⚠️ BLE джойстик: kernel bug в Linux 6.14.0-raspi

Bluetooth-подключение джойстика заблокировано известным kernel bug в Linux 6.14.0-raspi (Raspberry Pi kernel).  
**Симптомы**: устройство обнаруживается, но `/dev/input/jsX` не появляется, `jstest` зависает.  
**Решение**: используйте проводное управление (SBUS/serial) или дождитесь исправления ядра.  
**Статус**: открытый upstream issue.
```

Если в README уже есть секция с проблемами — добавить в неё. Если нет — добавить секцию перед "Contributing" или в конец файла.

**Verification**:
```bash
grep -q "kernel bug\|BLE\|6.14.0" src/rob_box_teleop/README.md \
  && echo "✅ BLE kernel bug documented" \
  || echo "❌ FAIL: BLE bug not in teleop README"
```

---

## Verification

```bash
# === Plan 01-03 Final Verification ===

echo "=== Check all src/ packages have README ==="
MISSING=0
for d in src/*/; do
  pkg=$(basename "$d")
  if [ ! -f "$d/README.md" ]; then
    echo "  ❌ MISSING: $pkg"
    MISSING=$((MISSING + 1))
  else
    echo "  ✅ OK: $pkg"
  fi
done
[ $MISSING -eq 0 ] && echo "✅ All packages have README" || echo "❌ FAIL: $MISSING packages missing README"

echo ""
echo "=== Check no 'jazzy' in voice README ==="
count=$(grep -c "jazzy" src/rob_box_voice/README.md 2>/dev/null || echo 0)
[ "$count" -eq 0 ] && echo "✅ No jazzy references" || echo "❌ FAIL: Found $count jazzy references"

echo ""
echo "=== Check no 'animation-player' in any src/ README ==="
count=$(grep -rn "animation-player" src/*/README.md 2>/dev/null | wc -l)
[ "$count" -eq 0 ] && echo "✅ No animation-player references" || echo "❌ FAIL: Found $count references"

echo ""
echo "=== Check new READMEs have key content ==="
grep -q "telegram_node\|/mcp/execute" src/rob_box_telegram/README.md \
  && echo "✅ rob_box_telegram README has topics" || echo "❌ FAIL: rob_box_telegram incomplete"
grep -q "PerceptionEvent" src/rob_box_perception_msgs/README.md \
  && echo "✅ rob_box_perception_msgs README has message" || echo "❌ FAIL"
grep -q "led_matrix_simple\|spi_device" src/led_matrix_driver/README.md \
  && echo "✅ led_matrix_driver README has node info" || echo "❌ FAIL"
grep -q "display.launch\|complete_system" src/rob_box_bringup/README.md \
  && echo "✅ rob_box_bringup README has launch files" || echo "❌ FAIL"
```
