Проверка фикса ADR-0127 на БОЕВЫХ эмбеддингах из провалившегося прогона
=======================================================================

Что сделано: из /data/speakers.db робота взяты два эмбеддинга, которые
night-marathon (run 35667281570, акт 2) записал в слитый профиль dc417cef —
emb1 = Саша (TTS anton), emb2 = Борис (TTS ermil). На них проиграна та же
последовательность регистраций, но уже пропатченным модулем
speaker_embeddings.py.

Где: контейнер voice-assistant на Vision Pi (10.1.1.21), 22.09.2026.
Скрипт: replay_act2.py (временный, лежит в /tmp контейнера).

    $ docker cp src/rob_box_voice/rob_box_voice/utils/speaker_embeddings.py \
          voice-assistant:/tmp/
    $ docker exec voice-assistant python3 /tmp/replay_act2.py

--- stdout ---------------------------------------------------------------

взято эмбеддингов из боевой БД: 2 (id=[1, 2])
cos(Саша[anton], Борис[ermil]) = 0.8464   (порог слияния 0.75)
n201 register 'Саша'  -> id=39c8bf1a reused=False
n204 register 'Борис' -> id=e8a95b62 reused=False name_conflict=True conflict_name='Саша' score=0.8464
--- итог в БД ---
   Саша     39c8bf1a  эмбеддингов: 1  эпитет: Странник
   Борис    e8a95b62  эмбеддингов: 1  эпитет: None
--- кого узнаёт identify() ---
   фраза Саша   -> Саша (1.000)
   фраза Борис  -> Борис (1.000)

--- stderr (логгер модуля) ------------------------------------------------

⚠️ register_or_merge: голос похож на 'Саша' (id=39c8bf1a, score=0.846 >= 0.75),
но регистрируют как 'Борис' — конфликт имён, слияния НЕ делаю, завожу
отдельный профиль (ADR-0127). Если это один человек — склеить вручную:
merge_speakers(src=<новый>, dst=39c8bf1a-d095-4a5b-9703-6d0f9fa59c43)

--- как было до фикса -----------------------------------------------------

speakers: 1 | embeddings: 2
   Борис        dc417cef  эмбеддингов: 2

(профиль Саши, переименованный в Бориса; сам Саша исчез)

--- чего этот замер НЕ доказывает -----------------------------------------

identify() здесь возвращает 1.000, потому что запрос — буквально один из
сохранённых референсов. На живом прогоне новая фраза даст скор ниже, и при
cos=0.846 между дикторами опознание всё равно будет монеткой. Целостность
БД фикс чинит, различимость голосов — нет; за неё отвечает новая таблица
map_tts_voice (evidence/tts-voice-distinctness-2026-09-22/).

Полноценный e2e акта 2 после фикса НЕ прогонялся.
